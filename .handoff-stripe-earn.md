# Handoff — stripe_earn.py (EARN-side Checkout helper)

## Phase 1: recon — DONE
- No pre-existing earn-side Checkout helper. `stripe_money.earn()` exists but is
  Payment-Link based (Product+Price+payment_links), no `client_reference_id`, no
  per-session poll. So a NEW module is correct (not a duplicate).
- Key loader to reuse: `stripe_money._read_hermes_env()` / `_secret()` / `detect_key()`.
  Reads `~/.hermes/.env` then env; detects by presence/length; NEVER prints value.
  SDK = stdlib urllib form-encoded POST with `Authorization: Bearer <secret>`.
- `python3 stripe_money.py` => present=true, kind=test, length=107. Good test key.
- macOS has NO `timeout` binary; use `perl -e 'alarm N; exec @ARGV'` for internal caps.

## Phase 2: build — DONE
- NEW file: `stripe_earn.py` (did NOT touch dashboard/*, orchestrator.py, build_runner.py,
  ledger.py, stripe_money.py, stripe_webhook.py).
- API:
  - `create_checkout_session(job_id, amount_cents, currency="usd",
    product_name="Producer video", success_url=None, cancel_url=None, dry_run=False)`
    -> {session_id, url, payment_status, status, livemode, client_reference_id,
        amount_cents, currency}; dry_run -> {dry_run:True, params:{...}}.
  - `get_session_status(session_id)` -> payment_status ('paid'|'unpaid'|'no_payment_required').
  - `get_session(session_id)` -> full dict.
  - `build_session_params(...)` -> single source of truth for request shape.
- Safety: `_assert_test_key()` refuses missing/live keys (LiveKeyRefused); every live call
  asserts `livemode==false` (LiveModeError otherwise). Key never printed. mode=payment,
  inline price_data line_item, client_reference_id=job_id, dashboard success/cancel URLs
  (http://localhost:3030/?paid=<job> / ?cancelled=<job>).
- CLI: `create --job --amount --name [--dry-run]`, `status --session`.
- NEW test: `tests/test_stripe_earn.py` (offline param-shape + live-key-refusal).

## Phase 3: verify — DONE ($0, test mode, no payment completed)
- Offline unit tests: 15 checks, 0 failures (param shape + live-key refusal).
- Dry-run CLI: params well-formed, no network call.
- REAL test-mode session created:
  - session_id = cs_test_a1wq06ojiwEo0p55jmN7B121SLVCj4QcW1bsw1JidK8WrhLwnFsDqJNcYs
  - url host = checkout.stripe.com (hosted Checkout)
  - payment_status = unpaid, status = open
  - livemode = FALSE
  - client_reference_id = test-001
- status poll on that session => payment_status = unpaid (matches).
- No key leaked (grep of all CLI output for sk_/rk_ = 0 hits).
- Did NOT complete a payment, did NOT stripe login/listen, did NOT touch spend-side or
  run any build. Webhook (checkout.session.completed) still blocked on Dennis's
  `stripe login`; poll backstop is live and working.
