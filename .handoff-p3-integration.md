# P3 — the integration (screenshots + walkthrough into a STANDARD build)

## STATUS: planner + wiring code DONE; full mock-pay build verify NEXT.

## STEP A — planner re-admits screenshot/walkthrough for STANDARD (verified at plan level, $0)
- `plan_job.py`
  - `_QUALITY_PROMPT["standard"]` rewritten: STANDARD = title -> 1-2 screenshot -> 1 walkthrough -> title (model null). No motion_graphic/cinematic.
  - `_enforce_quality` STANDARD branch now calls NEW `_enforce_standard_structure(plan, scenes)`: deterministic backstop that forces the shape (re-types stray cinematic/motion_graphic content scenes into screenshot/walkthrough; synthesizes a walkthrough if none; rewrites the walkthrough brief to the emphasis-specific multi-step goal via NEW `_walkthrough_brief_for_emphasis(brand, emphasis)`).
  - `_standard_template_plan` rewritten to title -> 2 screenshot -> walkthrough -> title (durations sum to target; emphasis threaded). `_template_plan` takes `emphasis`.
  - Premium branch UNCHANGED.
- `validate_planner.py`
  - `schema_check` `allowed_types` now quality-gated: standard={title,screenshot,walkthrough}, premium={title,cinematic,motion_graphic}.
  - standard branch requires n_screenshot>=1, n_walkthrough==1, n_cinematic==0; premium requires n_walk==0,n_shot==0 + >=2 cinematic.
  - job-keys strict check tolerates optional `emphasis`. SYSTEM_PROMPT + model-null + VO rules updated for screenshot/walkthrough.
- `plan_schema.py`: ALLOWED_TYPES += "screenshot"; screenshot in the model-null set; emphasis tolerated as an optional job key (presence-only required-key loop already allows extras).
- `planner-prompt.md`: replaced the "NEVER include a walkthrough" rule with the STANDARD structure (prose + SYSTEM PROMPT block + type enum comment).

VERIFIED at plan level ($0, live super-free brain): plan for orinovate.com + emphasis="instant quoting" => types `['title','screenshot','screenshot','walkthrough','title']`, total 30s, validate_plan + schema_check(standard) clean, walkthrough brief = "From the homepage, navigate to and demonstrate the instant quoting feature of Orinovate, showing 2-3 distinct steps."

## STEP B — emphasis threaded
- `plan_job.plan_job(..., emphasis=None)` -> stamped into `job["emphasis"]` (BEFORE _enforce_quality so the structure backstop can name the feature). Internal `_wordmark` hint stamped then stripped before return.
- `validate_planner` (job-keys ~271) + `plan_schema` (job keys ~162) tolerate emphasis.
- `build_runner.py` argparse `--emphasis` + `run(..., emphasis="")` -> plan_job. plan_job.py CLI `--emphasis` too.

## STEP C — real assets wired into scenes
- `style_fill.py`:
  - NEW `ARCH_SCREENSHOT="apple-screenshot"` + `_shape_screenshot` (imageSrc/frame/caption). role_map "screenshot"/"site" -> ARCH_SCREENSHOT; shaper registered.
  - NEW `wire_captured_assets(plan, out_dir)` (called in run_pipeline BEFORE build_props): maps Nth screenshot scene -> shot-NN.png (data.imageSrc + caption=page url); walkthrough scene -> clip from `WS_WALKTHROUGH_CACHE` (cache override) else `clips/NN_<sid>.mp4` (data.videoSrc). `_load_screenshot_manifest` + `_walkthrough_clip_for_run` helpers.
  - `_stage_audio` (P1/P2) already stages imageSrc + videoSrc into studio/public/.
- `build_timeline.py`: `_TYPE_TO_ROLE` += screenshot/site -> "screenshot".
- `adapters.py`:
  - `generate_walkthrough` CACHE OVERRIDE: if WS_WALKTHROUGH_CACHE exists, re-encode (+faststart, stream-copy then re-encode fallback) into out_path; returns {cached:True}. Mode-independent.
  - NEW `generate_screenshot_clip(scene, out_path, mode, shots_dir=None)`: builds a clip from shot-NN.png (scale/pad to 1920x1080 white), synth floor if no capture.
- `orchestrator.py`:
  - `FREE_TYPES += "screenshot"`; `shot_counter` + a `screenshot` FREE branch -> generate_screenshot_clip (passes 0-based _shot_index).
  - `_walkthrough_gen` now mode-INDEPENDENT: honors WS_WALKTHROUGH_CACHE (always calls generate_walkthrough); placeholder only when NO cache + mock studio.
  - `adapters_tool` += screenshot -> "site-capture".
- `build_runner.py`: capture screenshots BEFORE orchestrate too (idempotent), so orchestrator screenshot clips use the real PNG.

VERIFIED ($0): `wire_captured_assets` on a synthetic plan + runs/verify-decouple/screenshots + WS_WALKTHROUGH_CACHE=/tmp/walk-smoke.mp4 -> screenshot-home=shot-01.png (caption orinovate.com), screenshot-inner=shot-02.png (caption .../materials), walkthrough=walk-smoke.mp4 (has_walkthrough_clip True). All imports clean.

## NEXT (build verify)
1. Full mock-pay STANDARD build reusing the cached clip (no NIM):
   `WS_WALKTHROUGH_CACHE=/tmp/walk-smoke.mp4 PRODUCER_SIMULATE_PAID=1 python3 build_runner.py --url https://www.orinovate.com --goal "A 30-second brand explainer" --emphasis "instant quoting" --run-id p3-standard-verify --mode mock --quality standard --brain super-free --duration 30` (FOREGROUND, timeout 600000). Confirm delivered.
2. Extract scene-center frames -> /tmp/p3-verify/ and READ: branded white open? real orinovate SCREENSHOT in a browser card? WALKTHROUGH clip in a branded frame? CTA close? all white/on-brand, zero orange, no hollow copy.
3. ./run_all_tests.sh --no-eval (rebaseline only STALE assertions from intentional changes).

## DO NOT: commit · touch :3030 · run real walk-agent (use the cache) · Premium/real-pay.
