# Handoff — Fixed-tier customer pricing

## Status: DONE (code), with a test-rebaseline flag for the orchestrator

## The change
Customer PRICE is now a FIXED amount per quality tier, NOT cost-plus:
- standard -> 999c ($9.99)
- premium  -> 1999c ($19.99)

## Files changed (ONLY these two, per scope)
- `pricing.json`
  - `version` 3 -> 4, `model` "cost_plus" -> "fixed_tier"
  - added `quality.standard.tier_price_cents = 999`
  - added `quality.premium.tier_price_cents = 1999`
  - rewrote `_doc` to describe fixed-tier; old cost-plus knobs (markup/price_floor_cents/round_to_cents) retained but no longer drive the customer price
- `pricing.py`
  - new helper `tier_price_cents(quality, pricing=None)` — single source of truth for the customer price; reads `quality.<tier>.tier_price_cents`, falls back to `{standard:999, premium:1999}`
  - `price_for_plan(...)` now sets `base_price = tier_price_cents(q)` instead of `base_price_for_cogs(...)`; still threads `plan_cogs` for COGS/margin; return `model` -> "fixed_tier"
  - module + function docstrings updated
  - `base_price_for_cogs(...)` body UNCHANGED (retained for back-compat; still reads markup/floor/round_to)

## Pricing rule (exact)
    price_cents = quality.<tier>.tier_price_cents   # standard 999, premium 1999
COGS / plan_cogs_cents / margin are still computed: margin = (price - cogs) / price.

## Computed quotes (VERIFIED)
- standard, cogs=0   -> base_price_cents=999,  total_price_cents=999  ($9.99), margin 1.0
- premium,  cogs=0   -> base_price_cents=1999, total_price_cents=1999 ($19.99), margin 1.0
- premium,  cogs=300 -> price stays 1999 ($19.99), cogs 300, margin 0.85  (price independent of COGS, as intended)

## py_compile: PASS

## COGS / budget-gate / Stripe authorize: NOT TOUCHED
- Did not edit orchestrator.py or producer.py.
- producer.py:336 already calls pricing.price_for_plan(...) — it now receives the fixed tier price as suggested_price_cents (intended).
- orchestrator.py budget gate / money.authorize() / earn / per-scene COGS allocation: untouched.
- base_price_for_cogs (the old cost-plus math) preserved for any stale caller.

## TEST: 11 failures in tests/test_pricing.py — ALL are old-model baseline assertions
The existing suite hard-codes the OLD cost-plus expectations and MUST be re-baselined.
I could NOT edit it (scope = pricing.py + pricing.json only). Every failure is the test
asserting old cost-plus numbers/model, e.g.:
  - assertEqual(out["model"], "cost_plus")        -> now "fixed_tier"
  - standard total_price_cents == 500 (floor)     -> now 999
  - premium  total_price_cents == 600             -> now 1999
  - producer on-path suggested_price_cents 500/600 -> now 999/1999
PASSING still: all base_price_for_cogs(...) assertions (that function is unchanged),
selection-mapping / back-compat / consent-shim tests.

### Orchestrator: dispatch a follow-up to re-baseline tests/test_pricing.py
Update the cost-plus price assertions to the fixed tier prices (999/1999) and the
model string to "fixed_tier". Keep base_price_for_cogs tests as-is. Failing tests are
the ones touching price_for_plan / producer suggested_price_cents / model string.
