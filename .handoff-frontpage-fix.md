# Front-page fix — handoff checkpoint

Task: implement Dennis's front-page feedback (A) launch button → clear PLAY button;
(B) de-AI the borders (kill boxes-in-boxes + red-alert focus on load).
Edit ONLY dashboard/{styles.css,index.html,app.js}. NO commit. NEVER disturb :3030.

## Files / key lines (verified by Read)
- index.html:32  — #i-play symbol exists (filled triangle).  #i-loader at :30.
- index.html:207 — #build-go.cmp-send, currently <use href="#i-arrow"/>, title/aria "Build this video".
- index.html:195-200 — .q-opt quality cards.
- styles.css:427-436 — .hiro-composer .cmp-send (circle button + .ic rotate(-90deg)).
- styles.css:444-453 — .q-opt (unselected border line-2) / .q-opt.is-on (selected coral).
- styles.css:584-606 — .composer (border line-2 + shadow), :focus-within (coral),
  .cmp-in (border line), .cmp-in:focus (coral border + 3px ring), .cmp-bar (border-top line — KEEP).
- app.js:1058 btn.textContent="starting…"  | app.js:1071 btn.textContent="Build" (ERROR path, also wipes!) | app.js:1120 btn.textContent="Build"
  → all three wipe innerHTML/SVG. Replace with .is-loading class toggle.
- app.js:492 focusBuildUrl() — focuses #build-url; called by newBuild() (465) + hero-start click (2288). init() does NOT autofocus on load.
- Palette: --bg-1 #FFF, --bg-2 #F7F8FA, --line #E4E7EC, --line-2 #D5DAE2, --amber #D6351C, --r-ctl 11px, --r-card 15px, --focus-ring 0 0 0 3px rgba(214,53,28,.22).

## Status — ALL DONE + VERIFIED (preview :3099, node --check OK, no console errors)
- [x] A: index.html:207 #i-arrow→#i-play + #i-loader; title/aria "Start build".
      styles.css:431-44 removed rotate(-90deg), translateX(1px) optical-center,
      hover translateX(2.5px) scale(1.04), .is-loading swaps play↔loader (anim rot).
      Added `flex:none` to button+icons (two-SVG flex children were collapsing button to 4px).
      app.js:1061/1074/1123 textContent→classList add/remove('is-loading'). SVG never wiped.
- [x] B1: composer :focus-within no coral (shadow only); inputs calm on :focus,
      coral ONLY on :focus-visible. init() confirmed NO autofocus on load (activeEl=BODY).
- [x] B2: inputs borderless filled wells. NOTE: live cascade governed by .build-in
      (styles.css:1054-1063), NOT .cmp-in (597) — .build-in is later+wins. Fixed BOTH.
- [x] B3: .composer border --line-2 → --line (faint), kept soft shadow (one frame device).
- [x] B4: .q-opt unselected border → transparent (fill-distinguished); coral only on .is-on.
- [x] B5: structural hairlines on --line; --line-2 only on .dsc:hover (interactive). KEEP .cmp-bar (B6).
- [x] VERIFY: /tmp/frontpage-fix/{before,after}.png (2x). Play persists across is-loading toggle
      (uses=[#i-play,#i-loader] in all 3 states). Build flow wiring intact (click→startBuild).
