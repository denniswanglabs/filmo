# R11 — CTA stage-direction guard (style_fill.py ONLY)

## Problem
Notion rebuild leaked a STAGE-DIRECTION into the CTA title: rendered
"Closing call-to-action card urging users" — a meta-description of the CTA card,
NOT real CTA copy. plan_job's `_degut_stage_direction_beats` (R7-G3) caught
"Show call-to-action:" but NOT this phrasing. style_fill is the LAST line of defense.

## Scope
Edit ONLY `style_fill.py`. Tests in `tests/test_style_fill.py`. Repo IS git — do NOT commit.
$0. NEVER touch :3030.

## Plan
1. Add `_is_cta_stage_direction(text)` — case-insensitive, unicode-hyphen-robust,
   rejects self-referential meta-descriptions of the CTA card.
2. Add `_derive_cta(brand, raw_threaded)` — real brand-anchored imperative CTA.
3. Wire both into the `_is_closing_title` branch of `_shape_hero` (title + subtitle).
4. Keep R9 imperative + R10 keep-the-.com wins.
5. Add tests (TestR11CtaStageDirection). Verify isolated + `./run_all_tests.sh --no-eval`.

## Status — DONE
- [x] read brief, LEARNINGS, style_fill.py, test_style_fill.py
- [x] implement guard (_is_cta_stage_direction, hyphen-robust) + _derive_cta + _brand_domain
- [x] wire into closing branch (explicit title + punchy_cta + threaded + subtitle; derive imperative, not bare wordmark)
- [x] tests (TestR11CtaStageDirection, 11 new) + isolated verify table all green
- [x] run_all_tests.sh --no-eval GREEN (exit 0, "$0 spent"); test_style_fill 48/48

## Result
- 4 stage-direction CTAs rejected → "Get started with Notion" (brand-anchored imperative).
- 5 real CTAs pass unchanged incl. "Visit theverge.com" (R10 .com kept).
- Follow-up for orchestrator: extend plan_job `_degut_stage_direction_beats` to this
  meta-description phrasing too (out of this file's scope).
