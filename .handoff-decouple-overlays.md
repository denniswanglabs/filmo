# Handoff — Decouple studio overlays from mock mode (KILL THE ORANGE)

## Status: DONE — VERIFIED. All 5 scenes WHITE, zero orange. Full test suite OK ($0).
## verify run: runs/verify-decouple (mode=real, simulate-paid) -> status=delivered, 33.02s, overlays=studio, every scene studio.kind=authored/real=True. Frames /tmp/verify-decouple/ all white.

## What changed (2 files only)

### build_runner.py (~line 406)
BEFORE: `overlays=("studio" if mode == "mock" else "mock"),`
AFTER:  `overlays="studio",`  (always studio, with explanatory comment)

### orchestrator.py (line 187 -> now 192)
BEFORE: `studio_active = (overlays == "studio" and mode == "mock")`
AFTER:  `studio_active = (overlays == "studio")`

### orchestrator.py — real-asset guards (minimal adaptation, per brief)
The brief warned: do NOT break real-asset generation. With studio_active now
True in real mode, the REAL-MEDIA branches (walkthrough, paid cinematic) would
have routed to `studio_placeholder` instead of the genuine generators. Guarded
those 3 branches with `and mode == "mock"` so real mode still runs real assets:
- `_walkthrough_gen` closure (~368): `if studio_active and mode == "mock"` -> placeholder else real walk-agent
- paid cinematic approve (~517): `if studio_active and mode == "mock"` -> placeholder else generate_cinematic
- paid cinematic downgrade (~556): same guard
The CARD branches (title/motion_graphic at ~414 and ~462) keep bare `studio_active`
so white brand cards render in ALL modes. THIS is what kills the orange.

## Why this works
- adapters.generate_overlay() -> synth_clip(TYPE_COLOR["motion_graphic"]="B45309") = ORANGE.
  That path is only reached when studio_active is False. Now studio_active is True
  for title/motion_graphic in every mode -> adapters.studio_overlay() -> white brand cards.
- Standard orinovate plans contain ONLY title + motion_graphic scenes (verified from
  build-orinovate-0b0aa5 plan.json), so all 5 scenes go white; zero orange.

## Verify
`PRODUCER_SIMULATE_PAID=1 python3 build_runner.py --url https://www.orinovate.com --goal "A 30-second brand explainer" --run-id verify-decouple --mode real --quality standard --brain super-free --duration 30`
Then ffprobe + read scene-center frames: ALL WHITE, ZERO ORANGE.
Then `./run_all_tests.sh --no-eval`.
