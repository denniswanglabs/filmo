# Handoff: f1-grounding (Track A — Dim-3 copy grounding)

_Started 2026-06-22. Subagent owns ONLY: plan_job.py, validate_planner.py, planner-prompt.md, style_fill.py._

## Goal
Make VO/copy grounded, concrete, imperative like the reference films. Kill 4 bugs:
1. Hollow template ("This is [Brand] — straight from the real site…") — BAN.
2. Thin-data brands: use world knowledge of recognizable brand (TripAdvisor→reviews/hotels/restaurants).
3. Grammar bug: "See how to use the reading hotel reviews, step by step." (the "use the [gerund]" stitch).
4. Meta-dump headlines: raw truncated meta-description as headline ("Get more than ecomme").

## ROOT-CAUSE FINDINGS (Phase 0 — exploration)
- **R1 baseline beats (BEFORE)** captured from runs/r1-stripe + runs/r1-tripadvisor:
  - stripe: title-open "Stripe." / screenshot-home "This is Stripe — straight from the real site." / screenshot-inner "Here is the product, exactly as you would see it." / walkthrough "See how to use the pricing page, step by step." / title-close "Get started with Stripe today."
  - tripadvisor: same template + GRAMMAR BUG "See how to use the reading hotel reviews, step by step."
- **Both R1 runs fell to the DETERMINISTIC TEMPLATE** (`plan_job._standard_template_plan`, lines ~491-499) — NOT the LLM. Reason: `brain_key()` was False in that shell (OpenRouter key only in ~/.zshrc + ~/.hermes/.env). After `source ~/.zshrc` the key IS present (len 73) → LLM path WILL fire when I source zshrc before building.
  - => I must fix BOTH the LLM prompts (SYSTEM_PROMPT / _QUALITY_PROMPT / company_facts_block / planner-prompt.md) AND the deterministic template beats (the safety net that actually rendered R1).
- **Grammar bug location**: plan_job.py `_standard_template_plan` line ~491: `("See how to use the %s, step by step." % feature_label)`. emphasis "reading hotel reviews" → "use the reading hotel reviews". FIX: drop "use the" stitch, make imperative.
- **Hollow template location**: plan_job.py `_standard_template_plan` beats list lines ~493-499.
- **Meta-dump headline**: style_fill.py `_brand_fallback_title` uses `brand.get("tagline")` which brand_extract.py sets = meta description hard-cut at `[:80]` (mid-word: "Get more than ecomme"). `_title_from_text` also truncates at 72. FIX in style_fill shaper: reject truncated/meta-dump tagline as headline; derive clean complete headline from grounded facts/VO.
- **World-knowledge guard**: validate_planner.py `company_facts_block` returns "" when facts thin (tripadvisor: empty tagline+features) → no grounding at all. MUST add world-knowledge instruction while KEEPING "never invent a different business" guard.

## EDIT PLAN
- [ ] validate_planner.SYSTEM_PROMPT VOICEOVER RULES: ban hollow phrases, demand concrete feature names/numbers/imperative couplets, never describe the animation/site.
- [ ] validate_planner.company_facts_block: add world-knowledge clause for recognizable brands when facts thin; keep guard.
- [ ] plan_job._QUALITY_PROMPT["standard"]: add grounded-copy demands.
- [ ] plan_job._standard_template_plan beats: rewrite (no hollow phrases, imperative, grounded from features); fix grammar bug.
- [ ] planner-prompt.md: mirror the SYSTEM PROMPT + COMPANY FACTS edits.
- [ ] style_fill: title/headline shaper rejects truncated meta-dump; clean complete headline.

## VERIFY
source ~/.zshrc; WS_WALKTHROUGH_CACHE=/tmp/walk-smoke-short.mp4 PRODUCER_SIMULATE_PAID=1 ... --mode mock --quality standard --brain super-free --run-id f1-{brand}
brands: stripe.com, tripadvisor.com.tw. Then ./run_all_tests.sh --no-eval.

## STATUS: COMPLETE. All edits made, suite green, both brands verified.

## EDITS MADE (all 6 levers)
1. validate_planner.SYSTEM_PROMPT VOICEOVER RULES — banned hollow phrases, demand concrete real feature names + real-looking numbers + imperative couplets, world-knowledge clause for thin facts, never-describe-the-animation. ✓
2. validate_planner.company_facts_block(facts, company_url) — now world-knowledge for thin/empty scrape (names brand from URL, instructs own-knowledge value props), drops truncated meta tagline, keeps "never invent different business" guard. Added _brand_from_url helper. Always non-empty when URL parseable. ✓
3. plan_job._plan_with_nemotron — calls company_facts_block(company_facts or {}, company_url=url) unconditionally. ✓
4. plan_job._QUALITY_PROMPT["standard"] — appended grounded/imperative COPY demands + banned phrases. ✓
5. plan_job deterministic template — added _BRAND_WORLD_KNOWLEDGE map (stripe/tripadvisor/shopify/linear/plaid), _world_knowledge_for, _grounded_template_beats; rewrote _standard_template_plan beats (no hollow phrases); FIXED grammar bug ("See how to use the X" -> "Watch X, step by step." imperative). Threaded company_facts through _template_plan/_standard_template_plan + 3 call sites. ✓
6. style_fill — _is_truncated_meta + _clean_complete_headline; _brand_fallback_title rejects truncated meta tagline; _shape_hero (both branches) + _shape_explainer subtitle use clean headline. Salvages complete leading sentence; never mid-word. ✓
7. planner-prompt.md — mirrored SYSTEM PROMPT VO rules + COMPANY FACTS world-knowledge doc. ✓

## VERIFY RESULTS
- ./run_all_tests.sh --no-eval => ALL TESTS PASSED — $0 spent (no rebaselining needed).
- f1-stripe (LLM path fired, key sourced from ~/.zshrc): grounded — "135 currencies", "Stripe Billing", "subscriptions, invoices, usage-based pricing", "fraud prevention"; CTA "Start building your business at stripe.com."
- f1-tripadvisor (bot-blocked, empty theme): world-knowledge fired — "millions of traveler reviews", "8 million+ hotels", "5 million+ restaurant reviews", "compare prices", "book your trip"; CTA "Plan your trip on Tripadvisor."
- Programmatic asserts: NO banned phrases, NO use-the-gerund grammar bug, NO truncated/mid-word title frames — both brands.
