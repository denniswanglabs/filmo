# Handoff: direct manipulation in the editor preview (Tasks 1-3)

Goal: keyboard shortcuts + inline text editing + drag-resize/move in the live
@remotion/player preview. Match existing patterns (SelectionOverlay rect-tracking,
App.update(mutator), scene.data.geo.<key>). NO default-render change.

## Baselines (before any edit) — BOTH GREEN
- editor build: `cd studio/editor && npm run build` -> vite v5.4.21, 465.57 kB, built ok
- studio tsc: `cd studio && npx tsc --noEmit` -> exit 0

## Files in scope (each backed up to <file>.pre-directmanip.bak before edit)
- studio/editor/src/App.jsx (wire hook + inline-edit + drag state)
- studio/editor/src/SelectionOverlay.jsx (functional handles + body drag; rect helpers reused)
- studio/editor/src/EditPanel.jsx (export FIELD_MAP / text-key resolution + geo key sets for reuse)
- studio/editor/src/useShortcuts.js (NEW — keyboard hook)
- studio/editor/src/InlineEditor.jsx (NEW — overlay textbox) [or folded into SelectionOverlay]
- studio archetypes: ONLY if adding new clean geo keys (font-size for text resize)

## Geo read map (confirmed from archetypes)
- hero-title: titleFontSize(132), subtitleFontSize(34), kickerFontSize(26), plateW(1200), plateH(720)
- apple-hero: titleFontSize(140), subtitleFontSize(32)  [no plate W/H — padding-sized]
- apple-screenshot: cardW(1380), cardRadius(18), shotH(712)
- apple-statement: statementFontSize(116)
- card-ui / explainer-card / apple-registry / walkthrough-player primary boxes:
  absolute/grid layouts -> geometry intentionally skipped (text-only) per editability handoff.

## Status: Tasks 1 + 2 wired + building (470.57 kB). Implementing Task 3 (drag).

### Task 1 DONE — useShortcuts.js
Space play/pause; ArrowL/R ±1f; Shift+Arrow ±fps (1s); Home/End first/last; Esc
deselect+exit-edit; Enter (selected text) -> inline edit. Skips input/textarea/
select/contenteditable (except Esc). preventDefault on owned keys.

### Task 2 DONE (pending build of Task 3) — InlineEditor.jsx
Double-click (dblclick in usePreviewSelection -> onActivate) OR Enter-while-selected
-> App.beginInlineEdit -> editing={sceneId,field,key}. Player paused. Overlay
textarea hugs useTrackedRect rect, matches element font via getComputedStyle,
autofocus+select-all. Live onChange -> props.scenes[idx].data[key] via update().
Enter/blur commit; Esc cancel restores original. Hover cursor = I-beam on inline
text fields (INLINE_TEXT_FIELDS), pointer otherwise.
Inline-editable text fields = FIELD_MAP kind:"text" keys (TEXT_KEY_FOR_FIELD,
exported from EditPanel): title, subtitle, kicker, heading, headingAccent,
punchWord, statement, caption, overlayTitle, footnote, product.

### Task 3 DONE — drag resize + move (SelectionOverlay.jsx DRAG_GEO)
Functional corner handles + draggable box body for geo-backed elements; writes
scene.data.geo.<key> live via update(mutator). CSS-px delta -> comp-px via
1920/stage.clientWidth.
- DRAG (resize + move): hero-title `plate` (plateW/plateH + plateOffsetX/Y),
  apple-screenshot `card` (cardW/shotH + cardOffsetX/Y).
- DRAG (font resize, corners only, NO move — no offset literal exists):
  hero-title title/subtitle (titleFontSize/subtitleFontSize),
  apple-hero title/subtitle (same), apple-statement statement (statementFontSize).
- TEXT-ONLY (no drag): everything else — card-ui heading/cards, explainer-card
  kicker/title/subtitle/bullets, apple-registry heading/entities,
  walkthrough-player overlayTitle/card/caption, apple-hero kicker/product,
  apple-screenshot caption, apple-statement footnote. Reason: absolute/grid
  layouts (fixed insets / grid templates) — no single W/H or offset literal maps
  cleanly without restructuring layout (would risk changing default render).

### New backward-compatible geo keys ADDED to archetypes (default = identical):
- HeroTitle: plateOffsetX/plateOffsetY (??0) folded into the plate translate.
- AppleScreenshot: cardOffsetX/cardOffsetY (??0) folded into the card transform.
EditPanel GEO sliders + types.ts geo doc updated to match. apple-hero/statement
font-size keys already existed.

### BACKWARD-COMPAT (confirmed): with `geo` absent AND not editing, every
archetype falls back to its original literal (offsets ??0, sizes ?? literal) ->
render is byte-identical to before. data-* attrs stay inert.

### FINAL GATES — BOTH GREEN
- editor build: dist/assets/index-*.js 473.99 kB, ✓ built (bundle rebuilt last)
- studio tsc: exit 0

### Files edited (backups *.pre-directmanip.bak each):
studio/editor/src/{App.jsx, SelectionOverlay.jsx, EditPanel.jsx}
studio/editor/src/{useShortcuts.js, InlineEditor.jsx} (NEW, no .bak — new files)
studio/src/timeline/archetypes/{HeroTitle.tsx, AppleScreenshot.tsx} (+offsets)
studio/src/timeline/types.ts (geo doc comment only)
(AppleHero.tsx, AppleStatement.tsx backed up but NOT modified — font geo pre-existed.)

## Status: COMPLETE. All three tasks done, both gates green, bundle rebuilt.
