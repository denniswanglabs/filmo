# Handoff — real-mode parallel demo (Stripe docs)

## Goal
ONE real-mode build of sample-plan.json with HERMES_PARALLEL=1 to:
(a) confirm threaded walk-agent behaves end-to-end in real mode
(b) produce real demo footage
(c) measure wall-clock for rigorous serial-vs-parallel before/after

## Budget (HARD cap = 90cr)
- START balance: **1985.3 credits** (ultra plan, denniswanglabs@gmail.com)
- Plan: 3 paid Seedance cinematic scenes (~27cr each ≈ 81cr) + free title/walkthrough
- Gate PASSED: START high, plan est ~81cr < 90cr cap.
- NO second run, NO paid retry.

## Phase status
- [x] Read brief + handoffs (parallel/isolation in place; BEAM fix in place per handoff)
- [x] Budget gate: START=1985.3, plan ~81cr OK
- [x] Step 1: NemoClaw pre-flight DONE — walk-ultra sandbox HEALTHY.
      Inference healthy (nvidia-prod reachable, no 403/stale). Phase=Ready.
      exec probe returned PROBE_OK cleanly. Policies incl playwright-cdn/demo-targets/nim.
      Doctor "fail" = cosmetic container-name mismatch (openshell-cluster-nemoclaw vs
      actual openshell-walk-ultra-90fe...); "Connected:no" = dashboard not attached only.
      NO credentials reset needed (not stale). BEAM fix per handoff lives in walk-ultra/
      explainer-agent/agent.sandbox-v26.js (host-side, node --check passed earlier).
      Docker container "unhealthy" 44h uptime — cosmetic, control path (exec) works.
- [x] Step 2: real parallel build RAN. Gen+VO completed; orchestrator CRASHED at
      final stitch (ffmpeg concat resolution mismatch — see below). All footage on disk.
- [x] Step 3: timing measured (mtimes — ledger has NO per-event timestamps this version)
- [x] Step 4: ffprobe + frame-read DONE — walkthrough REAL + UN-CLIPPED
- [x] END balance 1940.3 -> actual spend 45.0cr (<=90cr OK)

## RESULTS
### Budget
- START 1985.3 -> END 1940.3 = ACTUAL SPEND 45.0cr (2 Seedance gens; s2_hero failed
  before charging). Under 90cr cap. NO retry done.

### Timing (epochs from artifact mtimes; ledger lacked timestamps)
- T0 (parallel kickoff/plan.json): 1781926979.44
- T_parallel (T0 -> VO done, full orchestrated work):  523.1s
- T_walk (threaded walkthrough gen, ~T0 -> walk_done):  520.6s
- T_cine_path (T0 -> last cinematic s4 done):           506.7s
- DERIVED T_serial = T_parallel + T_walk = 1043.8s
- Speedup 2.00x, saved 520.6s
- Overlap: walk finished at +520.6s, BEFORE run end (+523.1s) -> 100% of T_walk HIDDEN.

### Walk-agent (KEY real-mode confirmation): PASS
- Threaded walk-agent ran end-to-end INSIDE orchestrator, produced REAL
  04_s5_walkthrough.mp4: 2560x1440, 26.75s, 1605 frames.
- Frames: docs.stripe.com — full "stripe DOCS" wordmark (left) + full account-nav
  (right) visible -> BEAM 1440x900 fix HELD, NOT cover-cropped. Reached PaymentIntent
  create curl panel (matches brief).

### Isolation: WORKED
- s2_hero Seedance returned no asset_url (Higgsfield-side gen fail), was ISOLATED
  (ledger: "SCENE FAILED ... isolated; run continues"), run continued, VO dropped its
  beat, money accounting stayed serial+correct (75c locked, 22+22+9c, remaining 0c).

### BUG found (NOT money/isolation): stitch crash
- After isolation dropped s2_hero, survivors had MIXED resolutions: title 1920x1080,
  cinematics 1280x720, walkthrough 2560x1440. orchestrator stitch feeds raw clips to
  ffmpeg concat with NO scale/setsar normalization -> "Input link parameters do not
  match" -> error 234 -> exception killed run BEFORE writing final.mp4 + before ledger
  reached completed_with_warnings (status stuck at "running").
- This is a latent stitch bug EXPOSED by heterogeneous survivor sizes (isolation removed
  the scene that would have... no—unrelated; concat always needed normalization).
- Did NOT edit source (read-only). Reproduced final.mp4 via local $0 ffmpeg with
  scale+pad to 1920x1080 + VO mux -> runs/demo-parallel-stripe/final.mp4
  (39.83s, 1920x1080 h264 + aac mono 48k, 9.9MB). ffprobe verified.

### Content notes (OUT OF SCOPE — not the threading test, cosmetic)
- Title card rendered the raw brief text as copy + "OPERON SOLUTIONS" kicker (wrong
  brand pulled in) — title codegen content bug, separate.
- s4_network Seedance rendered a glowing phone, not isometric payment nodes — gen drift.
- Neither affects the parallel-threading deliverable.

## VERDICT
Threaded walk-agent end-to-end in real mode: CONFIRMED (real, un-clipped, BEAM held).
Parallel overlap: 100% of T_walk hidden -> 2.00x speedup, 520.6s saved (derived).
Isolation: confirmed working. Budget: 45cr spent, under 90cr cap. NO retry.
Open bug for Dennis: orchestrator stitch needs scale/setsar normalization before
ffmpeg concat (mixed survivor resolutions crash it post-isolation).
DONE.

## Key facts
- Run cmd: HERMES_PARALLEL=1 python3 orchestrator.py --plan sample-plan.json --mode real --run-id demo-parallel-stripe --vo edge
- Parallel code present: orchestrator.py _BgGen (line 65), PARALLEL_ENV (58), bg_walk (310)
- clips sorted before stitch (line 603)
- Walk-agent runs INSIDE orchestrator (threaded) — cannot intervene mid-run, must pre-flight NemoClaw first
