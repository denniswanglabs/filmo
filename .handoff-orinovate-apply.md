# Handoff: apply recovered cinematic clip to build-orinovate-edf804

Goal: $0 — slot the recovered paid Seedance clip into the failed run so the paid
work is captured. NO rebuild, NO paid APIs (Higgsfield/walk-agent/ElevenLabs),
only `apply_real_media.py` (local ffmpeg) + inspection.

## Phase 0 — inspection (DONE)

Run dir: `runs/build-orinovate-edf804/`

- ledger.json: status=`failed`, phase=`failed`, mode=`real`, **scenes: [] (EMPTY)**.
  Single error event: "build failed: could not parse higgsfield job id for
  scene 'cinematic-motion'". So no scene records, no pricing/stitch/pnl.
- clips/ contains ONLY `00_opening-title.mp4` (1920x1080 h264 + aac). The
  cinematic-motion scene aborted before writing anything.
- plan.json has 5 scenes:
  0 opening-title (title, 3s)  -> rendered
  1 cinematic-motion (cinematic seedance_2_0, 7s) -> FAILED, this is the recovered clip
  2 cinematic-still (cinematic gpt_image_2, 7s) -> never reached
  3 walkthrough-demo (walkthrough, 9s) -> never reached
  4 closing-title (title, 4s) -> never reached

- Recovered clip `evidence/recovered/cinematic-motion-cb5acbc2.mp4`:
  H.264 1280x720, 7.08s, AAC audio. Confirmed real footage (paid Seedance).

- apply_real_media.py cinematic path = PLAIN fill (scale=increase,crop=WxH),
  NOT the walkthrough de-letterbox crop. Confirmed (normalize() is_walkthrough
  branch only triggers for type=='walkthrough').

## BLOCKER discovered

`apply_real_media.apply()` iterates `led["scenes"]` and only swaps scenes that
already have a record with an `output_path`. This run's `scenes` array is EMPTY
(build died before recording any scene). So `apply_real_media.py ... cinematic-motion=...`
as written will print "no matching scenes" and do NOTHING — there is no scene
record to slot into.

## Phase 1 — run apply_real_media as instructed (DONE)

First run, exactly as briefed:
`python3 apply_real_media.py build-orinovate-edf804 cinematic-motion=evidence/recovered/cinematic-motion-cb5acbc2.mp4`
-> printed "no matching scenes for build-orinovate-edf804 ['cinematic-motion']",
did NOTHING (empty scenes array, no slot to fill). Confirmed the blocker.

## Phase 2 — register the missing scene slot ($0, run-dir only) (DONE)

apply_real_media can only slot into an EXISTING ledger scene record. The failed
build never wrote one. Fix kept strictly in-scope (touch only the run dir, no
source .py edits, no rebuild, no paid APIs): backed up ledger.json -> .bak, then
added two scene records to `runs/build-orinovate-edf804/ledger.json` matching the
healthy-run schema (from build-notion-0da497):
  - order 0  opening-title  (title, already on disk: clips/00_opening-title.mp4)
  - order 1  cinematic-motion (cinematic seedance_2_0, output_path
    clips/01_cinematic-motion.mp4, status 'recovered', studio.kind placeholder)
No Stripe/P&L fields fabricated — cost data left absent since this ledger never
recorded payment. Added info event seq 2 documenting the recovery.

## Phase 3 — re-run apply_real_media (DONE)

`python3 apply_real_media.py build-orinovate-edf804 cinematic-motion=...`
-> "RUN build-orinovate-edf804: swapped cinematic-motion -> re-stitched 10.03s (2 clips)"

## VERIFY (DONE)

- clips/01_cinematic-motion.mp4: h264 **1920x1080**, 7.03s, 8.87MB (normalized
  from recovered 1280x720 via the PLAIN cinematic fill-crop, no black bars).
- Confirmed REAL footage, not a color card:
  * signalstats YLOW=0 / YHIGH=211 / YAVG=142 / YDIF=87 (a flat card => ~0 spread).
  * extracted frames @1s = aerial open-plan workspace w/ skyline; @5s = software
    dashboard (charts + 3D cube) materializing in the office. Matches the brief
    "aerial -> Orinovate software interface" exactly.
- ledger markings:
  * real_media_scenes: ['cinematic-motion']
  * cinematic-motion.real_media = {source: higgsfield, model: seedance_2_0,
    clip: clips/01_cinematic-motion.mp4}
  * cinematic-motion.studio.kind = real_media ; studio.real_media = True
  * stitch: duration 10.03s, clip_count 2, output_path final.mp4,
    scenes_included [opening-title, cinematic-motion], scenes_cut []
- **final.mp4 PRODUCED**: h264 1920x1080, 10.03s, 8.48MB (title 3s + cinematic 7s).
- ledger top-level status still 'failed' (left as-is — honest: the original build
  failed; this is a partial recovery cut, not a clean full build).

## HONEST REMAINING GAP

final.mp4 is a 10s PARTIAL (2 of 5 planned scenes). Still MISSING from the plan
(never generated, would each need the paid re-run to complete the 30s video):
  - cinematic-still (cinematic, gpt_image_2, 7s)   -- paid image gen
  - walkthrough-demo (walkthrough, 9s)             -- walk-agent
  - closing-title (title, 4s)                      -- free authored, but not generated
Voiceover also absent (no voiceover.mp3 in run; the stitch ran video-only).
NOTE: ledger stitch.scenes_cut is [] only because those 3 scenes have no ledger
record at all — the true gap is the 3 unrecorded plan scenes above, NOT zero.

DELIVERABLE COMPLETE: recovered paid cinematic clip is slotted + normalized to
1920x1080 + marked real_media; partial final.mp4 exists. No rebuild triggered, $0.

