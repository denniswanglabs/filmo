# Editor palette unify — handoff (2026-06-22)

Task: re-skin the iMovie-style timeline editor (`studio/editor/`) from the WRONG
round-1 aesthetic (lime accent + dark glass chrome + aurora backdrop + heavy
glassmorphism) to the CANONICAL Walk Studio palette (= the dashboard's): LIGHT
chrome, hairline borders, soft neutral shadows, a single CORAL `#D6351C` accent
used sparingly, dark video canvas only. KEEP the iMovie structure + all
functionality (load run, live Player, live edits, Save, Export). git repo — DO
NOT commit. Edit ONLY files under `studio/editor/`.

## Token migration (single source: `studio/editor/index.html` :root)
Kept every token NAME so JSX CSS-var references cascade in one place. Before -> after:
- `--accent` lime `#c6f24e` -> coral `#D6351C`; `--accent-grad` multi-hue lime
  gradient+glow -> solid `linear-gradient(180deg,#E14730,#D6351C)` (dashboard CTA).
- `--accent-glow*`/`--accent-tint*`/`--accent-line` lime rgba -> coral rgba (subtle).
- chrome surfaces (`--bg`,`--glass`,`--panel*`,`--chrome`,`--field*`,`--glass-edge`)
  dark/translucent -> LIGHT (`#F4F5F7`/`#FFFFFF`/`#F7F8FA`/`#EEF0F4`, hairline `#E4E7EC`).
- ink (`--text/-2`,`--muted`,`--dim`) light-on-dark -> dark-on-light (`#14171C` etc).
- radii pulled back: `--r-ctl` 12->11, `--r-card` 18->15, `--r-lg` 22->18.
- shadows: dark drop-shadows/glass-inset -> dashboard's neutral card elevation
  (`0 1px 2px rgba(20,23,28,.04), 0 8-14px ... rgba(20,23,28,.06-.08)`).
- Added `--bg-1 #FFFFFF`, `--pos #0E9F6E`, `--neg #C01A2B`, `--video-bg #0C0E12`.
- DARK is kept ONLY for: the video canvas (`--stage`/`--video-bg`), the timeline
  track trough (`--rail #0C0E12`) and the clips (`--clip*`) — a deliberate
  cohesive inversion (the brief allows a subtle dark track).

## Anti-AI cleanup
- `body`: REMOVED the 3 aurora radial-gradients -> clean `#F4F5F7` page + one
  VERY faint coral bloom top-right (mirrors dashboard `.grain`).
- `.ws-glass`: was `backdrop-filter: blur(22px) saturate(150%)` translucent glass
  -> a clean white card (`background var(--glass)` + 1px `--line` + soft shadow).
  Kept the class name so all JSX cascades; it is NO LONGER glassy.
- Dropped the lime glow on the primary/play buttons -> dashboard's coral CTA shadow.
- Inspector sticky header: dark glass+blur -> light `--panel` gradient + hairline.
- Active-scene card: glow shadow -> dashboard "selected well" (coral tint + 1px coral).
- behind-stage grid texture: white-on-dark hairlines -> dark hairlines @ low opacity.
- favicon: dark-green tile + lime W -> dark `#0C0E12` tile + coral W.
- Range/scrub thumbs: lime-white -> white + coral border; tracks -> light recess.
- Status dots, danger button: dark-mode tints -> `--pos`/`--neg` semantics.

## Files edited (ALL under studio/editor/)
- `index.html` — :root token block, body, .ws-glass, scrollbars, selection,
  range+scrub slider chrome, favicon.
- `src/App.jsx` — Wordmark tile, status dots, grid texture, canvas frame, render log.
- `src/ui.jsx` — Button kinds (glass/primary/danger).
- `src/PlayerControls.jsx` — play-button shadow.
- `src/TimelineTrack.jsx` — track trough border/radius, active-clip fallback fill.
- `src/EditPanel.jsx` — inspector sticky header, active-scene context card.

## Structure + functionality: UNTOUCHED
iMovie layout (floating top bar -> preview stage -> bottom timeline w/ proportional
clips+thumbnails+playhead+ruler -> right inspector) and every handler (loadRun,
update/live-edit, selectScene seek, save, render/export) are byte-identical in logic.

## Verify — DONE (preview_start "hermes-editor" :3040, default-loads r5-stripe)
Computed-style + behavior checks all PASS:
- body bg = rgb(244,245,247) #F4F5F7; text #14171C; Hanken Grotesk. Aurora GONE
  (body bg image = single faint coral bloom only).
- header.ws-glass = white #FFFFFF, neutral soft shadow, backdrop-filter: none
  (glass blur removed from chrome).
- tokens: --accent #D6351C, --bg #F4F5F7, --r-card 15px, --glass #FFFFFF.
- Export btn = coral linear-gradient(#E14730,#D6351C) + white text + dashboard CTA
  shadow. No lime anywhere; no chrome glass.
- Only 4 backdrop-blurs remain = the small "01-04" index chips ON the dark clips
  (intentional badge-over-thumbnail detail; active chip is coral). Chrome is clean.
- FUNCTIONALITY: click clip 03 -> inspector switched to "scene 03 of 04 walkthrough
  player" AND player seeked to frame 371 (in_frame). Editing Title field -> timeline
  clip label updated live + Save button flipped to "Save edits" (dirty). Player ref
  exposed at window.__player. No Vite/server errors. iMovie structure intact:
  4 timeline clips, white inspector panel (388px), preview Player, scrub controls.
RESULT: editor now reads as the SAME app as the dashboard — light + coral +
Hanken/Newsreader, clean surfaces, no aurora/lime/glass-overload, no emojis.
