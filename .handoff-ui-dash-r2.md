# Handoff — Dashboard UI Polish Round 2

## Scope
Round-2 elevation of Walk Studio dashboard toward Linear/Vercel/Stripe bar.
Edit ONLY dashboard/styles.css (primary), index.html, app.js (markup hooks only).
NO commit. NEVER disturb :3030. Preview via hermes-static :3099
serverId `1d9eb10e-d792-458d-bb55-59fdc0a4be3d`. Repo IS git.

## Round-2 punch list (from brief)
1. Produced storyboard card → pure BLACK when scene video src 404s → poster/fallback.
2. Script VO / Shot-List loading lines thin → skeleton shimmer rows.
3. SIDEBAR build list dense/untouched → elevation pass (recency rhythm, active row, Brain footer).
4. Delivered hero raw <video> chrome → custom play/scrub bar or refined wrapper.
5. PAY-GATE awaiting_payment full screen → fixture run to view it, then polish.
6. Numeric alignment: tabular-nums + alignment audit (.dh-meta, .dsc-hint, P&L receipt, phase track).

## How screens are reached (polling)
- Fixtures = `runs/<id>/ledger.json`. Driven via preview_eval `beginPoll('<id>')`.
- PRODUCING screen: ledger phase=producing + scenes[] with statuses.
- PAY-GATE: ledger phase=awaiting_payment + earn block (checkout_url, payment_status:unpaid).
- DELIVERED: ledger status=delivered (use fin-stripe → beginPoll('fin-stripe')).
- Black-card repro: produced scene with output_path pointing at a missing file.

## Key code anchors
- app.js liveStoryboard L1521 (produced card <video> no poster = black on 404).
- app.js liveScript L1278 (vo-empty L1287, shot-list empty L1296 = thin spinner).
- app.js deliveredHero L608 (raw <video controls>).
- app.js payGate L1119 ; renderLive awaiting_payment branch L1063.
- app.js sceneGlyph L1326 ; renderRail L342 ; railRow L376.
- styles.css: tokens :root L10 ; rail L196-319 ; sb-* L1071-1136 ;
  script/vo L1138-1171 ; paygate L1173-1259 ; delivered hero L831-873 ;
  phase track L987-1011 ; dh-meta L856 ; dsc-hint L611.

## STATUS: COMPLETE — all 6 items landed, verified, fixtures deleted

## Edits made (file:line anchors are post-edit)

### Item 1 — produced-card BLACK on 404 → graceful slate fallback
- app.js liveStoryboard (~L1527): produced branch now wraps the <video> in
  `.sb-media` + a `.sb-media-fallback` slate ("Scene ready" + ghost glyph). The
  video carries `onerror`/`onstalled` → adds `.is-fallback` to the wrapper, which
  hides the <video> and reveals the slate (instead of the raw #0C0E12 void).
- styles.css (~L1117): `.sb-media` (radial dark-gradient poster, not flat black),
  `.sb-media .sb-media-fallback{position:absolute;inset:0;aspect-ratio:auto}`
  (two-class selector beats the later `.sb-state` base rule so it overlays, not
  stacks — that bug caused double-height cards on first pass; FIXED).
- VERIFIED: 404 card shows "SCENE READY" slate, all 5 cards uniform 16/9 height.

### Item 2 — thin script loaders → skeleton shimmer
- app.js: added `skeletonLines(n,cap)` + `skeletonShots(n,cap)` (after toolForType).
  liveScript vo-empty → skeletonLines(4); shot-list empty → skeletonShots(3).
- styles.css (after .vo-empty): `.sk-bar/.sk-chip/.sk-tag` shimmer (@keyframes
  sk-shimmer), `.sk-lines`, `.sk-shots`, `.sk-cap` (coral pulse dot). Honors
  prefers-reduced-motion. VERIFIED: tapering prose bars + 3 shot-shaped rows.

### Item 3 — sidebar elevation
- styles.css `.run-wrap.active`: now `--sel-fill` well + coral left-edge indicator
  (`::before`, animates in) — distinct from grey hover (was identical bg-2).
- `.rail-group` + `.rail-brain-lbl`: trailing hairline rule (`::after` gradient) →
  sectioned recency rhythm. `.rc-meta` got tabular-nums for date digits.
- VERIFIED: active row unmistakable; groups read as time bands.

### Item 4 — raw <video controls> → custom player chrome
- app.js deliveredHero (~L626): replaced native-controls <video> with `.dh-player`
  = bare looping <video> + center glass play disc (`.dh-pp`) + `.dh-ctrls` bar
  (inline play `.dh-c-pp`, `.dh-scrub` coral track w/ fill+head, tabular `.dh-time`
  readout, `.dh-mute` toggle). New `initDhPlayer()` (after shareConfirmed) wires
  play/pause, timeupdate+seeked paint, pointer/keyboard seek, mute. Called at L569.
- styles.css (after .dh-video): full player chrome. Controls auto-show on
  hover/focus + while paused, fade during playback. VERIFIED: 0:03/0:31 readout,
  coral scrub fill, glass disc on pause — replaces generic native scrubber.

### Item 5 — pay-gate side panel polish
- app.js payGate (~L1244): added `.pg-side-h` "Order summary" eyebrow.
- styles.css `.pg-side-h` (tracked eyebrow + divider); `.pg-line .v` got
  ellipsis + tabular-nums; `.pg-note` got a top divider (bottom-anchored already).
- VERIFIED: side panel reads as a deliberate order-summary card.

### Item 6 — numeric alignment audit
- Most numeric els already had tabular-nums (good). Added it to: `.log-seq`
  (P&L/event seq), `.rc-meta` (sidebar dates), `.pg-line .v` (item 5), and the new
  `.dh-time`. Phase track chips are pure text (no nums) — n/a.

## VERIFICATION
- `node --check dashboard/app.js` → OK (after every edit + final).
- Dashboard loads cold: composer intact, hero "Got a video idea, Dennis?",
  119 rail rows, Previous-7-days/Recents/Brain groups, conn "119 jobs".
- Full flow (landing→delivered→producing→paygate→back) → 0 console errors.
- Live walkcast panel WORKS (frame + LIVE pill + URL bar + 5 feed rows).
- Fixtures runs/_r2prod + runs/_r2gate DELETED (confirmed no _r2* remain).
- :3030 NOT touched. No commit.

## ROUND-3 punch list (remaining gaps spotted)
1. Produced-card slate "Scene ready" vs working "Rendering" use the same warm
   slate; could differentiate the produced-OK clip's first-frame brightness
   (some real clips are very dark navy — opening-title reads near-black; a subtle
   top vignette/label-scrim on `.sb-clip` would keep the foot legible).
2. dh-player: no buffered-range indicator on the scrub track (a lighter "loaded"
   bar behind the coral fill would match YouTube/Vimeo polish). Also no fullscreen
   button — premium players usually have one.
3. Pay-gate side panel still has mid-panel whitespace between rows and the
   bottom note on tall viewports; could add a thin "what happens next" mini-step
   list to fill it meaningfully rather than leaving air.
4. Skeleton loaders: the VO card skeleton could include a faint serif-height hint
   (the resolved copy is serif 16px/1.62 — bars are 11px, slight layout shift on
   resolve). Match the bar height/leading to the resolved line-height.
5. Sidebar: 119 rows is a lot; the active-row indicator is great but a sticky
   group header (Linear-style) on scroll would help orientation in long lists.
6. Storyboard working-card spinner is `i-loader` rotating; a determinate ring
   (if backend exposes per-scene render %) would beat the indeterminate spin.
