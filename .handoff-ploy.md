# Handoff — Ploy reframe (curation = the moat) + Anthropic-style sidebar titles

Date 2026-06-21. Scope: dashboard/{index.html, app.js, styles.css}.
Server :3030 orchestrator-managed — NOT restarted. Cost-plus pricing, Build flow,
pay-gate, live build view, sidebar logo all PRESERVED. NO EMOJI. Native SVG.
STATUS: COMPLETE + verified in the live preview.

## Backups
dashboard/{index.html,app.js,styles.css}.pre-ploy.bak written before any edit.

## TASK 1 + 2 — DROP STYLE LABELS / REFRAME GALLERY AS A CURATED LOOKBOOK — DONE
- index.html (#stage-empty styles section):
  - Removed the style-picker framing: dropped the `.styles-eyebrow`
    "WHAT WALK STUDIO MAKES" line AND the heading "Three looks, one agent —
    re-skinned to your brand."
  - New understated lookbook framing:
      `<h2 class="styles-h">A few we're proud of</h2>`
      `<p class="styles-note">The bar we hold every video to.</p>`
  - Removed ALL five per-card style-family pills: deleted every
    `<span class="sc-fam fam-kinetic|fam-apple|fam-cinematic">…</span>`
    ("Kinetic-light" / "Apple-style" / "Cinematic"). Also removed the
    family-grouping HTML comments ("Kinetic-light (Orinovate family)",
    "Apple-style", "Cinematic (Zelios…)").
  - Each card now = poster + brand name (.sc-name) + "Watch" link (.sc-watch)
    only — NO style tag. Same 5 curated films kept VISIBLE (Orinovate, Hotcake,
    Kuli, Trayd, JGB Property), posters intact in dashboard/assets/styles/.
  - Simplified poster alt text (dropped "kinetic-light"/"Apple-style"/"cinematic"
    from the alt strings too).
- styles.css:
  - Deleted the `.sc-fam` + `.fam-kinetic/.fam-apple/.fam-cinematic` rules (left a
    one-line NOTE comment explaining the removal).
  - Added `.styles-note` (quiet ink-4 sub-line). Trimmed `.styles-h` margins/size
    so the shelf stays lighter + lower than the composer.
  - Rewrote the gallery section comment header from "grouped by family" to the
    lookbook framing.

## TASK 3 — SIDEBAR DESCRIPTIVE TITLES (Anthropic-style) — DONE (app.js + styles.css)
- app.js: split the old `railTitle` brand-only logic into helpers:
  - `railBrand(r)`  — registrable brand from company_url host (unchanged logic:
    docs.stripe.com -> Stripe, linear.app -> Linear), run-id slug fallback.
  - `railDescriptor(goal)` — mines a short video-type phrase from job.goal text,
    matching {walkthrough, explainer, launch film, social ad, promo, demo, brand
    video} IN ORDER OF APPEARANCE, dedup, join first 1-2 with " + ". Falls back to
    "promo" so every row reads as a video, never a bare brand.
  - `railTitle(r)` now returns `"<Brand> — <descriptor>"`, clip()'d to 42 chars.
    e.g. "Stripe — explainer + walkthrough", "Linear — promo + walkthrough".
  - `railMeta(r)` + `relTime(ts)` — faint second line: relative date when a
    created_at timestamp exists ("today/yesterday/3d/2w/…"), else the run mode
    (mock/real). (Current runs have created_at=null so meta shows mode.)
- railRow(): now renders `.rc-text > (.rc-title + .rc-meta)`; status dot,
  hover-revealed `.rc-del`, two-step confirm delete, and `selectRun` wiring
  PRESERVED unchanged. Recency grouping (renderRail buckets) untouched.
- styles.css: added `.rc-text` (flex column) + `.rc-meta` (faint ink-4 line,
  ellipsis); adjusted `.rc-title` for the column; `.rc-dot` align-self center.

## TASK 4 — DECLUTTER — DONE
- Trimmed the greeting sub-line (#stage-empty-msg / .hiro-sub) from
  "Paste a company URL and a goal — the agent does the rest." (redundant with the
  composer placeholders) to a quiet "The agent does the rest." Kept it as a `<p>`
  with id="stage-empty-msg" (app.js showEmpty() + selectRun() error path require
  a `<p>` in #stage-empty). Greeting, pills, composer, chips, footnote untouched.

## PRESERVED / VERIFIED INTACT
Hiro composer (greeting word-build, warm glow, pills, coral circular send,
example chips), Build flow (#build-url/#build-goal/#build-mock/#build-go,
state.pace, /api/build {selection:{options:{premium_vo}}}), pay-gate quote, live
build view, sidebar logo. "cinematic" still appears ONLY as (a) the PACING pill
option (Snappy/Standard/Cinematic — legit, wired), (b) a prose adjective in an
example-chip data-fill, (c) the ledger scene-type in app.js/styles.css — none are
user-facing style-family LABELS.

## VERIFICATION (server NOT restarted)
- `node --check dashboard/app.js` -> JS OK.
- CSS brace balance 604/604 BALANCED.
- `curl :3030/` -> HTTP 302 (-> /dashboard/index.html, expected). Served
  index.html shows "A few we're proud of" + .styles-note, no sc-fam/eyebrow/
  "Three looks". Served app.js carries railBrand/railDescriptor/railMeta/relTime/
  rc-text/rc-meta.
- Live preview (serverId bc31a226…, the orchestrator's hermes-dashboard):
  * Gallery: galleryH="A few we're proud of", note="The bar we hold every video
    to.", eyebrowPresent=false, famPillCount=0, 5 cards (Orinovate/Hotcake/Kuli/
    Trayd/JGB Property), 5 Watch links, NO banned label text in #stage-empty.
  * Sidebar (21 rows): descriptive titles + meta confirmed — "Stripe — explainer
    + walkthrough"/"2d", "Linear — promo + walkthrough"/"mock", "Orinovate —
    promo + walkthrough"/"real" (+status dot). Groups "Previous 7 days"/"Recents"
    preserved.
  * Greeting "Got a video idea, Dennis?" + sub "The agent does the rest.";
    composer + 4 chips + pace pill + send button all present.
  * Build-open flow: clicking a rail row opened its detail (detailVisible=true,
    emptyHidden=true, active row matches) — selectRun intact.
  * Screenshot captured (sidebar two-line titles + delivered hero render).

## Files edited (+ backups)
dashboard/index.html, dashboard/app.js, dashboard/styles.css
dashboard/{index.html,app.js,styles.css}.pre-ploy.bak

COMPLETE.
