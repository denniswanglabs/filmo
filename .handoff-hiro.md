# Handoff — Hiro-feel empty state + bigger/tighter logo (Walk Studio dashboard)

Date 2026-06-21. Scope: dashboard/{index.html,app.js,styles.css} + branding/4 SVGs.
Server :3030 orchestrator-managed — NOT restarted. Cost-plus pricing preserved
(composer = URL+goal only; quote at pay-gate). NO EMOJI. Native SVG. STATUS: COMPLETE.

## Step 0 — DONE: backups
*.pre-hiro.bak for index.html, app.js, styles.css + all 4 branding SVGs.

## TASK A — LOGO — DONE
- "Studio" text x=263 -> 248 (gap to "Walk" @ x=120 tightened ~15px) in:
  branding/logo-walkstudio-{h-light,h-dark,light,dark}.svg AND the inline .bar-logo
  SVG in dashboard/index.html. (h-dark/dark use coral #FF6A4F; others #D6351C.)
- CSS .bar-logo height 28px -> 36px (~29% larger, aspect preserved via width:auto).
- Verified served + computed: studioX=248, bar-logo height=36px, no stray x=263.

## TASK B — HIRO EMPTY STATE — DONE (index.html #stage-empty rewrite + CSS + app.js)
- WARM GLOW: new .hero-glow div, coral radial-gradient (rgba 242,107,82,.16 +
  214,53,28,.10), blurred, centered upper, gentle breathe. .stage-empty now
  position:relative; inner is z-index:1 above the glow.
- GREETING: single bold "Got a video idea, Dennis?" (#hero-title, word-by-word
  build, "Dennis?" lands in coral via data-accent). Kept #stage-empty-msg as ONE
  tiny muted line ("Paste a company URL and a goal — the agent does the rest.").
- REMOVED TEXT: the AUTONOMOUS VIDEO PRODUCER eyebrow, the long hero-sub paragraph,
  and the .cmp-pricenote line under the composer. Styles-gallery heading shortened
  ("Three looks, one agent — re-skinned to your brand.").
- PILLS above composer (.hiro-pills): outlined "Widescreen 16:9" aspect pill
  (#aspect-pill, i-film icon) + the pacing pill (#build-pace kept w/ .pace-opt
  buttons — Snappy/Standard/Cinematic, Standard default).
- COMPOSER (.hiro-composer): polished white card, radius 18px, soft shadow. Goal =
  main prompt line (.cmp-goal, larger), URL = small field below with leading link
  glyph (.cmp-url-row/.cmp-url-ic). Bottom row: subtle mock toggle left ("mock ·
  $0"), CORAL CIRCULAR SEND button right (#build-go .cmp-send, 38px, border-radius
  50%, i-arrow rotated -90deg = up). font-size:0 on send so app.js's textContent
  "starting…"/"Build" collapses while the icon stays. Send rules scoped under
  .hiro-composer to beat the later .build-go base rules (cascade fix).
- EXAMPLES: .ex-divider ("Examples") + 4 .ex-chip starters (Brand explainer,
  Product walkthrough, Social ad, Launch film) with icons; click fills #build-goal
  + focuses it + nudge pulse (wired in app.js init via #ex-chips).
- Styles gallery kept (.styles-lite): lower (margin-top 72px), hairline rule above,
  slightly recessed, smaller heading.

## PRESERVE — VERIFIED INTACT
Build flow (#build-url/#build-goal/#build-go/#build-pace/#build-mock; state.pace;
/api/build {selection:{options:{premium_vo}}}), pay-gate, live build view, sidebar,
all app.js ids. selectRun error path still has a <p> (#stage-empty-msg) in
#stage-empty. showEmpty() still finds #stage-empty-msg.

## VERIFICATION (server NOT restarted)
- node --check dashboard/app.js -> OK. CSS braces 605/605 balanced. curl :3030 serves.
- Preview (serverId bc31a226...), fresh load of empty state:
  * Logo bigger + tighter (screenshot), studioX=248, height 36px.
  * Warm glow renders behind hero; greeting bold w/ coral "Dennis?"; tiny sub only.
  * Pills: "Widescreen 16:9" + pacing (Standard on) above composer.
  * Composer card polished; coral circular send (radius 50%, arrow up = matrix(0,-1,1,0,0,0)).
  * 4 example chips render; clicking "Product walkthrough" filled #build-goal.
  * Pacing click -> cinematic (state.pace wiring intact).
  * Build click w/ empty URL -> startBuild guard focused #build-url, stayed on
    empty state, no network, send icon intact (flow preserved, no spend).
  * Server error log = only benign BrokenPipe/ConnReset in serve.py copyfile
    (browser canceling assets on reload); NO app/JS errors from edits.

## BACKUPS
dashboard/{index.html,app.js,styles.css}.pre-hiro.bak +
branding/logo-walkstudio-{h-light,h-dark,light,dark}.svg.pre-hiro.bak
