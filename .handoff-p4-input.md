# P4 — 3rd composer input (EMPHASIS) → walkthrough target. DONE (subagent)

_Phase P4 of LOOP-STATE.md: wire the dashboard to collect + send `--emphasis` (backend already accepts it, added P3)._

## What changed (3 files, dashboard only — pipeline untouched)

### 1. `dashboard/index.html`
Added a 3rd composer input below the Company URL field, inside `.cmp-fields`:
```html
<div class="cmp-url-row cmp-emph-row">
  <input id="build-emphasis" class="build-in cmp-in cmp-url cmp-emph"
         placeholder="What should the walkthrough show off? e.g. the pricing page"
         autocomplete="off" spellcheck="false" />
</div>
```
Reuses `.cmp-url-row` + `.cmp-url` classes so it matches the URL field's small
secondary-field aesthetic EXACTLY (no styles.css edit needed / allowed). `#build-emphasis`
is the JS hook. No emojis.

### 2. `dashboard/app.js`
- In `startBuild()` (~line 957): read the input value
  ```js
  const emphEl = $("build-emphasis");
  const emphasis = emphEl ? (emphEl.value || "").trim() : "";
  ```
- POST body (~line 977): added `emphasis` to the `/api/build` JSON body alongside
  url/goal/mode/pace/selection/quality/brain.
- `init()` (~line 2017): added an Enter-to-build keydown handler on `#build-emphasis`,
  mirroring the existing `#build-url` one.

### 3. `dashboard/serve.py` — `_api_build`
- Read it (~line 237): `emphasis = (body.get("emphasis") or "").strip()`
- Append the flag ONLY when non-empty (~line 278), shell-quoted:
  ```py
  if emphasis:
      inner += " --emphasis %s" % shlex.quote(emphasis)
  ```
- Echoed `emphasis` in the 200 JSON response for parity with quality/brain.

## Full flow of `emphasis` (input → CLI flag)
UI `#build-emphasis` input
→ app.js `startBuild()` reads `.value.trim()` into `emphasis`
→ included in `/api/build` POST body `{ ..., emphasis, ... }`
→ serve.py `_api_build` reads `body.get("emphasis")`, strips it
→ if non-empty, appends `--emphasis <shlex.quoted>` to the `build_runner.py` invocation
→ build_runner argparse `--emphasis` (default="", build_runner.py:581) → threads to plan_job/walkthrough brief (P3).

## Empty-emphasis handling (graceful)
build_runner's argparse defaults `emphasis=""` (build_runner.py:581). serve.py OMITS the
flag entirely when the user leaves the field blank, so the backend uses its `""` default —
no empty-string flag passed, no breakage. Matches P3 behavior.

## Verification done (foreground, no server restart, no build)
- `python3 -c "import ast; ast.parse(open('serve.py').read())"` → OK
- `node --check app.js` → OK
- Re-read all `emphasis` regions from disk across all 3 files — consistent.
- Confirmed `build_runner.py` has `--emphasis default=""` (line 581) and threads it (lines 302/361/601).

## ORCHESTRATOR ACTION REQUIRED
Restart :3030 (`kill <pid>` then `SKIP_WALK=1 zsh ~/Desktop/start-walk-studio.command`)
and hard-refresh the browser to pick up the new serve.py / index.html / app.js. The
running serve.py process still has the OLD `_api_build` in memory until restarted.
