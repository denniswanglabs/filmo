# Handoff — Art-direction pass on remotion_codegen.py (L1+L2+L3+L5)

## Task
Apply Hera learnings L1 (typography), L2 (continuous motion through hold),
L3 (animated brand-colored background), L5 (varied motion vocab) to the three
agent-authored archetypes (centered / editorial / divider) in
`remotion_codegen.py` ONLY. Keep per-brand palette system intact. Deterministic,
no emoji, render path must keep working.

## Phase 0 — recon (DONE)
- Single highest-ROI learning per HERA-LEARNINGS §5 = **L2+L3+L1(font) art-direction
  pass on remotion_codegen.py**. Task also adds L5.
- Render path: `adapters.py::_studio_render` writes TSX to
  `studio/src/generated/active.tsx`, runs `remotion render src/index.ts Scene <out>
  --codec=h264` from `studio/` (cwd), timeout 300s. Chrome headless renderer.
- studio/node_modules is a SYMLINK -> Demos/apple-style-demo/node_modules.
- FONT FINDING: NO `@remotion/google-fonts` and NO `@remotion/fonts` in node_modules.
  Brief forbids adding new deps. apple-style-demo (renders fine via same node_modules)
  uses a **system-font CSS stack** (`"SF Pro Display", -apple-system, ... `) — NO
  loadFont, NO @font-face, NO Google Fonts. => Reliable dependency-free approach =
  strong system-font stacks. Google-Fonts @import is the "blank font" risk the brief
  warns about; avoiding it.
- Backed up current active.tsx -> /tmp/hermes_active_backup.tsx (RESTORE at end).
- Baseline codegen runs OK.

## Plan
- L1: replace `Helvetica, Arial, sans-serif` with strong system stacks; add optional
  per-brand `font` to BRAND_PALETTES + a strong `_default` font.
- L2: one element in continuous frame-based motion across [0, durationInFrames].
- L3: animated brand-accent background block in _HEADER (radial/linear gradient drift).
- L5: vary entrance per archetype (staggered build / overshoot / blur-in).

## Phase 1 — implemented (DONE, remotion_codegen.py only)
- L1 font: added optional per-brand `font` STACK to every BRAND_PALETTES entry +
  strong `_default` (SF Pro Display/Helvetica Neue/Avenir Next stack) + `_DEFAULT_FONT`
  defensive const. Stripe=Söhne→HelvNeue, Linear=Inter Display→Inter, Notion=Lyon
  Display→ui-serif→Georgia (intentional SERIF contrast), Vercel=Geist→Inter. Templates
  now use `fontFamily: FONT` (the injected stack) — Helvetica-only fallback removed.
- L3 animated bg: `AnimatedBg` React comp in _HEADER. Two drifting radial blooms in
  ${ACCENT}/${ACCENT2} (8-digit alpha hex) on a slow Lissajous path + a 1.0->1.04
  breathe + ${PANEL} base glow. seed param differs per archetype (centered 0,
  placeholder 1.3, editorial 2.1, divider 4.2) so bg motion isn't same-y. Fades in
  over 24f. Added FONT + PANEL to both substitute() calls.
- L2 continuous motion: `drift(frame,phase)` = slow sine over full TOTAL frames.
  centered: text stack float-Y + rule glow pulse. editorial: travelling gradient +
  glow on the accent bar + badge drift. divider: badge breathe + ring-glow pulse +
  travelling sheen on the hairline. All frame-based, deterministic.
- L5 varied vocab: centered = BLUR-IN title (blur 14->0) + spring rise. editorial =
  per-line STAGGERED build, each line its own bouncy spring (damping 14). divider =
  pronounced OVERSHOOT pop on badge (damping 9). Three distinct entrances.

## Phase 2 — VERIFIED (DONE)
- tsc --noEmit, EXACT studio/tsconfig.json options, all three TSX => TSC_EXIT=0.
- Rendered 1 clip each via studio (remotion render Scene, --codec=h264, ~bounded):
  Linear=centered, Stripe=editorial, Notion=divider. All RENDER_OK.
- Read MID-HOLD frames f78 (2.6s) AND f96 (3.2s) per clip — both past the 1s entrance:
  (a) FONT renders: Stripe/Linear sharp grotesque; Notion renders SERIF (Georgia
      fallback for Lyon) — visibly distinct per brand, NOT a blank/fallback failure.
  (b) BG animated: brand-accent blooms clearly visible on all three, not flat.
  (c) MOTION mid-hold: f78≠f96 — centered text drifted up + bloom migrated; editorial
      blooms migrated lower-left→lower-center; divider ring-glow intensified + bloom
      shifted. Frame is NOT frozen.
  (d) COLORS: Linear indigo #5E6AD2, Stripe navy+indigo+cyan, Notion #191919+blue
      #2383E2 — all the brand's, zero Hera colors.
- Determinism: generate() x2 byte-identical; no Math.random / no Date.
- Existing suites pass: test_producer 13 OK, test_orchestrator 6 OK (latter runs the
  real codegen+render path).
- active.tsx RESTORED byte-identical to original; all temp renders/frames cleaned.

## Font honesty (flag for Dennis)
NO @remotion/google-fonts or @remotion/fonts in the shared node_modules and the brief
forbids adding deps, so I used system-font STACKS (the proven-loadable approach the
sibling apple-style-demo uses through this exact node_modules). The lead face in each
stack is the brand's REAL type (Söhne, Inter Display, Lyon, Geist) but those are
proprietary/unlicensed-here, so they only render IF installed on the host; otherwise
the next strong fallback renders (proven by Notion → Georgia serif). To render the
exact brand faces, Dennis would need to drop the licensed font files into studio and
add a `loadFont`/@font-face — flagged, not done (license + dep). The current result
already reads far better than the old bare Helvetica and is reliable headless.

## DONE — render path works for all three archetypes.
