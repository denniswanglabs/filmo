# Handoff — Dashboard UI Polish Round 1

## Scope
Elevate Walk Studio dashboard (dashboard/styles.css primary; index.html / app.js for markup) to Linear/Vercel/Stripe beauty bar. NO commit. NEVER disturb :3030. Preview via hermes-static on :3099, serverId `1d9eb10e-d792-458d-bb55-59fdc0a4be3d`.

## Phase 0 — DONE: baseline capture
Captured all 4 screens via preview (1440x900):
1. Composer/landing — already strong, Claude-like.
2. Build list — sidebar, clean.
3. Build DETAIL (`beginPoll('fin-stripe')`) — delivered hero + proof-of-craft disclosures (code editor preview, source media).
4. PRODUCING (`beginPoll('_uitest')`) — status pill BUILDING, phase track, storyboard cards (hero DONE/wt WORKING/cta QUEUED), script disclosure (VO + shot list), agent-activity panel w/ live walkcast (stripe.com/pricing frame + live pill + action feed).

Fixture `runs/_uitest/` created (frame.jpg, walk/state.json, ledger.json) — DELETE at end.

## Critique vs bar (weakest spots from screenshots)
- Storyboard QUEUED card: diagonal hatch reads cheap; WORKING card spinner floats in vast white.
- Action feed empty state: bare lowercase "action feed", no real empty state.
- Script VO/shot-list loading: sparse single spinner lines.
- Proof-of-craft section eyebrow/rhythm could tighten.
- Walkcast caption + live pill could read more premium.
- Storyboard progress bar / counter spacing.

## Design system (preserve!)
Tokens in :root (styles.css 10-95): --bg/-1/-2/-3, --line/-2, --ink/-2/-3/-4, --amber (coral accent), --pos/--neg, radius --r-ctl 11 / --r-card 15 / --r-pill, --ease-rise/--ease-spring, --sel-fill/-line/--focus-ring. Fonts: --mono Hanken Grotesk, --serif Newsreader, --code mono. Single coral accent. NO EMOJIS.

## Phase 1 — edits DONE

### app.js
- L1423-1433 (liveAgent): action feed now has a designed EMPTY state (`.feed-empty` pulsing dot + "Watching for the agent's next move…") instead of bare header; header gets a leading live dot + capitalized "Action feed".
- L1318+ added `sceneGlyph(type)` helper (maps scene type → line icon).
- L1522-1527 (liveStoryboard): WORKING card now = ghosted type glyph + spinner + "Rendering" label; QUEUED card = ghosted glyph + "Queued" label (was empty hatch box).

### styles.css (all observable changes verified in :3099 preview)
- Storyboard cards (`.sb-*`): radius→--r-card, hover lift on produced, richer working glow; new `.sb-ghost`/`.sb-state-t`/`.sb-spin` slate-frame placeholders; badges→pill w/ leading status dot (pulsing on work); grid minmax 140→160, gap 10→12; lead/counter/progress bar elevated (4px bar w/ inset).
- Action feed (`.feed*`): header divider + live dot; `.feed-empty` empty state; rows roomier.
- Browser chrome (`.browser*`): drop shadow; colored mac traffic-lights; URL in address-bar pill; `.blive` → tinted pill; walkcast caption → leading coral dot + bold-ink step.
- Live header (`.live-head/.live-pill/.live-dot/.live-goal`): gradient surface + shadow; pill→uppercase pill w/ ring dot.
- Phase track (`.ph-chip`): tighter weight, done-state tint.
- Disclosures (`.dsc*`): shadow + hover border; chevron in round well, rotates+colors coral on open; eyebrow weightier.
- Script cards (`.script-head/.badge`): gradient heads, badge depth.
- Proof-of-craft lead (`.proof-lead`): coral tick eyebrow + indented sub.
- Primary CTAs unified (`.cmp-send/.dh-dl/.pg-pay`): coral vertical gradient + inset highlight + lift-on-hover + active press. Stripe-button feel.

## Phase 2 — verify
- `node --check app.js` → OK (twice).
- All 4 screens re-screenshotted, no regressions; producing/feed/walkcast all render.
- Fixture runs/_uitest DELETED.
