# Handoff — dashboard polish (Hera-informed motion/visual layer)

Scope: edited ONLY `dashboard/{index.html,app.js,styles.css}`. Did NOT touch
serve.py / adapters.py / finish_cut.py / remotion_codegen.py / runs/.
NOTE (carried, unrelated to this work): a serve.py restart is still pending from
the earlier declutter pass (the /api/delete endpoint). Not introduced here.

## Phase 1 — D2/D3 hero word-by-word build (DONE)
- app.js: added `heroWords(setup, accent)`, `heroWordsBrand(pre,brand,mid,accent)`,
  `heroAuto(text,tail)`, `buildHeroTitle()`, `payForLine(brand)`. Per-word spans,
  deterministic per-word animation-delay (HERO_STEP=.085s, accent gap .12s),
  accent clause lands LAST in --amber.
- Applied per state: idle `#hero-title` (data-setup/data-accent, built in init() +
  showEmpty()); delivered `.dh-title` (heroWordsBrand → "ready" lands amber);
  live `.live-goal` (heroAuto, builds once per run via state.liveGoalShown);
  pay-gate `.pg-for-v` (payForLine → brand lands amber, once via state.payForShown).
- styles.css: @keyframes blur-rise + grow-in; `.hw/.hw-accent/.hw-brand`;
  reduced-motion guard.

## Phase 2 — D2 motion vocabulary + D1 hero scale (DONE)
- Tokens --ease-rise / --ease-spring; grow-in on .margin-fig/.dh-badge/.pg-price.
- D1: idle .hero-title 30→38px; .dh-title 24→27px; `.proof-fold .margin-fig`
  demoted 64→46px so it doesn't fight the delivered video.

## Phase 3 — D4 kill planning dead-air (DONE)
- liveHeader: `.ph-fill` amber sweep (width by phase index) + ph-sweep shimmer +
  ph-now-pulse on active chip.
- storyboardLead(done,total): "N of M scenes" counter + `.sb-prog` fill bar.
- `.sb-card.s-queued` slow amber shimmer (sb-shimmer).

## Phase 4 — D7 radius tiers + D5 delete refine + D6 pace pill (DONE)
- D7: tokens --r-ctl 6 / --r-card 10 / --r-pill 999 applied to btn, build-go,
  build-in, run-wrap, run-card, dsc, slate, panel, ph-chip, pace pill.
- D5: `.rc-del` hover-reveal now fades AND expands (scale+translateX, spring);
  `.rc-confirm` gets a rise entrance. Two-step confirm logic untouched.
- D6: named pace segmented pill (Snappy/Standard/Cinematic) in build bar; sends
  `pace` in /api/build body (backend-safe extra field; no serve.py change).

## Declutter preserved
- one-primary-focus-per-state intact; native <details> folds (disclosure()) intact;
  run-delete + askDelete/confirmDelete two-step confirm intact.

## Verify ($0) — ALL GREEN
- node --check dashboard/app.js → OK
- styles.css braces balanced (451/451)
- structural per-state render harness → 24/24 PASS across idle / delivered /
  awaiting_payment / building / failed (hero-build markup + .hw-accent amber +
  .ph-fill sweep + N-of-M counter + s-queued shimmer target + <details> folds +
  delete/confirm wiring all present). Harness was temporary (dashboard/
  verify-states.mjs) and has been REMOVED.
- curl of served files on :3030 confirms edits live (app.js 10 matches, styles.css
  24 matches, index.html 5 matches). Static serving works without serve.py restart;
  the pending serve.py restart only affects /api/delete (carried, unrelated).
