# Handoff — Polish & Hardening (FIX 1 emphasis→overlay, FIX 2 load_pricing)
_2026-06-22 · $0, no NIM, no render. Repo is git — NOT committed._

## FIX 1 — Emphasis → walkthrough brief + overlay title — DONE, VERIFIED
Root cause: `_shape_walkthrough` (style_fill.py) derived the overlay title from
`data._text` (the planner's free-text VO), which narrates a DIFFERENT flow than the
emphasized feature. Also two double-"the" grammar bugs when emphasis starts with "the".

Changes:
- `plan_job.py`
  - `import re` added.
  - `_walkthrough_brief_for_emphasis`: emphasis used verbatim → "From the homepage,
    navigate to and demonstrate {emphasis}, showing 2-3 distinct steps." (dropped the
    "the … feature of {brand}" wrapper that made "the the pricing page feature").
  - `_standard_template_plan`: walk_beat strips a leading article from emphasis
    (no more "the the pricing page").
- `style_fill.py`
  - new `_emphasis_phrase()` helper: cleans raw emphasis → title phrase (strips
    leading article, caps first letter, clamps).
  - `_shape_walkthrough`: overlay title now prefers `data._emphasis` (the real run
    emphasis) over `_text`/brief. Order: explicit overlayTitle → "{wordmark} —
    {emphasis_phrase}" → wordmark/emphasis/fallback.
  - `build_props`: reads `plan.job.emphasis` once (`run_emphasis`), threads it as
    `data._emphasis` onto walkthrough-archetype scenes.

VERIFIED (plan-level, $0, stripe.com + emphasis="the pricing page"):
- BEFORE brief: "...demonstrate the the pricing page feature of Stripe..." (double the)
- AFTER  brief: "From the homepage, navigate to and demonstrate the pricing page,
  showing 2-3 distinct steps." → references "pricing", no "subscription", no "the the".
- BEFORE overlayTitle: "Stripe — Follow along as we create a payment link, set amount,
  share URL, test" (planner VO, ignored emphasis).
- AFTER  overlayTitle: "Stripe — Pricing page" → references "pricing", no leaked VO flow.

## FIX 2 — load_pricing hardening + atomic-write guard — DONE, VERIFIED
- `pricing.py`
  - `import time` added.
  - new `PricingConfigError(PricingError)` (so it's a ValueError/PricingError →
    back-compat for existing `except` callers).
  - `load_pricing`: json.load wrapped in try/except JSONDecodeError → retry ONCE
    after time.sleep(0.05) (writer may be mid-save) → else raise
    PricingConfigError(f"malformed pricing config {path}: {e}"). Returns dict
    unchanged on success (all callers unaffected).
  - Atomic-write: NOTHING writes pricing.json at runtime (it's static config; only
    the dashboard fetches it read-only). Added a header comment near PRICING_PATH:
    any runtime config write MUST be atomic (temp + os.replace), citing
    ledger.py:write as the canonical pattern.

VERIFIED: valid load unchanged; malformed file → PricingConfigError (not raw
JSONDecodeError), isinstance ValueError+PricingError; transient torn-write repaired
before retry sleep → recovered.

## TESTS
`./run_all_tests.sh --no-eval` → (see report) — no rebaseline expected (overlay title
still contains brand "Stripe"; brief still contains the emphasis).
