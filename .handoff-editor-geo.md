# Handoff: editable geometry literals (data.geo) for visual editor

Status: COMPLETE + VERIFIED. Backward-compatible. Restore tag: pre-video-editor-20260622-193030.

## What changed
Made hardcoded geometry literals editable as OPTIONAL `data.geo` overrides in the
two requested archetypes, plus click-to-select `data-*` attributes. Every change
reads `data.geo?.KEY ?? LITERAL`, so geo-absent runs (all production runs today)
render BYTE-IDENTICAL to before. Verified by checksum.

Files edited (ONLY these four; no .py, no other archetype):
- studio/src/timeline/types.ts
- studio/src/timeline/archetypes/HeroTitle.tsx
- studio/src/timeline/archetypes/AppleScreenshot.tsx
- studio/src/timeline/Timeline.tsx  (2 call sites only — threaded sceneId)

## Contract added to SceneData (types.ts)
`geo?: Record<string, number>;`  (optional; absent on every current run)

## Parameterized literals (file:line is approx post-edit; key — default)
HeroTitle.tsx:
- glow/plate width  → key `plateW`          default 1200
- glow/plate height → key `plateH`          default 720
- kicker fontSize   → key `kickerFontSize`  default 26
- title line fontSize → key `titleFontSize` default 132
- subtitle fontSize → key `subtitleFontSize` default 34

AppleScreenshot.tsx:
- browser card width        → key `cardW`      default 1380
- browser card borderRadius → key `cardRadius` default 18
- screenshot area height    → key `shotH`      default 712

## Editor key set (per archetype) — what the editor app writes into data.geo
- hero-title:       titleFontSize, subtitleFontSize, kickerFontSize, plateW, plateH
- apple-screenshot: cardW, cardRadius, shotH

## Click-to-select data-attrs (addressability)
sceneId IS wired (added optional `sceneId?: string` prop to both archetypes,
passed `scene.id` from the two call sites in Timeline.tsx SceneBody).
- HeroTitle:       data-scene-id + data-field on plate / title / subtitle nodes
- AppleScreenshot: data-scene-id + data-field="card" on the browser card node
data-* attrs have zero pixel effect (verified byte-identical below).

## Verification (props: runs/r1-stripe/props.json — 2 hero-title + 2 apple-screenshot, no geo)
- `tsc --noEmit` clean (exit 0), before and after.
- geo ABSENT, frame 70 (hero) + frame 160 (apple-screenshot):
  BEFORE vs AFTER stills BYTE-IDENTICAL (sha256 match, `cmp` IDENTICAL).
    before/after-hero  15a4a5dc...  IDENTICAL
    before/after-shot  47004eb9...  IDENTICAL
- geo PRESENT override (titleFontSize:200, plateW:600 ... / cardW:900, cardRadius:60, shotH:400):
  stills DIFFER from baseline (override takes effect; eyeballed correct).
Artifacts in /tmp/geo-verify/ (before-*, after-*, override-*, props-geo-override.json).
