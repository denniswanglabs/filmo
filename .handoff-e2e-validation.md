# E2E validation: fresh video through fixed pipeline (run-id validate-e2e-01)

GOAL: prove a brand-new video runs CLEAN end-to-end through the fully-fixed pipeline at $0
(MOCK only, PRODUCER_SIMULATE_PAID=1 for the pay-gate at $0). NEVER --mode real.

## Phase 0 — preflight (DONE)
- build_runner.py read: PRODUCER_SIMULATE_PAID=1 resolves pay-gate immediately (no 15-min block).
- ffmpeg/ffprobe/node present. python imports OK.
- Stripe TEST key present in ~/.hermes/.env (sk_test_, len 107) -> real cs_test_ session expected.
- Beat-alignment logic present (orchestrator.py 376-440): vo_rec carries beats_total/beats_aligned/
  beats_dropped + segments[] with per-beat start_s. final.mp4 stitched at L448.
- Reference complete run layout: runs/<id>/{plan.json,ledger.json,voiceover.mp3,final.mp4,clips/*.mp4,build.log}

## Checks to validate (6)
1. Phase flow planning->awaiting_payment->producing->delivered; earn block w/ cs_test_ id + payment_status.
2. Complete cut: all planned scenes produced, final.mp4 valid 1920x1080 (ffprobe).
3. Scene-aligned VO: per-scene beats, each placed at scene start offset, no long silent tail; cut-scene beat dropped.
4. Art-directed Remotion titles: mid-hold frame = animated brand-accent bg, brand font, motion.
5. Loudness ~ -14 LUFS (ebur128).
6. On-brand: Stripe palette + copy, no leaked direction-text/jargon on cards.

## Phase 1 — RUN (DONE)
Run completed: status=delivered, phase=delivered. final.mp4 (7.1MB), 6 per-beat VO mp3s + stitched voiceover.mp3.

## RESULTS — PASS/FAIL matrix
1. Phase flow .......... PASS  planning->awaiting_payment->producing->voiceover->stitching->delivered.
   earn: provider=stripe mode=test status=paid session_id=cs_test_a1KuZhMYJq... payment_status=paid livemode=False price=72c.
   (awaiting_payment + delivered phases confirmed via 2nd disk-watched micro-run + code path; intermediate phases only
    retained transiently because ledger stores current phase, not history.)
2. Complete cut ....... PASS  6/6 planned scenes produced (no failure/abort), scenes_cut=[]. final.mp4 = h264 1920x1080
   @30fps, 30.000s, AAC stereo 48k.
3. Scene-aligned VO ... PASS  beats_total=6 aligned=6 dropped=0. Each beat start_s EXACTLY matches produced-clip cumulative
   offset (0.0/4.053/10.112/15.168/19.221/27.264). voiceover.mp3=30.315s == video len; silences are inter-scene gaps only,
   no long dead tail. (Cut-scene drop path NOT exercised — plan had no cut scene; code path verified present.)
4. Art-directed titles  PASS  opening-title + closing-cta frames: Stripe blurple eyebrow, white bold Sohne headline,
   animated indigo->cyan gradient underline, navy gradient + drifting aurora bg (NOT flat), motion confirmed across frames.
5. Loudness ........... FAIL  delivered final.mp4 = -19.9 LUFS (ebur128) / loudnorm input_i -19.91. Target -14.
   ROOT CAUSE: the -14 loudnorm fix lives in finish_cut.py, which the live build_runner->orchestrator.orchestrate->
   adapters.stitch path NEVER invokes. adapters.py:488 comment "the finish pass does the loudnorm" defers it, but no finish
   pass runs in the e2e flow. Fix is sound in isolation: applying loudnorm I=-14 to a COPY of this final -> -14.1 LUFS.
   GAP = wiring, not the filter.
6. On-brand ........... PASS (with quality note)  brand_palette = real Stripe (#0A2540 bg, #635BFF blurple, #80E9FF cyan,
   Sohne, tagline "Build internet businesses"). On-screen CARD copy clean, zero jargon/direction-text. Jargon tokens
   (seedance/gpt_image/etc.) live only in INTERNAL ledger fields, never rendered. NOTE: 3/6 VO beats are scene-DESCRIPTION
   spoken aloud ("A glowing map shows digital transactions..."), but planner-prompt.md L115-116 EXPLICITLY instructs beats to
   "describe what that scene shows" — by design, not a leak. Quality flag, not a bug.

## VERDICT
A fresh video comes out COMPLETE and structurally correct (phases, full cut, scene-aligned VO, art-directed brand titles,
on-brand palette/cards) — 5/6 PASS. The ONE real gap: delivered loudness is -19.9 LUFS, ~6 LU under -14, because the
loudnorm fix is parked in finish_cut.py and not wired into the e2e build path. Single-line fix: have orchestrator stitch
apply loudnorm (or invoke the finish pass) before deliver.

## Cleanup: removing runs/validate-e2e-01 after evidence capture.
