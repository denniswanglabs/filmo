# Handoff — Dashboard typography + corner-radius restyle (Claude-like)

Status: COMPLETE. Static verification passed. No server started (CSS/HTML are
static-served; orchestrator refreshes Safari).

## What Dennis asked
"Find another font for the whole dashboard — use the same font as Claude — and
use rounded corners like Claude does." Apply across the ENTIRE dashboard.

## Fonts — SUBSTITUTION (be honest)
Claude's real typefaces are PROPRIETARY and cannot be embedded:
  · UI / interface  = Styrene A / Styrene B
  · display / reading = Tiempos / Copernicus
Used the closest FREE Google Fonts substitutes instead:
  · "Hanken Grotesk" — warm humanist grotesque ≈ Styrene → ALL UI/body/labels/
    inputs/buttons/sidebar/chips/composer/nav/analytics/about
  · "Newsreader"     — display serif ≈ Tiempos → large display headings + reading
    copy only (hero title, slate/live goal, VO script, briefs, pg-proposal,
    about-h, an-title, styles-h, sc-name, etc.)

Token-level wiring (one place, cascades app-wide):
  · `--mono`  redefined → Hanken Grotesk stack (kept the NAME so all 26 existing
     `var(--mono)` UI refs flip at once). Body font-family is `var(--mono)`.
  · `--serif` redefined → Newsreader stack (fronts the old ui-serif fallback).
  · NEW `--code` token = the original real monospace stack (ui-monospace, SF Mono,
     JetBrains Mono, Menlo, Consolas). Pointed the 4 genuine code/tabular surfaces
     at it so code still reads as code (Claude renders code in mono too):
       .code code, .pg-line .v.mono, .pg-note code, .an-state code
  · Logo wordmark <text> (Walk / Studio) font-family changed Söhne/Inter →
     Hanken Grotesk stack. Two-tone brand (ink "Walk" + coral "Studio") PRESERVED.

Loaded via Google Fonts <link> in index.html <head> (preconnect googleapis +
gstatic, then one css2 link with both families, display=swap). Loads in the
browser, not the shell — $0, no shell network calls.

## Corner radius — Claude's soft/generous rounding (token-level)
Radius tier tokens bumped:
  · --r-ctl   6px  → 11px  (buttons, inputs, chips, sidebar items, small controls)
  · --r-card 10px  → 15px  (content cards, panels, disclosures)
  · --r-pill 999px (unchanged — true pills)
All 11 hardcoded `border-radius:10px` content cards routed to `var(--r-card)`.
Larger panels softened: hiro-composer 18→22, composer 16→20, delivered-hero 14→18,
pg-main 12→16, pg-side 12→16, how-ico 12→14, pg-pay 8→12, sb-card 9→12.
Inner wells/buttons 8px → `var(--r-ctl)` (dh-dl/share, pl-cell) ; preview media
6→10. Small badges/pills bumped: all `5px`→7px; .pill/.sm-badge/.sb-badge/
.shot-tag/.an-mode 4→6; .replay 4→8.
INTENTIONALLY left small/sharp (design motifs, not generic chrome): .receipt 4px
(torn paper edge), .margin-bar 4px (thin progress bar), .clip 4px (filmstrip),
.rcpt-row .lbl .tag 3px, dot/circle 50% radii.

## Files changed
  · dashboard/styles.css  (font tokens + --code + all radius edits)
  · dashboard/index.html  (Google Fonts link + logo wordmark font-family)
  · app.js NOT touched (it has no font-family/radius declarations).

## Verification (static, in-agent)
  · token defs present (lines 63-65 fonts, 74-76 radius). OK
  · `ui-monospace` appears ONLY on the --code token line. OK
  · `ui-serif` appears ONLY on the --serif token line. OK
  · No `Söhne` / stray `Inter` font-family remains. OK
  · Google Fonts css2 link present in index.html (1 match). OK
  · app.js: 0 font-family refs — untouched, no node --check needed. OK
  · Emoji scan grep -nP over css/html/js → empty (exit 1). OK

## Not done / preserved
Coral accent (--amber #D6351C) untouched; palette/spacing/layout untouched.
Analytics tab, About view, Hiro composer, quality toggle, sidebar structure all
intact (only their type face + corner radii changed via tokens).
