# Handoff — walkthrough fill/trim/intro-drop fix

Scope: edit ONLY `apply_real_media.py`. The fix is the VIDEO-GENERATION-REVIEW Top-2 win
(walkthrough reads flat + dominates the cut). Three coherent sub-changes, all in `normalize()`.

## Phase 0 — context read (DONE)
- Read subagent-brief-stock, VIDEO-GENERATION-REVIEW.md, -VERIFICATION.md, apply_real_media.py, finish_cut.py.
- Reference fill filter is `finish_cut.py:61`:
  `scale=W:H:force_original_aspect_ratio=increase,crop=W:H,fps=FPS,setsar=1`

## Phase 1 — evidence on temp copy (DONE)
Copied `runs/demo-1-approve/clips/03_walkthrough.mp4` -> /tmp/wt_src.mp4 (read-only source, protected run untouched).
- ffprobe src: 1920x1080, 30/1 fps, h264, 14.000s.
- cropdetect t=2s: `1440:982:240:60`; t=8s: `1440:960:240:60`. => active content 1440x960 inside 1920x1080 = 33.3% black. Matches verifier.
- Frames: t=1s = GREEN "EXPLAINER AGENT · FEASIBILITY DEMO" intro card (#7CFFB2, off-brand).
  t=2s = black transition. t=2.6s = real docs content (caption "1"). t=13.5s = docs article (caption "3"), still useful, no green outro.

## Phase 2 — decision (DONE)
- FILL: replace `force_original_aspect_ratio=decrease`+`pad(black)` with `increase`+`crop=W:H` (video branch),
  matching finish_cut.py:61. Images keep letterbox-safe behavior? -> No: a still is already authored 16:9;
  but to stay consistent + avoid pad bars on any non-16:9 still, use the SAME fill filter for both. The
  walkthrough is the target; cinematic stills are 16:9 already so increase+crop is a no-op for them.
- TRIM + OFFSET (video only): `-ss 2.6 -t 7.5` => 7.5s window 2.6s..10.1s. Skips intro card + black
  transition; lands fully on real content. Applied to VIDEO inputs only (images still use -t duration_s loop).
- `-ss` placed BEFORE `-i` (input seek, fast + keyframe-accurate enough for this).

## Phase 3 — edit apply_real_media.py (DONE — see diff intent below)
## Phase 4 — verify new filter on /tmp copy (DONE — see evidence below)

## KEY FINDING during verification
Matching finish_cut.py:61 (increase+crop) ALONE does NOT remove the bars: the
source is already 16:9, and the black margins are BAKED INTO THE PIXELS (browser
viewport composited as a 1440x960 card onto a 1920x1080 black canvas). A fill that
only fixes aspect-ratio letterboxing is a no-op here — verified: cropdetect on the
increase+crop-only output still returned 1440:960:240:60. Correct fix = crop to the
content rect FIRST (crop=1440:960:240:60), THEN scale-to-fill (the brief's §3 option
"crop to the actual browser content rect before scaling").

## DIFF INTENT (old -> new), all in apply_real_media.py
normalize() filter:
  OLD (all inputs): scale=W:H:force_original_aspect_ratio=decrease,
                    pad=W:H:(ow-iw)/2:(oh-ih)/2:color=black,fps,format=yuv420p,setsar=1
  NEW non-walkthrough (cinematic vid + stills): scale=W:H:force_original_aspect_ratio=increase,
                    crop=W:H,fps,format=yuv420p,setsar=1   (matches finish_cut.py:61)
  NEW walkthrough: crop=1440:960:240:60, scale=W:H:force_original_aspect_ratio=increase,
                    crop=W:H,fps,format=yuv420p,setsar=1
Walkthrough input branch gains: -ss 2.6 (before -i) + -t 7.5 (trim).
normalize() gained param is_walkthrough; apply() passes is_walkthrough=(type=="walkthrough").
Constants added: WT_SS=2.6, WT_DUR=7.5, WT_CROP=(1440,960,240,60) — named + commented.
The trim/offset/content-crop are gated to walkthrough ONLY, so a Seedance cinematic
video re-normalized by this script is NOT trimmed/cropped (gets the plain fill).

## VERIFICATION EVIDENCE (on /tmp copy of demo-1 03_walkthrough.mp4; protected run untouched)
SOURCE: 1920x1080 30fps h264 14.0s; cropdetect 1440:960:240:60 (33.3% black). t=1s = green
  "EXPLAINER AGENT · FEASIBILITY DEMO" card.
NEW filter output (-ss 2.6 -t 7.5, crop-then-fill): 1920x1080 30fps, duration 7.500s.
  cropdetect = 1920:1080:0:0 across all 223 sampled frames => NO black bars.
  t=0 frame = full-bleed Stripe docs landing (real product content, green intro card GONE).
  t=4s frame = full-bleed "Developer resources" page. Both edge-to-edge.

## STILL NEEDS A REAL BUILD (Dennis's paid step)
End-to-end proof requires a real `--mode real` build that actually invokes the walk-agent + apply_real_media
against a fresh (non-protected) run, then finish_cut. This fix is verified at the filter/code level on a
temp copy only. Did NOT run apply_real_media.py against any protected run.
