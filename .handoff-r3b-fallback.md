# R3-B Fallback Fix Checkpoint — 2026-06-22

## Status: COMPLETE — all tests green

## What was done

### Problem
Bot-blocked brands (TripAdvisor, Shopify) fell back to a SOLID-BLACK box (shot 1) + an EMPTY
card with centred "Brand" text (shot 2). Both brands shared the IDENTICAL 8609-byte placeholder.
This caused D5 imagery to REGRESS from R1 (3.60→3.00) in R2.

### Fixes applied (files: `capture_screenshots.py`, `adapters.py` ONLY)

#### 1. Replaced synth fallback with populated brand-tinted mock (`adapters.py`)
- Added `_brand_mock_png(out_path, url, slot)` — uses ffmpeg `lavfi drawbox` only (no PIL,
  no drawtext/libfreetype required).
- Slot 0: "home/hero" layout — nav bar, large hero accent band, 2 feature tiles.
- Slot 1: "feature/list" layout — filter tabs, 3 list rows with left accent stripe + text bars.
- Brand accent resolved via `remotion_codegen.palette_for()` → fallback to `_BRAND_ACCENT_FALLBACK`
  map (tripadvisor=#00AF87, shopify=#96BF48, stripe=#635BFF, linear=#5E6AD2, plaid=#01B4E4).
- Added `_brand_accent_for_url()` and `_hex_to_rgb()` helpers.
- `generate_screenshots()` now generates BOTH slots (max_shots) on failure, not just one.
- Ultra-fallback (if drawbox itself fails): neutral slate `#1A2436` solid card (not black).

#### 2. De-duplicated the two slots (`adapters.py`)
- Slot 0 and slot 1 use structurally different layouts → different pixel data → different md5s.
- Verified: slot-0 md5 ≠ slot-1 md5 on every tested brand.

#### 3. Hardened capture against bot walls (`capture_screenshots.py`)
- Added `_EXTRA_HEADERS` dict with full realistic headers:
  `sec-ch-ua`, `sec-ch-ua-mobile`, `sec-ch-ua-platform`, `sec-fetch-*`,
  `Upgrade-Insecure-Requests`, bumped Chrome UA to `124.0.6367.82`.
- Playwright browser context now uses `_EXTRA_HEADERS` instead of the old 2-header dict.
- Added `_apex_url(url)` and `_CCTLD_RE` — for ccTLD hosts (e.g. tripadvisor.com.tw),
  tries the apex `.com` URL first before the standard same-URL retry.
- Bot-block retry sequence: (1) apex .com attempt → (2) fresh page with original URL.

## Verification results

### Shopify
- **SUCCEEDS** with real screenshots now: 1.76 MB home + 229 KB scrolled view.
  The new headers got past the Shopify bot wall (or the apex was not needed — shopify.com
  already wasn't ccTLD-blocked, but the improved headers help).
- Both shots are real site captures.

### TripAdvisor (tripadvisor.com.tw)
- Still bot-blocked even on apex tripadvisor.com (Cloudflare is aggressive).
- Fallback fires: 2 populated brand-tinted mock cards.
- slot-0 (mock-home): ~8.4 KB, md5=1cfd653e9d1efb3ee92b0d8be587d192
- slot-1 (mock-list): ~8.5 KB, md5=820ba4f8d4f29fc3de3da12e2fffa123
- md5s DIFFER ✓ — no longer the identical 8609-byte black-box duplicate.

### Before→after fallback
| Before R3-B | After R3-B |
|---|---|
| shot-01: solid black (0A0D0C), ~8609 bytes | slot-0: brand-tinted hero card (accent=#00AF87), ~8400 bytes |
| shot-02: empty "Brand" label card, ~8609 bytes | slot-1: brand-tinted list card (structurally different), ~8500 bytes |
| Both identical md5 | md5s differ |

### Tests
`./run_all_tests.sh --no-eval` → **ALL TESTS PASSED — $0 spent** (all suites OK).

## Files edited
- `/Users/dennis/Desktop/Projects/Hackathons/hermes-video-agent/capture_screenshots.py`
- `/Users/dennis/Desktop/Projects/Hackathons/hermes-video-agent/adapters.py`

## NOT touched
plan_job.py, validate_planner.py, planner-prompt.md, style_fill.py, studio/*, build_runner.py,
orchestrator.py, remotion_codegen.py, brand_extract.py, branding/, any test file.

## Next step for orchestrator
R3-B is LANDED. Gate: when R3-A and R3-C also land → regenerate subset → re-score → record R3.
