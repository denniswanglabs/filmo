# Handoff — Orinovate run: Remotion enrichment ($0)

Run: `runs/build-orinovate-edf804/` — was a 10s PARTIAL (opening-title + recovered
Seedance) after the real build crashed at scene 2 (higgsfield job-id parse abort).
Goal of this pass: enrich at **$0** by producing the FREE Remotion title scenes
with the current art-directed codegen. No PAID scenes produced.

## What was done (all $0)

1. Backed up the current ledger to `ledger.json.pre-remotion-enrich.bak`
   (the pre-existing `ledger.json.bak` is a stale earlier backup — left untouched).
2. Saved the studio's `src/generated/active.tsx` to `/tmp/active.tsx.restore`,
   then **restored it byte-identical** after rendering (verified `diff -q`).
3. Re-rendered the two FREE Remotion title scenes via the exact orchestrator studio
   path — `adapters.studio_overlay(scene, out, palette)` which does
   `remotion_codegen.generate` → write `studio/src/generated/active.tsx` →
   `remotion render src/index.ts Scene <out> --codec=h264` (cwd=studio). Each call
   bounded with `perl -e 'alarm 300'`.
   - `clips/00_opening-title.mp4` — archetype **centered**, 3.05s, render_ms 3459
   - `clips/04_closing-title.mp4` — archetype **editorial**, 4.05s, render_ms 3477
   - Palette: Orinovate resolves to `_default` kit (accent lime `#7CFFB2`,
     accent2 violet `#9A8CFF`, bg `#0A0D0C`, SF Pro Display font stack). brand=Orinovate.
4. Updated ledger scene records:
   - opening-title → `studio.kind=authored`, archetype centered, order 0
   - closing-title → `studio.kind=authored`, archetype editorial, order 4
   - cinematic-motion → **KEPT** as-is (recovered Seedance, `studio.kind=real_media`,
     order 1)
   - NO records for cinematic-still or walkthrough-demo (PAID, not produced).
5. Re-stitched in plan order of available scenes via `adapters.stitch` (video-only,
   no VO present): opening-title → cinematic-motion(Seedance) → closing-title.

## New cut

- `final.mp4` — **14.03s**, **3 clips**, 1920x1080 @ 30fps, video-only.
- scenes_included: `["opening-title", "cinematic-motion", "closing-title"]`
- Structure: 2 Remotion (authored) titles bracketing 1 Seedance cinematic.
  - 0.00–3.05s  opening-title  (Remotion, authored, centered)
  - 3.05–10.08s cinematic-motion (Seedance 2.0, recovered real_media)
  - 10.08–14.03s closing-title (Remotion, authored, editorial)

## Verification (all $0, done)

- ffprobe both title clips: 1920x1080 @ 30fps (opening 3.05s, closing 4.05s).
  (Remotion bakes a silent AAC track; concat maps only `[i:v:0]` so it's harmless.)
- ffprobe final.mp4: 1920x1080 @ 30fps, 14.03s.
- Read mid-hold frame of opening-title: lime "INTRODUCING" kicker + "Orinovate"
  wordmark + lime→violet glowing rule + drifting green/violet bloom bg. Art-direction
  PRESENT (animated brand-accent bg, brand font, motion — not a flat slide).
- Read mid-hold frame of closing-title: lime "GET STARTED" + "Start building" +
  "orinovate.com" + vertical lime accent bar + arrow badge + drifting bloom bg.
  Art-direction PRESENT.
- Read final.mp4 @6.5s: photoreal glass-walled office (Seedance) — cinematic RETAINED.

## Remaining gap (the ONLY PAID scenes left to complete the full video)

- `cinematic-still` (gpt_image_2, PAID) — not produced.
- `walkthrough-demo` (walk-agent, PAID/slow) — not produced.
Producing both (a `--mode real` build or targeted generation) is required to reach
the full 5-scene 30s promo. This pass deliberately stopped at $0.

## Constraints honored

- $0: no Higgsfield / walk-agent / ElevenLabs calls; no `--mode real`.
- Edited ONLY `runs/build-orinovate-edf804/` (clips + ledger) + used studio for render.
- Did NOT edit `remotion_codegen.py`, `dashboard/*`, or other runs.
- active.tsx restored byte-identical. No emoji.
