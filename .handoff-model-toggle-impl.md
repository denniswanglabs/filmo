# Handoff: operator BRAIN model toggle — IMPLEMENTATION progress

Spec: `.handoff-model-toggle.md`. This file tracks the implementation subagent's progress per phase.

## Phase 0 — investigation (DONE)
Read all target files at their CURRENT (post-restyle, post-blank-scenes-fix) state:
- `validate_planner.py` — `call_model(messages)` at L140, NVIDIA_API_KEY at L141, API_URL/MODEL at L15-16. main() calls `call_model(messages)` (no brain) → defaults free.
- `plan_job.py` — blank-scenes fix LANDED (`_enforce_quality`, `_standard_template_plan`, quality-aware). `plan_job(...)` L95, `_plan_with_nemotron(...)` L266, key gate `if not os.environ.get("NVIDIA_API_KEY")` at L268.
- `build_runner.py` — `WS_VO_ENGINE` wiring INTACT (L59-70, L249-261). `run(...)` L168, planner call L207, argparse L383-401.
- `dashboard/serve.py` — quality thread INTACT. `_api_build` L206, quality read L232-235, `inner` cmd L241-245, response L248.
- `dashboard/index.html` — Hanken Grotesk + Newsreader fonts via Google link (L15). Restyle tokens present. `.rail-foot` (operator surface) at L93-107 holds `#rail-about` + `#rail-analytics`.
- `dashboard/styles.css` — `--mono`=Hanken Grotesk, `--serif`=Newsreader, `--r-ctl`/`--r-card`/`--r-pill`, `--amber` coral. `.rail-analytics` operator rows at L186-196. `.pace-opt`/`.is-on` pill pattern at L321-327.
- `dashboard/app.js` — `state` L6-20 (has `pace`, `selection.quality`), payload L967, pace-pill wiring L1931-1935, qual wiring L1940-1944.
- `hermes_judgment_eval.py` — own API_URL/MODEL L28-29, call_model L87-97, key check L89.
- `hermes_skill_eval.py` — own API_URL/MODEL L23-24, call_model L98-107, key check L101.

DECISION: operator brain control placed in `.rail-foot` (operator surface, alongside Analytics — NEVER in the clean Hiro composer). Stored on `state.brain`, sent in `/api/build` payload. Default `super-free`.

## Phase 1 — brain.py (DONE)
Created `brain.py`: BRAINS registry (3 options), DEFAULT_BRAIN=super-free, VALID_BRAINS,
normalize_brain/brain_def/brain_endpoint/brain_key helpers. Stdlib only.

## Phase 2 — validate_planner.py (DONE)
- Imported `brain as brain_mod`. Repointed legacy API_URL/MODEL constants to the registry.
- `call_model(messages, brain=None)` — brain-aware: endpoint=OpenRouter, model=selected slug,
  key=OPENROUTER_API_KEY, added HTTP-Referer/X-Title headers. Default brain=None → super-free.
- main() still calls `call_model(messages)` (no brain) → free Super, $0. Unchanged behavior.

## Phase 3 — plan_job.py (DONE)
- Imported `brain as brain_mod`.
- `plan_job(..., brain="super-free")` — normalize + pass to `_plan_with_nemotron`.
- `_plan_with_nemotron(..., brain="super-free")` — key gate changed NVIDIA_API_KEY → brain_mod.brain_key()
  (OPENROUTER_API_KEY). Both `vp.call_model(...)` calls pass `brain=brain`. Template fallback preserved.
- argparse: added `--brain` (choices=VALID_BRAINS, default super-free); plan_job(... brain=a.brain).

## Phase 4 — build_runner.py (DONE)
- Imported `brain as brain_mod`. `run(..., brain="super-free")`.
- Normalized brain + stamp `os.environ["HERMES_BRAIN"]` (alongside HERMES_STYLE). WS_VO_ENGINE UNTOUCHED.
- Planner call: `plan_job.plan_job(... quality=quality, brain=brain)`. plan["selection"] now {quality, brain}.
- argparse: added `--brain`; run(... quality=a.quality, brain=a.brain).

## Phase 5 — dashboard/serve.py (DONE)
- Added `_BRAINS`/`_DEFAULT_BRAIN` sourced from brain.py (sys.path insert, try/except fallback).
- `_api_build`: read `brain` from body/selection (default super-free); forward ` --brain %s` (shlex.quote)
  in inner cmd (mirrors --quality); response JSON now includes "brain". quality + WS path untouched.
- `.bak` snapshots written: dashboard/{serve.py,app.js,index.html,styles.css}.pre-brain.bak

## Phase 6 — dashboard UI (DONE)
Placement: operator surface in `.rail-foot` (above #rail-analytics), NOT the customer composer.
- index.html: `#rail-brain` group w/ label ("Brain", i-layers icon) + 3 `.brain-opt` buttons
  (data-brain = ultra-paid | super-free[is-on] | super-paid). Each shows the model name + a
  free/paid tag; full spec labels in the title attrs. super-free default-selected.
- styles.css: `.rail-brain` / `.rail-brain-lbl` / `.brain-opt` / `.bo-tag` rules using --mono
  (Hanken Grotesk), --r-ctl rounding, --amber coral selection (matches .rail-analytics / .q-opt).
  Paid tag = coral, free tag = green (--pos).
- app.js: state.brain="super-free"; payload now sends `brain: state.brain`; click wiring on
  `#rail-brain .brain-opt` (one is-on at a time -> state.brain), mirrors the pace pill.

## Phase 7 — eval scripts (DONE)
- hermes_judgment_eval.py + hermes_skill_eval.py: import brain; API_URL=OPENROUTER_URL,
  MODEL=brain_def("super-free")["slug"], call_model keys on brain_key() (OPENROUTER_API_KEY),
  added HTTP-Referer/X-Title; updated docstrings + error messages NVIDIA_API_KEY -> OPENROUTER_API_KEY.
  (The JSON dump still writes {"model": MODEL} which now = the free slug — correct.)

## VERIFICATION (DONE)
- py_compile: PY_COMPILE_OK (brain, validate_planner, plan_job, build_runner, serve, both evals).
- node --check dashboard/app.js: NODE_CHECK_OK.
- brain registry: super-free slug = nvidia/nemotron-3-super-120b-a12b:free; DEFAULT=super-free;
  VALID=ultra-paid,super-free,super-paid.
- emoji scan (true pictographic + variation selectors, excluding typographic arrows): 0 hits.
  (The 11 "→" U+2192 are PRE-EXISTING typographic arrows — identical counts in .pre-brain.bak;
  none in any brain-control line.)
- --brain call chain confirmed UI->serve->runner->plan_job->validate_planner (grep, all hops present).
- $0 super-free SMOKE CALL SUCCEEDED: plan_job(... brain="super-free") hit OpenRouter free Super and
  returned a valid 5-scene/30s standard plan (title + 3 motion_graphic + title, voice=Adam).
  OPENROUTER_API_KEY present (len 73). NO paid slug ever called.
- RESTYLE INTACT: Hanken Grotesk + Newsreader font links present; --mono=Hanken Grotesk,
  --serif=Newsreader, --r-ctl:11px, --r-card:15px all present.
- WS_VO_ENGINE INTACT: VO_ENGINE_ENV const, vo_engine_enabled() def, and the guarded _run_vo_engine
  call in build_runner.py all present and untouched.
- Static assets served correctly on :3030 (brain control HTML/CSS/JS all present in served files).

## NOTES FOR ORCHESTRATOR
- SERVER RESTART: the running `dashboard/serve.py` process predates this change, so its `_api_build`
  won't read/forward `--brain` until the server is restarted. This is backward-compatible — an
  un-restarted server simply omits `--brain` and build_runner defaults to super-free ($0). Restart
  serve.py to activate operator brain forwarding end-to-end. (Restarting the long-running server is an
  orchestrator action, not a subagent one.)
- `.bak` snapshots written: dashboard/{serve.py,app.js,index.html,styles.css}.pre-brain.bak

## FOLLOW-UP (OUT OF SCOPE — do NOT implement here, per spec §7)
- TODO: "Add planner-token COGS for paid brains so cost-plus stays honest." Capture the planner call's
  usage.prompt_tokens/completion_tokens, multiply by brain_def(brain)["price_per_1m"], convert to cents,
  thread into total_cogs_cents before price_for_plan. Touches validate_planner.call_model (return usage),
  plan_job, build_runner._price_plan, producer.cmd_estimate. Safe to defer: the FREE super-free default
  has genuinely $0 planner cost, so cost-plus stays honest as-is unless an operator flips to a paid brain.
