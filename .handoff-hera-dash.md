# Handoff — Hera UI patterns in Walk Studio dashboard

Task: implement Hera (hera.video) UI discipline in the Producer Console, adapted to
OUR palette (light #F4F5F7 + coral #D6351C used SPARINGLY + Hanken Grotesk/Newsreader).
Edit ONLY dashboard/styles.css, dashboard/index.html, dashboard/app.js. NO commit.
Never disturb :3030. Preview = hermes-static :3099 (serverId 1d9eb10e-...). Repo IS git.

## AUDIT (done)
Token system in styles.css :root (L10-95):
- --bg #F4F5F7, --bg-1 #FFF (card), --bg-2 #F7F8FA (recessed well), --bg-3 #EEF0F4,
  --line #E4E7EC (hairline), --line-2 #D5DAE2.
- --amber #D6351C = coral primary. --sel-fill rgba(214,53,28,.06) coral well,
  --sel-line rgba(214,53,28,.55) coral border, --focus-ring coral halo.

### P3 coral over-use to demote (the coral-TINTED active/selected wells):
- L280 `.run-wrap.active { background: var(--sel-fill) }` + L283 coral left-edge +
  L288 hover rgba(214,53,28,.09). DEMOTE fill to neutral (--bg-2/--bg-3); keep a thin
  coral left-edge indicator only.
- L256 `.brain-opt.is-on { background: var(--sel-fill); border-color: var(--sel-line) }`
  DEMOTE to neutral gray well.
- L462 `.q-opt.is-on { border-color: var(--sel-line); background: var(--sel-fill) }`
  + L468/470 coral name/price. Active MODEL row → neutral.
- L400 `.hpill-pace .pace-opt.is-on` coral fill + L1093 `.pace-opt.is-on` coral fill →
  these are the pacing toggle; per spec pacing is NOT in the allowed-coral list, demote
  to neutral dark/ink pill.
KEEP coral: composer PLAY (#i-play / .cmp-send L427), phase-track now/live (.ph-chip.now
L1142, .live-dot, .render-dot), true primary CTAs (.dh-dl Download L985, .hero-start,
pay/export), DELIVERED pill.

### P2 surface unify: most cards already use border 1px --line + radius var(--r-card)
+ bg-1, BUT box-shadows vary (.03/.04/.05) and several have NONE (.slate L727,
.margin-card L769, .strip L788, .gate L806, .studio L829, .panel L1010, .sm-card L1038,
.script-card L1290). PLAN: add one shared `--card-shadow` token (~5% black soft) and
apply to all card surfaces consistently.

### P5 mono icons: coral icons to neutralize (color: var(--amber) on icons):
L192 .rail-new .ic, L221/223 .rail-analytics .ic hover/on, L485 .ex-chip .ic,
L575 .hero-sample .ic, L840 .editor-bar fname svg, L1012 .panel h4 svg,
L1167 .agent-think svg, L1206 .sb-empty svg, L1270 .sb-spin svg, L1308 .vo-empty svg,
L1435 .pg-q-cap svg, L1438 .pg-nourl svg, L1607 .an-state.empty .ic, L1742 .about-built .ic,
L554/556 .how-ico sub/svg. KEEP coral only on .cmp-send play.

### P4 composer chips: index.html L149-209. Quality (Standard/Premium) is nested
bordered .q-opt rows (L192-201) → present aspect/duration/pacing/voice/Brain as white
pill-chips ABOVE input. Keep .cmp-send coral PLAY hero. Must preserve ids app.js wires:
#build-goal #build-url #build-emphasis #build-mock #build-go #cmp-quality .q-opt
#build-pace .pace-opt #aspect-pill.

## STATE — COMPLETE
- [x] audit + BEFORE screenshots (composer + demo-1-approve delivered detail)
- [x] P3 ration coral — active sidebar row / Brain / model / pacing all NEUTRAL now;
      --sel-fill (coral well) used NOWHERE; --sel-line only on input :focus-visible.
      Added --sel-fill-n/--sel-fill-n2/--sel-line-n tokens. Thin coral left-edge kept.
- [x] P2 unify surfaces — added --card-shadow token (0 1px 2px .05, 0 8px 24px .05);
      applied to slate/margin-card/strip/gate/studio/viewer/panel/sm-card (had NONE)
      + dsc[open]/sb-card/script-card/an-card/ab-step/bo-card/about-thesis-card (varied).
- [x] P5 mono icons — neutralized ~15 coral icon rules to --ink-3/--ink-4; kept coral
      ONLY on .cmp-send play + .sc-play storyboard play affordance. Also demoted
      repeating coral badges: ab-step-n, att-n, bo-sub, shot-tag.model, script-head .badge(VO/N).
- [x] P4 composer chips — Quality moved OUT of nested .q-opt description cards INTO a
      white pill-chip segmented control (.hpill-seg/.seg-opt) in the chip row above the
      input, alongside Aspect + Pacing. #cmp-quality/.q-opt/data-quality PRESERVED so
      app.js wiring untouched (verified: state.selection.quality + state.pace toggle).
- [~] P6 route cards — DEFERRED (composer already clean + scannable; didn't fit without
      crowding the single-hero-action layout; not forced per spec).
- [x] verify — node --check app.js OK; CSS braces 869/869; build flow + live panel +
      New-build-return all functional; computed-style checks confirm neutral actives.

CAPTURES: /tmp/hera-dash/before-composer.png + after-composer.png (2880x1800, 2x DPI).
Detail/producing states verified via preview MCP (:3099) inline screenshots.
