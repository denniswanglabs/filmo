# Handoff: loudness verify + test fixes ($0 overnight, mock only)

## Task 1 — test regression FIXED ✅
- File: `tests/test_build_runner_failure.py`
- Root cause: line 235 monkeypatch `lambda url, goal, dur: fake_plan` didn't accept the
  new `style=` kwarg that `build_runner.run()` now passes at `build_runner.py:93`
  (`plan = plan_job.plan_job(url, goal, target_duration, style=style)`). Production code fine;
  the MOCK was stale.
- Fix: `lambda url, goal, dur, **kw: fake_plan`
- Confirmed: `python3 -m unittest tests.test_build_runner_failure` → 9 tests, OK.

## Task 2 — new tests wired into runner (audit F1) FIXED ✅
- File: `run_all_tests.sh`
- Added section 2b (5 unittest modules) + 2c (stripe_earn by path):
  - `tests.test_adapters_higgsfield_parse` (25) OK
  - `tests.test_adapters_cinematic_prompt` (21) OK
  - `tests.test_remotion_codegen_cardcopy` (24) OK
  - `tests.test_orchestrator_budget_bridge` (5) OK
  - `tests.test_build_runner_failure` (9) OK
  - `tests/test_stripe_earn.py` (script, by path) 15 checks 0 failures
- `./run_all_tests.sh --no-eval` → SUITE_EXIT=0, ALL TESTS PASSED — $0 spent.

## Task 3 — loudness verify CONFIRMED ✅ (PASS)
- Fresh $0 mock build: stripe.com, goal "30-second explainer plus a short walkthrough",
  run-id verify-loudness-01, --mode mock --pace 0, PRODUCER_SIMULATE_PAID=1.
  Completed: status=delivered, phase=delivered, final.mp4 = 6.6 MB, 30.0s, 5 clips + VO.
- Loudnorm IS firing in the build path — it lives in `adapters.stitch()` (adapters.py:746):
  `-af loudnorm=I=-14:TP=-1.0:LRA=11` applied when muxing VO over the concat master.
  (Brief's "~592-604" was the synth_voiceover region; the actual loudnorm sits at ~739-751.)
- MEASURED delivered final.mp4 integrated loudness:
    - loudnorm print_format JSON: input_i = -15.11 LUFS
    - ebur128 (authoritative):    I = -15.1 LUFS
  → lands ≈ -14 LUFS (within ~1.1 LU of target; NOT the broken -19.9). PASS — no gap.
  The ~1 LU shortfall is the normal single-pass dynamic-loudnorm estimation residual,
  not a wiring failure. (If exact -14 is ever required, a 2-pass measured loudnorm in
  stitch() would close it — flag only, NOT fixed here per brief.)
- Cleanup: `rm -rf runs/verify-loudness-01` done; `dashboard.serve.rebuild_runs_index()`
  run to drop the stale index row (15 surviving runs, verify-loudness-01 gone).

## Files edited (only the two permitted)
- tests/test_build_runner_failure.py  (monkeypatch lambda: +**kw)
- run_all_tests.sh                     (sections 2b + 2c)
No pipeline source touched.
