# Verifier handoff — Orinovate "blank scenes" fix

## VERDICT: PASS

The fix eliminates the blank scenes. RENDERED the studio `Timeline` composition from
the $0/offline-engine props and inspected pixels: every VO span — including the two
formerly-blank cinematic scenes and the walkthrough beat — now renders a real, on-brand
explainer card. No frame is the old flat amber `0xB45309` placeholder; no frame is flat.

Scratch dir (only files I wrote): `runs/_verify-blankscenes/`
- props.json, timeline.json, vo_alignment.json (reproduced $0)
- chunkA.mp4 / chunkB.mp4 / chunkC.mp4 (frames 0-302 / 303-637 / 638-906 @ scale 0.5)
- stills/*.png (60 stills @ 2fps)

## Phase 1 — reproduced props ($0 offline, NO paid APIs)
`style_fill.run_pipeline(d97898 plan, branding/orinovate-brand-theme.json,
"orinovate-kinetic-light", out, fps=30, do_align=True, do_render=False)` — edge-tts synth +
local whisper-cli (~/.cache/whisper/ggml-base.en.bin). 5 scenes, 907 frames @ 30fps.
- THEME = CURATED light/navy/blue: bg `#ffffff`, navy `#1a3a5c`, accent `#2563eb`,
  text `#0f2338`, wordmark "Orinovate". NOT the old generic dark+mint (#0A0D0C/#7CFFB2).
- Routing (the fix):
  - opening-title  -> hero-title     [frames 0-106]
  - cinematic-aerial   -> EXPLAINER-CARD [106-382]  (was solid amber 2-11s)
  - cinematic-dashboard-> EXPLAINER-CARD [382-637]  (was solid amber 11-20s)
  - walkthrough-setup  -> EXPLAINER-CARD [637-838]  (was dropped/failed)
  - closing-cta    -> hero-title     [838-907]
  Spans are contiguous 0->907, no gaps. ExplainerCard bullets = real brand features
  (3D Printing / CNC Machining / Sheet Metal / Laser Sintering), numbered badges, NO emoji.

## Phase 2 — rendered (Remotion CLI, composition id "Timeline", src/index.ts, --concurrency=8, --scale=0.5)
Chunked under the Bash cap (perl alarm 520): chunkA 303f/817kB, chunkB 335f/734kB,
chunkC 269f/683kB. All 907 frames rendered; NONE is a ~24KB flat-color clip.

## Phase 3+4 — pixel assertions (ALL PASS)
Per-still PIL mean+stddev over 60 stills:
- FLAT frames (max channel stddev < 4): **0**.  min spatial stddev across all frames = **4.94**
  (a flat solid color is ~0; the old blank had stddev 0). The two lowest-variance frames
  (A_001 @ t0.0s, C_015 @ t≈28s) are light-bg word-rise fade entrances, well above flat, NOT amber.
- PLACEHOLDER-color frames (mean within tol of B45309 / 1F3A8A / 0F766E / 0A0D0C / 3A0D0D): **0**.
  Every frame mean is light (~240-253), confirming the light theme.
- 1x1 avg-color of each chunk mid-frame (diagnosis method): A=`fefeff` B=`f2f3f7` C=`eeeff3`
  — NOT `b45309`.
- signalstats YAVG varies 238-254 across the whole timeline (old blank was CONSTANT 105) -> real content/motion.
- Explicit amber-pixel scan (within tol 20 of 0xB45309) over A_014/B_011/B_005/C_008
  (formerly-blank cinematic + walkthrough): **0 amber pixels of 518400 each**. Not one placeholder pixel survives.

Visual read (stills inspected):
- A_006 opening-title: HeroTitle, "Orinovate" wordmark + navy "On-demand manufacturing, instant quotes", light bg.
- A_014 cinematic-aerial (was BLANK amber): ExplainerCard — navy rail, blue "ORINOVATE" kicker,
  navy word-rise title "A sweeping aerial shot over a modern factory", blue underline sweep,
  numbered bullets 1 3D Printing / 2 CNC Machining / 3 Sheet Metal. On-brand, legible.
- B_011 cinematic-dashboard (was BLANK amber): ExplainerCard "A composed hero image of the Orinovate",
  staggered numbered bullets revealing, navy/blue on white.
- C_008 walkthrough-setup: ExplainerCard "Demonstrate the user navigating the Orinovate",
  4 numbered bullets incl. Laser Sintering.
- C_016 closing-cta: HeroTitle "Orinovate" wordmark + kicker, light bg.

## Remaining blank spans: NONE.

Notes for orchestrator:
- This verifies the ENGINE picture path (style_fill -> Timeline render) the impl agent wired,
  rendered directly via the Remotion CLI from reproduced props (impl agent had not rendered).
- I did NOT run the full `build_runner.py --mode mock` end-to-end (the WS_VO_ENGINE wrapper in
  build_runner + the orchestrator money path), nor the WS_VO_ENGINE=0 legacy-path-reachable
  check — out of scope for a blank-frame pixel proof, but those remain unverified if the
  orchestrator wants full integration coverage.
- Rendered at scale 0.5 (sufficient for a blank/flatness/theme check); a final delivery render
  should be full-res.
