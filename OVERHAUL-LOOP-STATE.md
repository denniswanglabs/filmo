# OVERHAUL-LOOP-STATE — Walk Studio video overhaul (self-improving generate→judge loop)
_Resume doc + LESSONS ledger. A fresh context reads this top-to-bottom to continue the loop without losing state. Started 2026-06-23 ~01:30. The loop is SELF-IMPROVING: every round, read this whole doc, analyze the full round, append lessons, and improve — never repeat a mistake (Dennis's explicit requirement)._

> **AUTONOMOUS OVERNIGHT RUN — Dennis authorized ~02:35 2026-06-23, away ~8h (won't check until ~10:30am).** On EVERY re-invocation (agent completion or watchdog event): IMMEDIATELY CONTINUE — judge the latest render (extract + READ per-scene frames), append the round's lesson here, dispatch the next round's fix agents (background, watchdogged), re-render `overhaul-stripe-rN+1`, and **re-arm the single watchdog** (Monitors die at ~40min, but an agent completion always re-invokes you — keep an agent OR a fresh Monitor in flight so the chain never stalls). Do NOT wait for the user or ask questions; spend tokens freely for quality. Tear down the prior watchdog before arming the next (recurring orphan bug). Push a milestone + sample stills (SendUserFile, proactive) each round so Dennis wakes to progress. Keep going until Stripe clears the rubric (≥36/45, layout/coherence/motion each ≥4), THEN generalize to 1–2 other brands. Current frontier: R2 (visual done = near-white; content in flight = copy/logo/walkthrough) → re-render + judge R2 next.

## GOAL
Perfect the **Stripe** Walk Studio video to the quality of Dennis's **Orinovate `kinetic-light`** + **TapPay** promos — full Luceo-grade motion graphics, coherent story, **text-left / screenshot-right** format (text and screenshot reinforce each other), brand logos, background music — THEN generalize to other brands. Videos people *want* to watch.

## ARCHITECTURE (locked by Dennis)
- **Generator = Nemotron 3 Super (free, $0)** — plans the Stripe storyboard/copy + drives the build.
- **Judge = Claude (orchestrator)** — renders, WATCHES the frames, scores vs the rubric, drives improvements.
- **Self-improving:** every round read this doc + the LESSONS LEDGER, analyze the whole round, record what worked/failed and WHY, improve. Lessons compound.

## DECISIONS (Dennis, 2026-06-23)
1. Scope: perfect ONE brand (Stripe) first, then generalize.
2. Animation: FULL Luceo-grade motion graphics.
3. Logos: YES — pull the brand's real logo (don't care about trademark).
4. Music: Pixabay OK; use his downloaded tracks; pick one by the company's style.
5. Reference/bar: Orinovate + TapPay videos.
6. ALL scenes (hero/screenshot/walkthrough/CTA) get the new motion system.

## REFERENCES / ASSETS
- Orinovate kinetic-light: `~/Desktop/Projects/Orinovate/orinovate-kinetic-light/`, `orinovate-video-zh/` (rendered `orinovate-video-zh/out/orinovate-kinetic-light-1080p.mp4`).
- TapPay (phone-animation motion Dennis loves): `~/Desktop/Projects/Demos/tappay-promo/` (`renders/`).
- Music pool (pick by style): `Demos/{tappay-promo,alai-promo,cluely-promo-zelios,kuli-promo,webduino-promo}/public/music.mp3`.
- Stripe logo: NOT yet fetched — fetch from stripe.com (favicon/og/SVG) via brand_extract.
- **THE BUILD SPEC:** `VIDEO-OVERHAUL-SPEC.md` (repo root) — written by Phase-0 agent `a78a5d13`. The build follows it.

## CURRENT STATE (handoff)
- **Clean slate:** 127 prior runs archived to `~/walk-studio-runs-archive-20260623-011654` (reversible); `runs/` empty except operational jsons; dashboard rail empty.
- **Dashboard:** durable on http://localhost:3030 (`dashboard/serve.py`, nohup). Editor folded in at `/editor/`; Abort button live; stale-session cache-bust shipped.
- **GitHub:** today's work pushed (commit 1362251, github.com/denniswanglabs/walk-studio). UNCOMMITTED since: copy-fix (`style_fill.py` + `capture_screenshots.py`) + `.gitignore` dist → fold into next commit. The 2 strategy docs are gitignored (NOT shared with Nous).
- **Mock = Standard (USE FOR THE LOOP, $0):** a `--mode mock` build with `PRODUCER_SIMULATE_PAID=1 --brain super-free` now runs the REAL `walk_native` walkthrough + standard output at $0 (no payment, free brain). `WS_WALKTHROUGH_NATIVE=1` is set by build_runner.
- **Pipeline fixes already in (today):** editor integration; screenshot headlines (style_fill); walkthrough placeholder brand-colored + dropped when not a real capture; mock=standard; abort; headline-truncation (`_clip_to_clause`) + screenshot distinctness (capture_screenshots).

## PHASE PLAN (8h)
- **Phase 0 (DONE):** `VIDEO-OVERHAUL-SPEC.md` written — text-left/UI-right layout DNA; `frame-rise` (TapPay phone-rise → browser frame); reuse `motion.ts` + named gaps (frameRise/cursorAt/highlightBox/zoomPunch/rollNumber/nfcRipple); music = `Demos/tappay-promo/public/music.mp3` for Stripe; real-logo capture in brand_extract; 7-scene arc (Hook→What→3 How beats→Proof→CTA); 9-dim rubric, ship ≥36/45 (layout/coherence/motion each ≥4); 8-task fan-out A–H; SHARED files = motion.ts/types.ts/Timeline.tsx.
- **Phase 1 Wave 1 (IN PROGRESS):** Task A FOUNDATION agent `ad6a0800` (motion.ts primitives + types.ts optional fields + Timeline.tsx wiring incl. BGM `<Audio>`) ∥ Task F PIPELINE agent `a5aa0275` (real logo capture + screenshot focus-rect + music pick). **Wave 2 (after A lands):** archetype agents — B = screenshot-v2 (text-left/right split + highlight/cursor/zoom keystone), C/D = hero/CTA bookends + walkthrough motion + cursor/camera ports (each edits its OWN archetype .tsx, disjoint, using A's motion/types). **Then Phase 2:** Stripe plan/fixture (Task G) → `$0` mock render → JUDGE round R1. NOTE: Monitors/agents are SESSION-BOUND — a fresh context must re-arm the watchdog and check each agent's `.handoff-*.md` + outputs to resume.
- **Phase 1:** fan out build agents per the spec — text-left/right archetype + animated browser/device frame + callout (text↔shot); motion layer in `motion.ts` + archetypes applied to ALL scenes; logo fetch+placement; music mix (ducked under VO). Judge each vs the Orinovate/TapPay bar.
- **Phase 2:** the self-improving generate→judge loop on Stripe (below).
- **Phase 3:** lock + commit + generalize-test on 1–2 other brands.

## THE LOOP (run each round R_n)
1. **READ** this whole doc (esp. LESSONS LEDGER) + `VIDEO-OVERHAUL-SPEC.md`.
2. **GENERATE:** `$0` mock Stripe build — `cd <root> && source ~/.zshrc; PRODUCER_SIMULATE_PAID=1 python3 build_runner.py --url https://stripe.com --goal "<goal>" --emphasis "<feature>" --run-id overhaul-stripe-rNN --mode mock --quality standard --brain super-free --duration 30` (background; ~minutes; real walk_native walkthrough).
3. **JUDGE:** ffmpeg-extract frames across every scene + READ them; score each rubric dimension 1–5 vs Orinovate/TapPay; list the top 2–3 gaps.
4. **ANALYZE & LEARN:** review the whole round vs the prior — what changed, what the score did, what worked/failed + WHY. Append a LESSONS entry.
5. **IMPROVE:** dispatch fresh-context fix agents (background, watchdogged) for the top gaps (motion/archetype/planner-prompt edits).
6. Append the round to ROUND LOG. Loop until every dim ≥4/5 (Orinovate/TapPay-grade) or time/context runs out. Push a milestone + sample stills when Dennis is away.

## RULES (Dennis, durable)
Delegate task-work to BACKGROUND subagents (prepend `~/.claude/templates/subagent-brief-stock.md`); fan out; keep chat free. Watchdog ANY in-flight agent (Monitor: 15-min heartbeat + ~10-min-flat stall; follow symlinked output via `wc -c <`). Spend tokens for fast+good. Mock=$0 — flag before any real-money burst (paid = `ultra-paid` 550B nav / ElevenLabs / Higgsfield). No emojis. Canonical light palette, coral rationed. To VERIFY a render, pin the exact run-id + read frames from THAT final.mp4 (never grep-guess; a final.mp4 is frozen at render-time code — fixes only show in NEW renders). [[feedback_video_run_disconnect]] [[feedback_self_improving_orchestrator]]

## >>> VERDICT (R7, 2026-06-23): OVERHAUL COMPLETE — Stripe + 2 verticals all clean ≥4 keystones <<<
**The perfected Walk Studio video system is NOT Stripe-overfit, and all 3 verticals now clear every keystone.** Judged from each delivered `final.mp4` (R7 = the small brand-agnostic copy/capture polish on top of the R6-proven system):
- **Stripe r7 ~38/45** — all 3 keystone dims ≥4 (layout 4.5 / coherence 4 / motion 4.5); NO regression.
- **Notion g4 ~36/45** — all 3 keystone dims ≥4 (layout 4 / coherence 4 / motion 4); NO regression (distinct headlines, grounded supporting lines, clean close + walk overlay).
- **Allbirds g2 ~38/45** — coherence keystone LIFTED 3.5→4 (layout 4.5 / coherence 4 / motion 4.5). The R6 copy bugs are GONE: no section-label supporting lines, no dangling "&"/"made" tails (opening + walk overlay + screenshot headlines all complete), and the geo-shipping modal is DISMISSED so the product shots are no longer occluded.

**ALL THREE VERTICALS CLEAR the brief's bar:** Stripe (fintech) ≥38, Notion (SaaS) ≥36, Allbirds (e-commerce) ~38 — each ≥36 total with layout/coherence/motion all ≥4. The system is data-driven, not overfit, and the residual copy/capture polish is now closed. **Shopify's earlier miss remains a KNOWN PRODUCT CAPTURE-ROBUSTNESS LIMITATION** (server-side geo-IP zh-TW + bot-block from this machine's egress; the brand-agnostic en-pin/locale-strip/favicon code is correct but can't beat it from this network path) — separate from generalize. **OVERHAUL COMPLETE — Stripe + 2 verticals all clean ≥4 keystones.**

## ROUND LOG
- **Phase 1 BUILD COMPLETE (2026-06-23 ~02:00).** All 4 agents landed + verified (tsc 0; stills read):
  - Foundation: `motion.ts` primitives (frameRise/cursorAt/highlightBox/zoomPunch/rollNumber/nfcRipple/approveChip) + `types.ts` optional fields + `Timeline.tsx` ducked music `<Audio>`. Contract in `studio/src/timeline/.handoff-foundation.md`.
  - Pipeline: real logo capture (`theme.logoSrc`, real Stripe SVG confirmed), screenshot `focus` rect (`scene.data.focus`), music pick (`theme.music` → staged `studio/public/music.mp3` = tappay track). `.handoff-pipeline.md`.
  - Screenshot-v2 (KEYSTONE): `AppleScreenshot.tsx` split layout (text-left / browser-frame screenshot-right + highlightBox+cursor+zoomPunch over `data.focus`). Verified still `/tmp/b-split.png`. `.handoff-screenshot-v2.md`.
  - Bookends/walkthrough: HeroTitle(hero+CTA pill)/WalkthroughPlayer(frameRise+highlight+cursor+zoom)/AppleHero/Statement/Registry/CardUi/ExplainerCard got motion + logo corner-mark. Stills `/tmp/cd-hero.png`,`/tmp/cd-walk.png`. `.handoff-bookends.md`.
- **GAP found (close before R1):** `AppleScreenshot` defaults to CENTERED unless `data.layout=="split"`; NOTHING sets it yet → a build renders the OLD format. FIX in `style_fill.py` `_shape_screenshot`: set `layout="split"` for apple-screenshot scenes + populate `eyebrow`(from kicker)/`supporting`/`punchWord`. (Task G.) Then the new format renders.
- **R1 procedure (the loop's first round):** (1) close the split gap in style_fill; (2) generate the `$0` mock Stripe build (`PRODUCER_SIMULATE_PAID=1 python3 build_runner.py --url https://stripe.com --goal "A 30-second brand explainer" --emphasis "accept payments in one integration" --run-id overhaul-stripe-r1 --mode mock --quality standard --brain super-free --duration 30`); (3) ffmpeg-extract a frame from EACH scene of `runs/overhaul-stripe-r1/final.mp4` and READ them; (4) score the 9-dim rubric (in VIDEO-OVERHAUL-SPEC.md) vs Orinovate/TapPay; (5) append an R1 lessons entry below + dispatch fix agents for the top gaps; (6) R2. Render uses live `src/` archetypes (no editor rebuild needed for the render; rebuild editor only for the /editor preview). Verify `theme.music`/`logoSrc`/`focus` actually resolve in the render.
- **R1 RESULT (run `overhaul-stripe-r1`; judged scene02 + agent gap-list; frames `/tmp/r1-scene0{1..4}.png`):** FORMAT WORKS — split renders (eyebrow + purple-punch headline + supporting; real Stripe shot in a browser frame; highlight-box). Music staged (AAC in mp4). **R2 GAPS:** (1) **bg is DARK NAVY → must be near-white `#F6F9FC`** (biggest visual miss vs the Orinovate/TapPay light bar) + the whole light treatment; (2) **copy incoherent / text↔shot MISMATCH** — headline "billing model" vs eyebrow "ACCEPT PAYMENTS" vs highlight on "Sign up with Google" vs supporting "Build internet businesses" (repeats across scenes 1/2/4); the highlight/focus must target what the headline NAMES, lines must cohere + not repeat (planner-prompt + `style_fill` grounding + focus↔punch alignment); (3) **logoSrc NOT staged** though real `screenshots/brand/logo.svg` was captured — fix Task-F staging in `style_fill`/`brand_extract` so `theme.logoSrc` is set; (4) **walkthrough scene = flat purple placeholder** — mock=standard didn't yield real walk_native UI; investigate (did walk_native run/capture? did it fall back?). Rubric spot: layout ~4, coherence ~2, light ~2, copy ~2.5. **R2 = two disjoint fix agents:** R2-VISUAL (near-white bg + Luceo-light treatment in `studio/src` archetypes/theme) ∥ R2-CONTENT (copy coherence + focus↔headline tie + logo staging + walkthrough investigation, in `style_fill`/`build_timeline`/`adapters`/planner-prompt) → re-render `overhaul-stripe-r2` → judge.
- **R2 FIXES APPLIED + RE-RENDER IN PROGRESS (agent `a9bfbf4c`):** R2-VISUAL done (`lightenTheme` in Timeline → near-white `#F6F9FC`, white cards; fixed walkthrough black letterbox). R2-CONTENT done — copy coherence (`_focus_for_headline` re-targets the highlight to the headline's subject; distinct per-scene eyebrows + `_supporting_line` dedup; planner-prompt now mandates one concrete feature/scene + distinct multi-shots), **logo staging wired** (`apply_captured_logo` now called with the manifest — R1's logo was captured but never applied), **walkthrough SALVAGE** (`_salvage_walk_frames` stitches the real captured frames — R1 had 140 genuine Stripe frames but timed out before stitching; bumped 200→230s). Re-rendering `overhaul-stripe-r2` + extracting `/tmp/r2-scene*.png`.
- **NEXT (fresh context — JUDGE R2):** read `/tmp/r2-scene*.png`, score the 9-dim rubric vs Orinovate/TapPay, append the R2 lesson here, dispatch R3 fix agents for the top gaps, re-render → R3. Check: did bg go light? logo render? walkthrough REAL (clip >100KB, not the 31KB purple placeholder)? copy cohere + highlight aligned? **scene count** — planner may still emit only ONE apple-screenshot scene; if so, fix `plan_job` STANDARD shot cap so there are 2–3 distinct screenshot beats. Then continue the autonomous loop per the directive at the top.
- **R2 RESULT (run `overhaul-stripe-r2`, delivered; agent checklist + orchestrator spot-judge of scene03):** MAJOR step — ALL R1 gaps fixed: light near-white render (note props `theme.bg` still reads dark `#0A2540` but Timeline `lightenTheme` lightens at RENDER → judge FRAMES not JSON), real Stripe logo renders, walkthrough is a REAL ~411KB clip (53+ real payments-page frames, salvaged), copy coheres + highlight aligned, 2 distinct screenshot scenes. Scene03 (split, stripe.com/pricing) spot-judged **~4/5** (layout 4.5, coherence 4, light 4, copy 3.5 — headline slightly wordy). Sent Dennis the scene03 still. **NEXT (fresh context = FULL R2 JUDGE):** read ALL `/tmp/r2-scene0{1..5}.png` (only scene03 spot-judged; still need hero/screenshot-2/walkthrough/CTA), score the 9-dim rubric, append the per-scene R2 lesson here, then dispatch **R3 POLISH** (likely: tighten headline punchiness, motion/pacing, highlight precision, + any hero/walkthrough/CTA gaps). If R2 already ≥36/45 with layout/coherence/motion ≥4, declare Stripe near-bar and start GENERALIZE (re-run on 1–2 other brands). The watchdog `birkvm5la` (on the now-done R2-render) re-invokes ~12-15min to trigger this — when you dispatch R3, TEAR DOWN birkvm5la and re-arm onto the R3 agent.

- **R2 FULL JUDGE (all 5 frames) — per-dim:** layout 4, coherence 2.5, motion 3.5, kinetic-typo 3.5, brand-truth 4.5, story-arc 4, pacing 3.5, audio 4, polish 3. **Total ~32.5/45** (below the 36 gate; coherence <4). Killers identified across ALL scenes (not just scene03): (a) **`focus.label` raw DOM text rendered as an ugly monospace caption plastered over the screenshot** on scenes 2+3 — the coherence killer (looked like debug text on the shot); (b) **headlines too long** — scene03 "Enable any billing model with Stripe's flexible subscriptions" wrapped to 4 lines in the 660px left column (orchestrator's flag confirmed + scene02 "Stripe is the backbone" also weak); (c) **CTA scene read as a 2nd opening title** — plain muted tagline "Build internet businesses", no CTA pill (slogan did NOT land on the CTA); (d) **opening eyebrow "STRIPE" echoed the wordmark/logo** right above it (redundant chrome). Light bg ✓, real logo ✓, real walkthrough ✓ all held from R2 fixes.
- **R3 RESULT (run `overhaul-stripe-r3`, delivered; full 5-scene judge, frames `/tmp/r3-scene0{1..5}.png` + `/tmp/r3-scene05-late.png` = landed CTA):** POLISH round — 4 disjoint fixes, all verified in the render (tsc 0; 69 style_fill+build_timeline unittests OK):
  1. `AppleScreenshot.tsx` — **removed the `focus.label` text overlay**; highlight box is now a clean accent ring only (label stays upstream rect-picking metadata, never rendered). Coherence killer GONE (scenes 2+3 confirmed clean).
  2. `style_fill._shape_screenshot` — screenshot headline limit **64→38**; headlines now punchy 2-line ("Accept payments", "Enable any billing model" — was the 4-line wall).
  3. `style_fill._shape_hero` (close) — closing subtitle upgraded to **"Start now → stripe.com"** when it lacks a CTA signal → `HeroTitle.isCtaBeat` fires the accent CTA pill (confirmed at frame 920). `_brand_domain` now also reads `brand.host`.
  4. `style_fill._shape_hero` (open) — **dropped the wordmark-echo eyebrow** when the logo/wordmark already shows above the headline (scene01 eyebrow now empty).
  **R3 per-dim:** layout 4.5, coherence 4, motion 3.5, kinetic-typo 4, brand-truth 4.5, story-arc 4.5, pacing 4, audio 4, polish 4. **Total ~37/45.** Clears the ≥36 gate, no dim <3, layout+coherence ≥4 — BUT **motion (dim 3) = 3.5, still <4** (the dims-1/2/3-each-≥4 sub-gate is NOT fully met). Gap = the **walkthrough scene reads small/floating** (real Stripe clip but a small contained frame with big margins; weak vs TapPay device-hero motion) and held-frame life can't be proven ≥4 from stills. **STRIPE AT-BAR on total + 2 of 3 keystone dims; ONE dim (motion) short.**
- **NEXT (fresh context — R4 = MOTION):** the ONLY thing between here and the full gate is dim-3 motion ≥4. Dispatch ONE fix agent on the **walkthrough-player** (`studio/src/timeline/archetypes/WalkthroughPlayer.tsx`) + `motion.ts`: make the walkthrough clip fill the frame like the TapPay device-hero (larger frame, frame-rise arrival, a cursor/zoom-punch pass or KPI counter-roll overlay so it isn't a small static clip), and add held-frame life (parallax-drift) anywhere a beat holds >40f. Re-render `overhaul-stripe-r4`, re-judge dim 3. If motion hits ≥4 → **STRIPE CLEARS → generalize to 1–2 other brands** (re-run build_runner on a non-fintech URL; the picks are data-driven so only data should change). Watchdog the R4 agent; tear down any prior watchdog first.

- **R4 RESULT (run `overhaul-stripe-r4`, delivered; full 5-scene judge + walkthrough motion-sweep, frames `/tmp/r4-scene01..05.png` + `/tmp/r4-scene04-{arrive,click,mid,settle}.png`):** MOTION round — the walkthrough-player rebuilt as a **device-hero** (`WalkthroughPlayer.tsx`, backup `.pre-r4.bak`; tsc 0). Changes, all SELF-SUFFICIENT (work with the walkthrough scene's data, which carries NO focus/cursor/zoom/kpi — so the motion must be synthetic):
  1. **Frame OWNS the canvas** — card 1420→**1640** wide, clip window 738→**880**, nudged up slightly. R3's small floating card is gone; the captured Stripe UI now fills the frame like the TapPay phone (verified `/tmp/r4-scene04-*`). `videoFit` default flipped to **cover** when style_fill doesn't set it (the r4 run set `contain`, but the 1280×800 clip's aspect is close enough that it still nearly fills — no big margins).
  2. **Auto Ken-Burns zoom-drift** (`zoomPunch`, dur 70, slow push toward a derived hotspot) so the held UI is ALWAYS moving even with no scene data.
  3. **Auto guided cursor** (`cursorAt`) — synthesizes a lower-left→hotspot→click path with a ripple when `data.cursorPath` is absent; the "guided walkthrough" gesture.
  4. **Auto highlight ring** (`highlightBox`) draws on at the cursor click, tied to the hotspot (`data.focus` overrides geometry).
  5. **KPI counter-roll chip** (`rollNumber` + `approveChip` + `tailGlow`) — a floating glass stat that rolls up; only renders when a real number exists in `data.kpi`/overlayTitle (NEVER invents; r4 had none so it correctly stayed hidden). New OPTIONAL `data.kpi` field in types.ts.
  6. Motion verified across **arrival(470)→click(512)→mid(620)→settle(760)** — the clip plays through DIFFERENT real states (homepage → open nav mega-menu), cursor points, ring holds, Ken-Burns pushes; nothing freezes.
  **R4 per-dim (R3→R4):** layout 4.5→4.5, coherence 4→4, **motion 3.5→4.5**, kinetic-typo 4→4, brand-truth 4.5→4.5, story-arc 4.5→4.5, pacing 4→**3.5** (the walkthrough scene is 11s > the 9s budget — minor new drag), audio 4→4, polish 4→4. **Total ~38.5/45.** Gates: total ≥36 ✓; no dim <3 ✓; **layout 4.5 / coherence 4 / motion 4.5 — ALL THREE keystone dims ≥4 ✓.**
- **STRIPE CLEARS → GENERALIZE next (re-run on 1–2 other brands to confirm not Stripe-overfit).** The full ship gate (≥36/45 AND layout/coherence/motion each ≥4) is MET. Next: re-run `build_runner.py` on 1–2 non-fintech URLs (e.g. a SaaS + an e-commerce brand) with `--mode mock --quality standard --brain super-free` and judge — every pick is data-driven, so ONLY the data (copy/logo/shots/clip) should change; if a non-Stripe render scores <36 or drops a keystone dim <4, that's a Stripe-overfit bug to fix in the archetype/style_fill, not in per-brand data. Watch the new pacing watch-item: the walkthrough scene's duration (cap it ≤270f / 9s in `plan_job`/`build_timeline` if it keeps overrunning).

- **GENERALIZE-NOTION (run `overhaul-notion-g1`, delivered $0 mock; full 5-scene judge + walkthrough motion sweep; frames `/tmp/notion-scene0{1..5}.png`, `/tmp/notion-scene05-late.png`, `/tmp/notion-walk-{arrive,click,mid,settle}.png`, `/tmp/notion-c01-3.0.png`, `/tmp/notion-c02-3.0.png`, `/tmp/notion-ss-inner.png`).** Brand = Notion (SaaS/productivity, deliberately non-fintech). Total 36.3s / 5 scenes (title→ss-home→ss-inner→walkthrough→cta). **THE BIG FINDING: the `build_runner.py --mode mock` render path is a DIFFERENT pipeline than the `studio/src/timeline` split-archetype system that R1–R4 perfected for Stripe.** Per ledger `scenes[].studio`: only the TITLE scenes (00 opening, 04 cta) are `kind="authored"` Remotion via `remotion_codegen.py` (archetypes `centered`/`editorial`) — the screenshot + walkthrough scenes (01/02/03) are `kind=None`, rendered as **raw full-page captures with Ken-Burns drift + letterbox**, NOT the text-left/screenshot-right split archetype (no browser chrome, no left kinetic headline column, no highlight ring, no frame-rise). So the R1–R4 split work (AppleScreenshot.tsx etc.) renders ONLY in the `studio/` Remotion studio, and `build_runner` mock never invokes it for body scenes. **This is the headline overfit/architecture finding for the orchestrator:** the Stripe 38.5/45 was scored against the `studio/` render; the actual product pipeline (`build_runner`) does not use it. (The brief's R1–R4 frames may have come from a studio render, not a build_runner final.mp4 — worth the orchestrator reconciling which final.mp4 the Stripe scores judged.)
  - **What HELD (not Stripe-overfit, all data-driven, correct):** (1) brand accent is **Notion blue `#2383E2`** everywhere (eyebrows, underline, rules) — ZERO Stripe purple `#635BFF` leakage; grep of plan/theme/ledger = 0 occurrences of `635BFF`/`F6F9FC`/`0A2540`; the only "Stripe" string in the ledger is payment-plumbing ("payment received via Stripe Checkout"), not copy. (2) **Real Notion logo SVG captured + staged** (`screenshots/brand/logo.svg` = the genuine Notion wordmark path, renders in the corner mark) — not the derived blue-block fallback. (3) **Walkthrough is a REAL clip** (`clips/03` 1.5MB, 1280×800, 293 captured frames, walk reached `notion.com/product`) — NOT the 31KB purple placeholder; the R2 salvage holds for Notion. (4) Copy is grounded in Notion (tagline "One workspace. Every team.", "Meet the night shift", "Keep work moving 24/7", "notion.com" CTA) — coherent, no Stripe leakage. (5) `focus.label` ("Meet the night shift.") stays metadata, never rendered as a caption (L1 held).
  - **OVERFIT / BRAND-AGNOSTIC BUGS for the orchestrator to fix:**
    1. **(brand-agnostic, BIGGEST) Body-scene render path bypasses the split archetype.** Screenshot/walkthrough scenes in `build_runner --mode mock` render as raw full-bleed captures, not the split layout. Either route body scenes through the `studio/` archetypes, or port the split layout (browser chrome + left headline + highlight) into `remotion_codegen.py`/the screenshot compositor so the product pipeline actually shows the R1–R4 craft. This is why Notion's layout/coherence read far below Stripe's studio scores.
    2. **(overfit) Near-white bg is NOT brand-agnostic — it was Stripe-specific.** Title bookends render on Notion's DARK `#191919` (Notion's real brand bg). The Stripe `lightenTheme` that forced `#F6F9FC` either (a) only runs in the `studio/` path, or (b) is gated on a fintech/light assumption. The spec/rubric anchor is the Orinovate/TapPay LIGHT bar; Notion's dark bookends + dark "Meet the night shift" hero break that anchor. Decide: is near-white a universal treatment (then apply a `lightenTheme`-equivalent in `remotion_codegen`), or is dark OK when the brand is genuinely dark (then update the rubric so dim-1/9 don't penalize honest dark brands)? Right now it's neither — inconsistent (scene01 dark, scene02-inner intro light `#F6F9FC`).
    3. **(brand-agnostic) Walkthrough is 16.3s (>>9s budget) and letterboxed.** Plan said 10s; rendered clip is 16.3s (worse than Stripe's 11s, L9). It's a long single auto-scroll of the marketing homepage (not a guided 2–3-step product demo as the brief intended), and the 1280×800 clip is letterboxed with black bars top/bottom in the 1920 frame (does NOT own the canvas like the R4 TapPay device-hero — the R7/L7 footprint fix also lives only in the `studio/` WalkthroughPlayer, not this path). Cap ≤270f and cover-fit in the build_runner path too.
    4. **(minor, brand-agnostic) CTA eyebrow echoes the headline.** Close renders eyebrow "GET STARTED" directly above headline "Get started" (redundant, the L4 echo problem in a new spot) with only a faint "→" as CTA signal and "notion.com" as supporting — no logo lockup, no CTA pill. Land "Start now → notion.com" on the headline + drop the echoing eyebrow (apply L3/L4 to the `editorial` codegen archetype, not just `HeroTitle.tsx`).
    5. **(watch-item) KPI chip:** plan surfaced NO public Notion number (no `data.kpi`), so the counter-roll chip correctly stayed hidden (L8 honest-degrade held) — but this means the KPI path is still UNTESTED on a real number. Notion does have public figures (e.g. "100M+ users") the planner could surface; if the orchestrator wants to prove the chip lights up, feed a brand whose plan carries a KPI string.
  - **Notion per-dim (vs Orinovate/TapPay bar, judging the build_runner final.mp4):** layout **2.5** (raw full-bleed shots, no split, letterboxed walkthrough — far below Stripe-studio's 4.5), coherence **2.5** (headlines live on the raw screenshots' own text, not a left column tied to a highlight; VO is generic "keep work moving 24/7" boilerplate), motion **3** (Ken-Burns drift on shots + real title kinetics, but no frame-rise/cursor/highlight on body scenes), kinetic-typo **3.5** (title scenes have eyebrow+serif+accent+underline; body scenes have none), brand-truth **4.5** (real blue, real logo, real surfaces — the strongest dim), story-arc **3.5** (hook→shots→walk→cta present; bookends a bit generic), pacing **2.5** (16.3s walkthrough blows the budget), audio **3.5** (AAC stereo present, RMS -21.8dB/peak -4.8dB continuous; ledger shows only STITCHED, no explicit finish_cut music-mix event — may be VO-only, unconfirmed BGM), polish **3** (clean light intro frame but letterbox bars + raw-shot look). **Total ~28.5/45 — BELOW the 36 gate; layout/coherence/motion all <4.** 
  - **HOLDS-THE-BAR? NO.** Notion does NOT clear the ship gate through the `build_runner` pipeline. The DATA generalized cleanly (blue not purple, real logo, real walk, grounded copy = no per-brand-data overfit). The CRAFT did not — because the build_runner mock render path never invokes the R1–R4 split archetypes/light treatment/device-hero walkthrough. The fixes are brand-AGNOSTIC pipeline-routing/port issues (bugs 1–4 above), not Notion-specific data. **Recommendation for the orchestrator:** before any per-brand polish, reconcile which render path the Stripe scores judged and unify body-scene rendering so `build_runner` actually uses the studio split-archetype craft (or port it). Then re-run Notion.

- **GENERALIZE — SHOPIFY (run `overhaul-shopify-g1`; e-commerce, $0 mock; full 5-scene judge + walk motion-sweep; frames `/tmp/shopify-scene0{1..5}.png` + `/tmp/shopify-scene05-late.png` + `/tmp/shopify-walk-{arrive,click,mid,settle}.png`; handoff `.handoff-generalize-shopify.md`).** Build EXIT=0, 31.68s, 1920×1080 h264+AAC, 949f, 5 scenes (title→ss-home→ss-inner→walkthrough→cta).
  - **⚠️ RENDER-PATH RECONCILIATION (resolves the Notion agent's BIG FINDING above — they CONFLICT):** The Notion agent concluded `build_runner --mode mock` body scenes (`kind=None`) render as RAW full-bleed Ken-Burns captures and never hit the split archetype. **Shopify's render DISPROVES that.** Shopify's ledger has the IDENTICAL shape (only title-open/title-close are `kind="authored"`; screenshot-home/screenshot-inner/walkthrough are `kind=None`), YET the Shopify `final.mp4` clearly renders the FULL split archetype — browser chrome, left kinetic headline column, green accent underline, highlight rings, device-hero walkthrough. Build.log proof: `final.mp4` is ONE continuous Remotion render of `Composition "Timeline"` (949 frames, "Getting composition / Composition Timeline"), reading `props.json` which carries `layout:"split"`, `frame:"browser"`, `eyebrow`, `punchWord`. The `clips/*.mp4` are intermediate per-scene captures, NOT the source of `final.mp4`. **Conclusion: the studio split-archetype craft IS in the build_runner product pipeline; `studio.kind=None` is NOT a "raw render" signal.** The Notion agent likely judged a different artifact (concatenated `clips/` or a stale final.mp4) — orchestrator should re-extract Notion frames from the single-Timeline-render `runs/overhaul-notion-g1/final.mp4` and re-judge before acting on Notion bugs 1–2 (they may be measurement error, not a real pipeline gap).
  - **SYSTEM IS NOT STRIPE-OVERFIT (generalizes cleanly):** accent = **Shopify green `#96BF48`** (NOT Stripe purple `#635BFF`), ink `#0F2338`, navy `#1A3A5C`, near-white `#FFFFFF` bg, wordmark "Shopify", CTA "Start now → shopify.com". ZERO hardcoded Stripe colors/copy/labels. Split text-left/UI-right layout, line-stagger + green accent-pop, device-hero walkthrough (R4 synthetic cursor+highlight-ring+Ken-Burns all FIRE; clip plays home→signup through distinct real states), centered bookends, slogan-on-CTA pill (L3 fired), BGM staged (`music-overhaul-shopify-g1.mp3`) — all data-driven and held. Near-white light treatment DID apply for Shopify (unlike the Notion-dark case) since Shopify's brand bg is genuinely white.
  - **Per-dim:** layout 4.5, **coherence 2.5**, motion 4.5, kinetic-typo 4, brand-truth 3.5, story-arc 4.5, pacing 3.5, audio 4, polish 4. **Total ~35/45 — BELOW the 36 gate; coherence 2.5 (<4 keystone).** HOLDS-THE-BAR? **NO** — but every miss is in the DATA layer (copy grounding / logo / capture locale), not the visual system.
  - **Brand-agnostic BUGS (orchestrator to fix, serialized — parallel Notion agent shares files):**
    1. **[coherence, biggest] Supporting lines are raw scraped marketing fragments** ("Your brand has entered the chat", "Meet your secret weapon, Sidekick") unrelated to the eyebrow/headline. `_supporting_line` in style_fill pulls brand `features[]` blindly; ground it to the headline subject like `_focus_for_headline` does for the ring.
    2. **[coherence] Screenshot headline doesn't name the UI shown** — "Run payments, shipping" sits over a product-collection list. Headlines come from the planner emphasis, not page content; re-target the headline to the page or pick the page to match.
    3. **[brand-truth] Both split screenshots clipped at the left edge** (wordmark/first column cut off) — browser-card crop/object-position is off for wide captures in `AppleScreenshot`.
    4. **[brand-truth] Real logo NOT staged** — no `brand-logo-overhaul-shopify-g1.svg` (the Notion run DID stage one); logo capture (inline SVG/favicon/og:image) failed silently for shopify.com → derived green-block wordmark fallback. Honest-degrade, but below the Stripe-R3 real-SVG bar.
    5. **[brand-truth/coherence] Walkthrough drifted into zh-TW (Taiwan) locale** — captured Shopify UI is Traditional Chinese against an English script. `walk_native` didn't pin `Accept-Language: en` / `?locale=en`. Geo-dependent, brand-agnostic.
    6. **[pacing] Walkthrough = 330f / 11s > 9s budget** — the R9 watch-item RECURS on a 2nd brand → now confirmed systemic; cap walkthrough ≤270f in `plan_job`/`build_timeline`.
  - **Watch-items:** KPI chip — Shopify surfaced NO parseable public number in the plan ("millions" = a word, not a number); chip correctly stayed HIDDEN (L8 honest-degrade held). The chip-LIGHTS path is STILL untested — needs a brand whose plan carries a real number string. walk_native timed out (>230s, output_exists:False) but `_salvage_walk_frames` stitched 200 real frames → 3.76MB clip; **salvage fallback generalizes.**

- **R5 = GENERALIZE-FIX (2026-06-23; runs `overhaul-{shopify-g2,notion-g2,stripe-r5}`; all EXIT=0 $0 mock; full 5-scene judge + walk motion-sweep + CTA-late from each `final.mp4`).** Applied the diagnosis FIX-1/2/3 + the Shopify/Notion brand-agnostic bugs as SMALL grouped edits. **Verify gate: `studio` tsc --noEmit EXIT 0; 69 style_fill+build_timeline unittests OK.**
  - **FIXES APPLIED (by file):**
    1. `build_timeline.py` — **walkthrough scene window clamped ≤270f/9s** (`WALKTHROUGH_MAX_FRAMES=270`, `_WALKTHROUGH_ROLES`), capping the `media_frames` floor that drove the 10-11s overruns; preserves a longer VO span (`max(270, span_frames)`) so narration is never cut. **CONFIRMED in props: Stripe walk 11→9.0s, Shopify 11→9.0s, Notion 10→9.5s (the .5 = a 9.5s VO span, correctly preserved).** L9 closed.
    2. `style_fill.py` — (a) **`_supporting_line` grounded to the headline subject**: features re-ranked by stem-overlap with the headline (mirrors `_focus_for_headline`); a crude `_stem` (strips ing/ment/s + un-doubles "shipp"→"ship") so "payments"↔"Payment processing", "ship"↔"Shipping" match; degrades to first-distinct feature on zero overlap (never worse). (b) **close-hero headline clamped to ≤48 chars** via `_clip_to_clause` — **Notion's 4-line CTA → 2 lines ("Start using Notion today at notion.com")**, confirmed in `/tmp/notion-cta-late.png`. (c) **`_surface_aligned_headline`** names the captured surface from the shot `<title>`/capture-label (pricing/dashboard/payments → "Pricing"/"Dashboard"), only overriding when the VO headline doesn't already reference the surface; `_load_screenshot_manifest`+`wire_captured_assets` now thread `_shot_title`/`_shot_label`. **CONFIRMED on Stripe scene03: headline "Pricing & Fees" over the `stripe.com/pricing` page (was a mismatch).**
    3. `capture_screenshots.py` — (a) **dark-hero scroll**: `_frame_yavg` + `_DARK_HERO_YAVG_CEIL=80`; a dark above-the-fold homepage hero (rich body) reshoots a scrolled-down surface and keeps it only if ≥15 YAVG lighter (else restores the hero from a backup). (b) **logo `/favicon.ico` last-resort fallback + stderr log on EVERY fallback step** (svg→icon/og→favicon→derived) so a silent degrade is impossible. (c) wide-shot fix in `AppleScreenshot.tsx`.
    4. `studio/src/timeline/archetypes/AppleScreenshot.tsx` — **`objectPosition: top center → top left`** on the shot `<Img>` (both split + centered paths) so a wide capture under `objectFit:cover` keeps the left-edge wordmark/first-nav column instead of cropping it.
    5. `walk_native.py` — **`Accept-Language: en`** (bare, was `en-US,en;q=0.9`) + a **`_GEO_LOCALE_RE` post-goto re-navigation** that strips a `/tw/`-style locale segment once.
  - **PER-BRAND (before→after, judged from the new `final.mp4`s):**
    | Brand | R-prev total | R5 total | layout | coher | motion | verdict |
    |---|---|---|---|---|---|---|
    | **Stripe r5** | 38.5 (r4) | **~38.5** | 4.5 | 4 | 4.5 | **≥38 ✓ NO REGRESSION** (walk now 9s — pacing 3.5→4; Pricing scene now names the page) |
    | **Notion g2** | 28.5 (g1, mis-measured) | **~33.5** | 4 | **3** | 4 | ✗ <36 — fixes WORKED (close 2-line, walk ≤9.5s ENGLISH, real logo+blue) but **scenes 2&3 share an IDENTICAL headline+eyebrow** ("The AI workspace…") + 4-line opening hero + close scene padded to 11.3s |
    | **Shopify g2** | 35 (g1) | **~32.5** | 4 | **2.5** | 4 | ✗ <36 — **NOT a code regression: shopify.com CAPTURE FAILED (`real=False`, mock placeholder shots, no logo) + walk drifted to zh-TW DESPITE the en/locale fix** (Shopify geo-IP-serves Taiwan from this machine + bot-blocks capture). Plus a 3-line truncated opening hero ("…line up") |
  - **GENERALIZE PROVEN? NO (this round).** Stripe holds ≥38 with all 3 keystone dims ≥4 and the walkthrough-pacing fix actually IMPROVED it. The fixes are demonstrably correct WHERE THE PIPELINE GETS REAL DATA (Stripe: surface-aligned Pricing headline + 9s walk; Notion: 2-line close + 9.5s English walk + real logo). But neither Notion nor Shopify cleared 36: **the residual gaps are NOT in the visual system — they are (i) a NEW copy bug (duplicate screenshot headlines, no headline-dedup) + a long-opening-hero/close-pad pacing miss on Notion, and (ii) a Shopify-specific ENVIRONMENT block (capture + walk both fail / geo-zh-TW) that brand-agnostic en-pinning can't override from this machine.**
  - **R6 PRECISE GAPS:**
    1. **[Notion, coherence, BIGGEST] Duplicate screenshot headlines.** Both apple-screenshot scenes derive the SAME headline+eyebrow because Notion's `<title>` ("The AI workspace that works for you. | Notion") is identical on home + /product AND the VO beats distill to the same line. There is supporting-line cross-scene dedup (`_used_supporting`) but NO headline dedup. R6: add a `_used_headlines` cross-scene list (threaded like `_used_supporting` in `build_props`); when scene N's headline duplicates an earlier one, fall to the surface-aligned headline (scene2="Workspace"/home, scene3="Product"/the /product surface) or the next distinct grounded clause.
    2. **[Notion+Shopify, pacing/layout] Opening-hero headline too long.** Notion opens on a 4-line serif wall, Shopify on a 3-line truncated "…line up". Apply an L2-style clamp to the OPENING hero headline in `_shape_hero` (the R5 clamp only touched the CLOSE branch). Also the Notion CLOSE scene window padded to 11.3s (the `target_duration_s` last-scene pad on a 37.2s plan) — cap the close hold or shorten the plan total.
    3. **[Shopify, environment] Capture + walk fail for shopify.com from this machine.** `real=False` mock shots + zh-TW walk. NOT fixable in brand-agnostic code alone. Options for R6: (a) pick a different e-commerce brand that doesn't geo-block (e.g. bigcommerce.com, squarespace.com, or a US-served store) to PROVE generalize on a 2nd brand whose capture succeeds; (b) add a `branding/shopify-brand-theme.json` fixture + a pre-staged English Shopify shot if Shopify specifically must be the proof; (c) route capture through a US-egress proxy. Recommend (a) — the point is to prove NOT-overfit, which a clean-capturing 2nd brand does.
  - **Watch-items:** logo `/favicon.ico` fallback + dark-hero scroll are IN but UNEXERCISED here (Stripe/Notion captured a real header SVG; Shopify never reached a live page) — they'll exercise on the next brand whose homepage hero is dark / whose logo is favicon-only. KPI chip still untested (no brand carried a real number).

- **R6 = GENERALIZE-FIX-2 (2026-06-23; runs `overhaul-{notion-g3,allbirds-g1,stripe-r6}`; all $0 mock; full 5-scene judge + walk sweep + CTA-late from each `final.mp4`).** Applied the two SMALL grouped fixes the R5 gaps named (headline dedup + bookend pacing). Shopify replaced by **Allbirds** (clean-capturing e-commerce) per L12 — don't burn rounds on a geo-blocked brand. **Verify gate met BEFORE render: `studio` tsc --noEmit EXIT 0; 69 style_fill+build_timeline unittests OK.**
  - **FIXES APPLIED (by file; backups `*.pre-r6.bak`):**
    1. `style_fill.py build_props` — **`_used_headlines` cross-scene list** threaded to every apple-screenshot scene (mirrors `_used_supporting`). 
    2. `style_fill.py _shape_screenshot` — on a headline collision with an earlier scene, derive a DISTINCT headline from THIS scene's own surface (`_surface_aligned_headline`), else a distinct grounded brand value-prop, else drop the headline (never a duplicate). The eyebrow is `_short_eyebrow(headline)` so a distinct headline auto-yields a distinct eyebrow. **CONFIRMED in the render: Notion scene3 was an IDENTICAL "The AI workspace…" twin in R5 → now "Calendar" (eyebrow "02 · CALENDAR") matching the `notion.com/product/calendar` shot; Allbirds scene2 "Comfortable, Sustainable Shoes &" vs scene3 "On Sneakers" — distinct.**
    3. `style_fill.py _shape_hero` (OPENING branch) — **clamp an OVER-56-char opening title** to a complete shorter sub-clause, but ONLY when the clamp lands exactly at a comma/clause boundary (preserves a complete phrase; rejects a hard mid-thought chop). Deliberately does NOT touch titles ≤56 (the grounded-headline ceiling) — the existing tests (`test_real_opening_headlines_pass_unchanged`, `test_hero_opening_title_keeps_numeric_noun`) require complete long headlines like "Allbirds makes comfortable shoes from natural wool" (50ch) and "...800 million trusted reviews" (70ch) to pass UNCHANGED. **CONFIRMED: Notion opened on "Notion is the connected workspace" (clamped at the comma from the 90-char wall) — 3 lines but COMPLETE, vs R5's 4-line wall.** (LIMITATION: a long grounded headline with NO comma boundary still can't be clamped without breaking the tested cases — see L13; Allbirds opened on a complete-but-dangling "...comfortable shoes made".)
    4. `build_timeline.py` (last-scene pad) — **cap the close-hero pad to the per-scene 9s budget**: a multi-scene film's last scene pads to at most `max(270f, its content floor)`, leftover target time dropped (a slightly shorter film beats an 11s dead static close). Single-scene films honor the full target. **CONFIRMED: Notion close 11.3s (R5) → 5.4s (R6); Stripe close 5.9s, Allbirds close 4.0s.**
  - **PER-BRAND (before→after, judged from the new `final.mp4`s):**
    | Brand | R5 total | R6 total | layout | coher | motion | verdict |
    |---|---|---|---|---|---|---|
    | **Stripe r6** | ~38.5 | **~38** | 4.5 | 4 | 4.5 | **≥38 ✓ NO REGRESSION** (distinct headlines, Pricing surface-align held, walk 9s, close 5.9s — dedup + opening-clamp + pad-cap did not drop it) |
    | **Notion g3** | ~33.5 | **~36** | 4 | **4** | 4 | **✓ CLEARS the gate** — the R5 coherence KILLER (identical scene2/3 headline+eyebrow) is FIXED → scene3 "Calendar"; close 11.3→5.4s; opening complete (not the 4-line wall). All 3 keystone dims ≥4. |
    | **Allbirds g1** | n/a (new) | **~36** | 4 | **3.5** | 4.5 | **✗ keystone coher 3.5<4** — captured CLEANLY (real yellow `#F0C511`, real script logo, real US/EN surfaces, distinct headlines, device-hero walk through real states); BUT two supporting lines are raw section labels ("Popular Picks"/"All Sale") + two headlines dangle ("...Shoes &", "...shoes made") from page-`<title>`/emphasis clipping. Total at the gate, coher keystone short. |
  - **GENERALIZE PROVEN? YES (on the brief's 2-vertical bar).** Stripe ~38 with all 3 keystone dims ≥4; Notion ~36 with all 3 keystone dims ≥4 (fintech + SaaS = 2 verticals proven, NOT overfit). Allbirds (e-commerce, a 3rd vertical) captured cleanly and reached the ~36 total with layout 4 / motion 4.5, missing ONLY the coherence keystone (3.5) on a small, brand-agnostic COPY bug (section-label supporting lines + dangling-tail headlines) — NOT the visual system, NOT environment. The fixes that the R5 gaps named are demonstrably correct in the render on every brand that reaches real data.
  - **SHOPIFY GEO/BOT-BLOCK = KNOWN PRODUCT CAPTURE-ROBUSTNESS LIMITATION (separate from generalize).** shopify.com bot-blocks the headless capture (`real=False` mock shots, no logo) AND geo-IP-serves zh-TW to this machine; the brand-agnostic en-pin + `/tw/`-strip + favicon fallback are all CORRECT and IN but cannot beat server-side geo-IP + an active bot-block (L12). This is a capture-ROBUSTNESS limitation of the product from THIS egress (fix = US-egress proxy / a curated fixture / a residential IP), NOT a generalize gap in the visual system. Allbirds — a clean-capturing e-commerce brand — proves the e-commerce vertical works; Shopify specifically needs a different network path.
  - **R7 PRECISE GAPS (all small, brand-agnostic COPY, in `style_fill`/`capture` — NOT the visual system):**
    1. **[Allbirds, coherence, BIGGEST] Section-label supporting lines.** `_supporting_line` fell to scraped section labels ("Popular Picks", "All Sale") when NO brand `features[]` overlapped the headline. These pass the current `_is_ui_nav_label`/`_is_weak_headline` gates but read as chrome. R7: when the best feature has ZERO stem-overlap with the headline AND is itself a short Title-Case section-noun (fails `_looks_like_headline` = no verb), DROP the supporting line rather than render a section label (an empty supporting line is cleaner than "All Sale"). Apply the same positive-headline test `_shape_hero` uses for the opening subtitle.
    2. **[Allbirds, coherence/polish] Dangling-tail screenshot headline from page-`<title>` clipping.** `_surface_aligned_headline` clipped "Comfortable, Sustainable Shoes & Apparel" at 38ch → "Comfortable, Sustainable Shoes &" (dangling "&"). `_clip_to_clause` strips dangling WORDS but not a dangling trailing "&"/conjunction-symbol. R7: add "&"/"and"/"or"/"+" (and a trailing "·"/"|") to the dangling-tail strip in `_clip_to_clause`/`_strip_dangling_tail`.
    3. **[Allbirds, coherence] Opening + walk-overlay headline dangles on "made".** The opening hero "Allbirds offers the world's most comfortable shoes made" (53ch, no comma) and the walk overlay "Allbirds — Comfortable everyday shoes made" both end on a dangling participle "made" (a clause-bridge). The opening-clamp (R6 fix 3) can't touch it — no comma boundary, and ≤56ch is the protected band. R7: extend the `_DANGLING_PARTICIPLES`/`_DANGLING_WORDS` strip so a headline NEVER ends on a bare past-participle "made"/"built"/"designed" (and apply it to the walk `overlayTitle` / emphasis phrase, not just `_clip_to_clause`). This is the residual of the "opening wall" gap that the comma-boundary clamp can't reach (L13).
    4. **[Allbirds, polish] Geo-shipping modal overlays the product shots.** Both screenshot captures show allbirds.com's "Where are we shipping to?" interstitial over the product. R7 (capture-side, brand-agnostic): dismiss a common geo/cookie interstitial (`[aria-label*=close]`, `button:has-text("Confirm")`, ESC) before the screenshot, OR scroll past it (the dark-hero-scroll machinery is adjacent).
  - **Watch-items:** KPI chip STILL untested (no brand carried a real public number string into `data.kpi`); favicon-logo fallback + dark-hero scroll still unexercised (all 3 R6 brands captured a real header SVG / light hero). Next brand with a public headline number lights the chip; next dark-hero/favicon-only brand exercises those.

- **R7 = OPTIONAL POLISH — lift Allbirds coherence keystone 3.5→4 (2026-06-23; runs `overhaul-{allbirds-g2,notion-g4,stripe-r7}`; all $0 mock EXIT 0; full 5-scene judge + CTA-late from each `final.mp4`).** Applied the 3 SMALL brand-agnostic copy/capture fixes the R7 gaps named. Generalize was ALREADY PROVEN (R6); this round closes the residual copy/capture polish so all 3 verticals clear every keystone. **Verify gate met BEFORE render: `studio` tsc --noEmit EXIT 0; 69 style_fill+build_timeline unittests OK (NOT regressed; L13-protected opening headlines pass unchanged).** Backups `*.pre-r7.bak` (style_fill / capture_screenshots / walk_native).
  - **FIXES APPLIED (by file):**
    1. `style_fill.py _supporting_line._distinct` — **drop a SECTION/nav label** (`_is_section_label_subtitle(c)` → reject): a short Title-Case noun phrase with no verb ("Popular Picks"/"All Sale"/"Shop All") is store chrome, not a value-prop. Reuses the SAME positive-headline gate `_shape_hero` applies to the opening subtitle. Falls to the next feature/tagline/brief, else "" (an empty supporting line beats a section label). **CONFIRMED: Allbirds scene2/3 `supporting` = None (R6 rendered "Popular Picks"/"All Sale").**
    2. `style_fill.py _strip_dangling_tail` (+ new `_DANGLING_SYMBOLS`, `_DANGLING_PAST_PARTICIPLES`) — **strip a dangling connector SYMBOL** (`&`/`+`/`·`/`•`/`|`/…, bare "Shoes &" or glued "Apparel|") AND a **dangling PAST-participle tail** ("...shoes made"/"...software built"/"...products designed"). Only ever inspects the LAST token, so a COMPLETE phrase keeps its object ("shoes made from natural materials" untouched, ends on a content word). Because `_clip_to_clause`/`_punchy_headline`/`_emphasis_phrase` all route through `_strip_dangling_tail`, this fixes the opening hero headline, the screenshot headlines, AND the walk `overlayTitle` (emphasis → `_emphasis_phrase` → `_clip_to_clause`) in one place — brand-agnostic, every brand. **CONFIRMED: Allbirds scene1 "Allbirds makes comfortable shoes" (was "...shoes made"); scene2 "Comfortable, Sustainable Shoes" (was "...Shoes &"); scene4 overlay "Allbirds — Comfortable everyday shoes" (was "...shoes made"). Interior "&" preserved: Stripe scene3 "Pricing & Fees" intact = NO regression.**
    3. `capture_screenshots.py` + `walk_native.py` (new `_dismiss_interstitial(page)` in BOTH) — **dismiss a geo/shipping/region/cookie interstitial before the shot/walk.** Brand-agnostic + non-destructive: clicks the first VISIBLE close/dismiss/confirm/accept affordance (13 selectors; close/dismiss FIRST so we never submit an unrelated form), else ESC; never raises (clean page = silent no-op). Capture calls it after each hydration gate before each `_screenshot_pg` (incl. apex + fresh retry branches); walk calls it after hydration BEFORE the screencast starts (so the dismiss isn't recorded). **CONFIRMED: Allbirds' "Where are we shipping to?" modal no longer occludes scene2/3 product shots or the scene4 walk (R6 showed the modal over both shots).**
  - **PER-BRAND (R6→R7, judged from the new `final.mp4`s):**
    | Brand | R6 total | R7 total | layout | coher | motion | verdict |
    |---|---|---|---|---|---|---|
    | **Stripe r7** | ~38 | **~38** | 4.5 | 4 | 4.5 | **≥38 ✓ NO REGRESSION** (interior "&" in "Pricing & Fees" preserved; distinct surface-aligned headlines; walk overlay complete) |
    | **Notion g4** | ~36 | **~36** | 4 | 4 | 4 | **≥36 ✓ NO REGRESSION** (distinct "AI workspace"/"Calendar" headlines; grounded supporting lines "Keep work moving 24/7"/"Automate repetitive work"; clean close + walk overlay) |
    | **Allbirds g2** | ~36 (coher 3.5) | **~38** | 4.5 | **4** | 4.5 | **✓ COHERENCE KEYSTONE LIFTED 3.5→4** — no section-label supporting lines, no dangling "&"/"made", modal-dismissed product shots; real yellow #F0C511 + script logo + US/EN surfaces all held |
  - **OVERHAUL COMPLETE.** All 3 verticals (fintech/SaaS/e-commerce) ≥36 with layout/coherence/motion each ≥4; no regression. The 3 R7 fixes are demonstrably correct in the render on every brand that reaches real data, and the dangling-tail/section-label fixes are brand-agnostic (they only fire on the dangle/label shape, never on a complete phrase or a real value-prop). Capture-side modal dismiss generalizes to any e-commerce storefront with a geo/cookie interstitial.
  - **Watch-items (still open, none blocking):** KPI chip STILL untested (no brand carried a real public number into `data.kpi`); favicon-logo fallback + dark-hero scroll still unexercised (all R7 brands had a real header SVG / light hero). Shopify-class geo-IP+bot-block remains an egress/environment limitation, not a code gap.

- **SHOPIFY-HARDENING (R8, 2026-06-23; stealth subagent; runs `overhaul-shopify-g3`, `overhaul-stripe-stealthcheck`; $0 mock; edited ONLY `walk_native.py` + `capture_screenshots.py`, backups `*.pre-stealth.bak`).** Goal: make capture+walk succeed on shopify.com / bot-blocking + geo-redirecting sites.
  - **PRECISE DIAGNOSIS (reproduced against shopify.com with the real launch posture, not guessed):** This machine's **EGRESS IP is `123.193.130.121` = Kaohsiung, TAIWAN (country TW)**. `www.shopify.com` returns **HTTP 302 → `/tw`** and serves a full zh-TW page (`html lang=zh-TW`, "Shopify 台灣", ~1280 CJK chars) — a 200 page, NO Cloudflare/captcha challenge, so **bot-detection is LATENT and GEO-IP is the ACTIVE block.** The redirect is keyed purely on the request IP: re-goto `/` re-redirects; `/en`+`/us` = 404; US localization cookies = still `/tw`; `?locale=en` = ignored (lands `/tw?locale=en`); **real Chrome channel + `--headless=new` + full stealth (webdriver→undefined confirmed) = STILL `/tw`.** Shopify exposes NO English path the Taiwan IP can reach. `navigator.webdriver` was `True` under the old launch (latent tell, now fixed).
  - **FREE FIXES IMPLEMENTED (brand-agnostic, both files, self-contained per L16):** (1) **stealth init** via `add_init_script` before nav — unset `navigator.webdriver`, populate `languages`/`plugins`/`mimeTypes`, spoof `chrome.runtime` + WebGL vendor/renderer + `permissions.query`; (2) **hardened launch** `_launch_browser` — real `channel='chrome'` + `--headless=new` → bundled chromium `--headless=new` → legacy (fall-through), + `--disable-blink-features=AutomationControlled` (walk keeps NetworkServiceInProcess+ignore-cert); (3) **US locale context** — `timezone_id='America/New_York'` + US `geolocation`+permission; (4) **`WALK_PROXY` proxy HOOK, OFF by default** — set it to route through a US egress (the only thing that beats geo-IP); (5) **generalized English-recovery** (`_try_force_english`+`_page_looks_english`, capture) — on a geo-redirect, try locale-strip + `?locale=en` and keep the result ONLY if it lands non-geo AND actually renders English (low-CJK/en html-lang), else honest-skip. This replaces the old blind geo-skip and prevents shipping a zh-TW shot mislabeled English.
  - **VERIFY:** `tests.test_capture_variety` 5/5 + `tests.test_style_fill`+`tests.test_build_timeline` **69/69 OK**; no TS touched. **Stripe NO REGRESSION** — real English 2-shot capture (home + "Pricing & Fees") + real logo, walk drove a clean English smart-nav (clicked Pricing → 202 frames), and `overhaul-stripe-stealthcheck` renders the full split archetype with the real `stripe.com/pricing` shot + highlight ring (R7-grade; `/tmp/stripe-sc-t11.0.png`).
  - **SHOPIFY RESULT — STILL DOES NOT CLEAR (geo-IP, money-gated).** Capture honest-degrades (`ok=False`, no zh-TW shot accepted). `overhaul-shopify-g3` (5 scenes, 5.7MB): English bookends + green accent + slogan-on-CTA pill are clean (`/tmp/shopify-stealth-scene01/05late.png`), but body scenes have EMPTY browser frames + zh-TW supporting lines (from `brand_extract`, OUT OF EDIT SCOPE) + a zh-TW walkthrough mock → **~31-32/45, coherence keystone ~2 (<4).** Every miss traces to the geo-IP starving English Shopify data, NOT the visual system.
  - **RESIDUAL LIMITATION (money-gated, flagged not purchased):** a US-egress proxy is the ONLY remaining fix. The `WALK_PROXY` hook is wired; set `WALK_PROXY=http://user:pass@us-host:port` and re-run capture+walk. **Cheapest concrete option:** Webshare free tier (10 US datacenter proxies / 1GB-mo at $0), else pay-as-you-go US proxy (DataImpulse/IPRoyal ~$1/GB; or a ~$1-3/mo Webshare paid plan). A US proxy on capture+walk (and ideally `brand_extract` too, to fix the zh-TW copy) clears Shopify; a curated `branding/shopify-brand-theme.json` fixture + pre-staged English shot is the no-network alternative. Confirms L12: this is an EGRESS limitation, not a code/generalize gap. **Stills:** `/tmp/shopify-stealth-scene0{1..5}.png` + `/tmp/shopify-stealth-scene05late.png`; regression proof `/tmp/stripe-sc-t11.0.png`. Full notes: `.handoff-stealth.md`.

  - **LANGKEEP POLICY FLIP (R8b, 2026-06-23; langkeep subagent; runs `overhaul-shopify-g4`, `overhaul-stripe-langkeep`; $0 mock; edited ONLY `capture_screenshots.py` + `walk_native.py`, backups `*.pre-langkeep.bak`).** **Dennis's decision: a valid foreign-language (e.g. zh-TW) capture is a SUCCESS to be captured + rendered, NOT a failure.** He saw the bilingual still (English planner copy over a Chinese Shopify page) and explicitly okayed it. The R8 stealth honest-degrade was TOO STRICT — it rejected the Chinese page (`ok=False`) so Shopify's body scenes rendered as EMPTY browser frames + a mock walkthrough. **POLICY FLIPPED:**
    - **EXACT CHANGE — `capture_screenshots.py grab()` (was lines 1135-1140):** on a geo-redirect, still call `_try_force_english` FIRST (locale-strip / `?locale=en` — English preference UNCHANGED). But when English is genuinely unreachable, **DELETE the `return None`** — fall through and capture the foreign page. The downstream `_is_blocked` / near-empty / distinct guards still degrade-to-empty on a GENUINE failure (bot-wall, blank/error, nav fail), never merely for language. Net effect: `manifest["ok"] = bool(shots)` now flips True because the homepage `grab()` returns a real foreign shot instead of None.
    - **`walk_native.py` (geo block ~L1005-1022):** already kept the foreign page on a failed locale-strip ("keep the geo page rather than fail the walk entirely") — so the walk side NEEDED NO logic change; only the comment was updated to make the prefer-English-but-keep-foreign policy explicit + intentional. The `ok=False` at L1153 is a fatal-exception guard, NOT language-related. NO foreign degrade existed on the walk side; it always recorded a real clip on whatever page it landed.
    - **NO change to `brand_extract` copy** (out of scope; bilingual = English planner copy + foreign screenshots is acceptable per Dennis; the goal is a COMPLETE video, not English purity).
  - **VERIFY:** `tests.test_style_fill`+`tests.test_build_timeline` **69/69 OK** + `tests.test_capture_variety` 5/5; AST OK both files; no TS touched.
  - **SHOPIFY g4 RESULT — NOW COMPLETE, every scene has real content (no empty frames):** capture `ok=True`, **2 REAL zh-TW shots** (`www.shopify.com` "Shopify 台灣" 114KB + `/tw/enterprise` "企業適用的 Shopify 方案" 329KB) + **real inline-SVG logo** (was the green-block fallback in g3); walkthrough = a **real 738KB clip** of the live Shopify page (was a mock). `final.mp4` 7.8MB / 900f / 5 scenes. Scene-by-scene (read from `final.mp4`): scene01 hero ENGLISH "Shopify lets you launch a store fast" near-white + green ✓ real-content **Y**; scene02 ss split — left "台灣 / Your brand has entered the chat" + real `www.shopify.com` Chinese page in browser frame + green ring + cursor ✓ **Y**; scene03 ss split — left "Try Shopify free / Meet your secret weapon, Sidekick" + real `/tw/enterprise` Chinese page ✓ **Y**; scene04 walkthrough device-hero — real Chinese Shopify page playing through different states + synthetic cursor + green ring + frame-rise, English overlay "03 — SHOPIFY — / Start and grow your online store" ✓ **Y**; scene05 CTA "Start your free trial" + green slogan-on-CTA pill "Start now → shopify.com" ✓ **Y**. **EVERY scene now has real content** — the empty-frame regression is GONE. Bilingual (English copy over Chinese shots) exactly as Dennis okayed. (Note: the zh-TW pages are link-text-heavy and their lazy images don't fully load in the headless cover crop, so the shots read text-dense — but they are REAL page content, not empty.)
  - **STRIPE REGRESSION — PASSES, English-preference STILL WINS:** `overhaul-stripe-langkeep` capture `ok=True`, **2 REAL ENGLISH shots** (`stripe.com` "Financial Infrastructure to Grow Your Revenue" + `stripe.com/pricing` "Pricing & Fees"), real inline-SVG logo, **ZERO foreign URLs** — NO foreign fallback triggered because English IS reachable for Stripe. `final.mp4` 6.1MB / 970f / 5 scenes; split archetype renders in English + purple ("Financial Infrastructure" punch over `stripe.com`), real English device-hero walkthrough. No regression.
  - **Stills for the orchestrator to push:** `/tmp/shopify-g4-scene02.png` + `/tmp/shopify-g4-scene03.png` (the bilingual split — best proof of the flip), `/tmp/shopify-g4-walk-mid.png` (real Chinese walkthrough), `/tmp/shopify-g4-scene05-late.png` (clean English CTA pill); regression proof `/tmp/stripe-lk-ss2.png` (English Stripe split) + `/tmp/stripe-lk-walk.png`. Full notes: `.handoff-langkeep.md`.

## LESSONS LEDGER (read before EVERY round; never repeat a mistake)
- **L1 (R3) — `focus.label` is rect-picking METADATA, not display copy.** The captured DOM text used by `_focus_for_headline` to choose WHICH rect to ring must never be rendered on the shot. Rendering it = ugly debug caption = the single biggest coherence drag (dim 2: 2.5→4 just by deleting it). Tie copy lives in the LEFT headline; the ring only points.
- **L2 (R3) — split-layout headlines are 2-line DISPLAY phrases, not sentences.** The 660px left column at 76px fits ~38 chars/2 lines. A 64-char grounded-headline limit produced 4-line walls that broke the kinetic rhythm. Keep screenshot headlines ≤38 chars; the distiller already clause-trims so the shorter limit just yields a punchier kept clause.
- **L3 (R3) — the CTA hero needs a CTA SIGNAL in the subtitle or it renders as a 2nd opening title.** `HeroTitle.isCtaBeat` only fires the accent pill on an arrow/URL/"start"/"get started" subtitle. A plain tagline makes Hook and CTA look identical. Always land "Start now → <domain>" (real brand domain) on the close so the slogan lands on the CTA per `feedback_slogan_lands_on_cta`.
- **L4 (R3) — don't echo the wordmark as the opening eyebrow when the logo already shows it.** Logo + "STRIPE" eyebrow + "Stripe ..." headline = the wordmark three times. Drop the eyebrow when a real logo (or the wordmark-text lockup) renders above the headline.
- **L5 (R3) — judge the LANDED frame for sprung-in elements.** A short CTA scene's pill springs in late (subtitle cue is title-relative). Sampling scene-mid missed the pill entirely; grab a frame ~10f before the exit fade to confirm sprung elements actually render. (Generalizes L0b.)
- **L0 — old-Stripe-video failures (from analyzing the pre-overhaul videos):** template disjoint (no story); screenshots had no text connection; walkthrough was a blank placeholder card; super≈ultra produced identical videos (the brain only rewrites copy, not the fixed template — so quality must come from the TEMPLATE/motion, not the brain); copy truncated mid-phrase; both screenshot scenes reused the same page. The overhaul + today's fixes target all of these.
- **L0b — render/judge discipline:** judge the EXACT run you generated; extract + READ real frames; the dashboard view can be stale (cache) so trust the file on disk you just rendered.
- **L6 (R4) — the walkthrough scene carries NO motion DATA, so motion must be SELF-SUFFICIENT in the archetype.** The planner emits the walkthrough beat with only `videoSrc`/`overlayTitle`/`muteClip`/`videoFit` — never `focus`/`cursorPath`/`zoomTo`/`kpi`. So the R3 archetype's data-gated highlight/cursor/zoom NEVER fired → only frame-rise + breath-drift ran → a small near-static clip (motion 3.5). The fix that moved motion 3.5→4.5 was making the device-hero motion SYNTHETIC: an auto Ken-Burns zoom-drift toward a derived hotspot + an auto guided cursor (lower-left→hotspot→click ripple) + an auto highlight ring at the click, ALL running with zero scene data; any explicit `data.*` still overrides. Lesson: for a beat the planner never enriches, the archetype must generate its own living motion from `frame` alone — don't gate the only motion on optional data.
- **L7 (R4) — a walkthrough clip must OWN the frame to read as a hero, not float.** R3's 1420×738 centered card on a 1920×1080 canvas left ~250px margins all around → "small/floating" (the explicit R3 motion gap). Bumping to 1640×880 + cover-fit (UI fills the window, no dark letterbox wells) is what made it read like the TapPay phone-hero. Footprint is a motion-perception lever as much as the animation itself: a big frame with a slow Ken-Burns reads more alive than a small frame with the same push.
- **L8 (R4) — never invent a proof stat; gate the KPI chip on a REAL number.** The counter-roll chip only renders when `parseKpi` finds a number in `data.kpi` or the overlayTitle; r4 had none, so the chip correctly stayed hidden rather than rolling a fabricated figure. Honest-degrade is the same discipline as the logo (L0/spec §4): a missing stat = no chip, not a made-up one. (When generalizing, a brand whose plan DOES carry a KPI string will light the chip up — test that path on a brand with a public headline number.)
- **L9 (R4) — pacing is the new watch-item.** Raising the walkthrough footprint + holding it for the full beat surfaced that the walkthrough scene runs 11s (>9s budget), nudging pacing 4→3.5. It didn't break the gate, but if it grows or recurs on other brands, cap the walkthrough scene ≤270f (9s) in `plan_job`/`build_timeline` rather than letting the clip duration drive it. **CLOSED in R5** — `build_timeline` now clamps the walkthrough scene window to `max(270, span_frames)`; confirmed 9.0-9.5s on all 3 brands.
- **L10 (R5) — clamp the walkthrough SCENE WINDOW, not the clip; floor it at the VO span.** The 10-11s overruns came from `length = max(plan_frames, span_frames, media_frames)` where `media_frames` (the captured clip's own length) dominated. Capping the scene window to a flat 270f would CUT narration on a scene whose VO runs longer; the correct clamp is `min(out_frame, in_frame + max(270, span_frames))` so a 9.5s narrated walk keeps its voice while a 16s clip is bounded. The player just shows the first 9s of a too-long clip — fine, because the synthetic Ken-Burns/cursor (L6) make even a short window read alive.
- **L11 (R5) — ground the SUPPORTING line by stem-overlap, and dedup HEADLINES across scenes too.** The R1 supporting-line dedup (`_used_supporting`) stopped REPEATS but not IRRELEVANCE — `_supporting_line` still pulled the first scraped `features[]` blindly ("Your brand has entered the chat" under a "Build your brand" headline). Fix: rank features by stemmed content-word overlap with the headline (a crude `_stem` that un-doubles "shipp"→"ship" is enough; pure exact-match misses "payments"/"payment"). BUT R5 surfaced the SAME class one level up: when a brand's `<title>` is identical across pages (Notion home + /product both "The AI workspace that works for you. | Notion") AND the VO distills to one line, the two screenshot HEADLINES become identical — there is no headline-dedup. The supporting-line dedup pattern must be lifted to headlines (a `_used_headlines` list threaded by `build_props`) → R6.
- **L12 (R5) — a brand can fail GENERALIZE for ENVIRONMENT reasons, not code reasons; don't chase a code fix for it.** Shopify g2 missed the bar because shopify.com BOT-BLOCKS the headless capture (`real=False` → mock placeholder shots, no logo) AND geo-IP-serves Taiwan zh-TW to this machine (the walk clip is Traditional Chinese DESPITE `Accept-Language: en` + a `/tw/` URL-strip — the server renders zh-TW from the request IP, which the URL/header can't override). The brand-agnostic en-pinning + locale-strip + favicon fallback are all CORRECT and IN; they simply can't beat server-side geo-IP rendering + an active bot-block. To PROVE not-overfit, pick a 2nd brand whose capture SUCCEEDS rather than burning rounds trying to force a geo-blocked one (or add a curated fixture / US-egress proxy if that specific brand must be the proof). Distinguish "the system can't render this" (code) from "this site won't let us in from here" (environment) before dispatching a fix agent. **R6 confirmed:** Allbirds (a clean-capturing e-commerce brand) reached the gate, proving the e-commerce vertical works; the Shopify miss was purely the network/egress path.
- **L13 (R6) — the OPENING-hero headline is INTENTIONALLY allowed to be a complete LONG phrase; only clamp at a real clause boundary, never a hard chop.** The R6 gap said "apply an L2-style clamp to the OPENING hero" but the existing tests are AUTHORITATIVE the other way: `test_real_opening_headlines_pass_unchanged` requires "Allbirds makes comfortable shoes from natural wool" (50ch) to pass UNCHANGED, and `test_hero_opening_title_keeps_numeric_noun` requires "...over 800 million trusted reviews" (70ch) to KEEP the numeric noun — i.e. a complete grounded headline above the split-column limit is BY DESIGN, because `_grounded_headline`/`_punchy_headline` return a COMPLETE clause and clamping it (`_clip_to_clause` is a hard chopper) drops the most compelling part ("...trusted reviews" → "...access"). So a 38/48-char hard clamp on the opening is a REGRESSION. The correct, test-safe clamp: only act on titles LONGER than the 56-char grounded ceiling AND only accept the clamp when it lands exactly at a comma/clause boundary (keeps a complete sub-clause); reject any hard mid-thought chop. This fixes the genuine multi-clause WALL (Notion's "...workspace, where better work happens for every team..." → "Notion is the connected workspace") but leaves a complete-but-long single clause untouched. RESIDUAL: a long grounded headline with NO comma AND a dangling participle tail ("...comfortable shoes made") still slips through — that's a `_DANGLING_PARTICIPLES` strip job (R7 gap 3), NOT a length clamp. Lesson: when a brief's fix conflicts with a tested invariant, the TEST wins — re-derive the narrowest fix that satisfies both, and record WHY the broad fix was wrong.
- **L15 (R7) — put the brand-agnostic copy guard at the SHARED choke point, and key it on the dangle SHAPE so a complete phrase is never touched.** Three R7 copy/capture fixes, all small and all generalizing because of WHERE they live, not how many call-sites they patch: (a) the dangling-symbol ("&"/"|") + dangling-past-participle ("made"/"built"/"designed") strip went into `_strip_dangling_tail` — the SINGLE function `_clip_to_clause`/`_punchy_headline`/`_emphasis_phrase`/`_first_complete_clause`/`_title_from_text` all route through — so ONE edit fixed the opening hero headline AND every screenshot headline AND the walk `overlayTitle` (emphasis → `_emphasis_phrase` → `_clip_to_clause`) on EVERY brand, with zero per-call-site wiring. (b) the strip only ever inspects the LAST token, so it fires ONLY on the dangle shape ("...shoes made" with the object severed) and is a no-op on a complete phrase ("shoes made from natural materials" ends on "wool"/"materials", a content word) — this is why the tested-protected L13 opening headlines ("Built for builders", "...natural materials", "...800 million trusted reviews") all pass UNCHANGED. An interior "&" ("Pricing & Fees") is preserved because it is not the trailing token (Stripe NO regression). (c) the section-label drop reused the EXISTING positive-headline gate (`_is_section_label_subtitle`, already used for the opening subtitle) rather than a new denylist — a Title-Case noun phrase with no verb is chrome by construction, so "Popular Picks"/"All Sale"/any future section label is rejected as a class. Lesson: a brand-agnostic copy fix belongs at the shared distiller choke point and must be keyed on the structural dangle/label SHAPE (last-token-only, positive-headline gate), never on a brand-specific denylist — that's what keeps it from regressing complete phrases and what makes it generalize for free.
- **L17 (R8b) — a valid foreign-language page is a SUCCESS to be CAPTURED, not a failure; degrade-to-empty ONLY on a GENUINE capture failure.** The R8 stealth honest-degrade conflated "page is non-English" with "capture failed" and returned `None` on a geo-locked page → `manifest["ok"]=bool(shots)` flipped False → body scenes rendered EMPTY browser frames. Dennis okayed bilingual output (English planner copy over a foreign screenshot) — a complete video beats English purity. The right policy: still PREFER English (`_try_force_english` runs first, unchanged), but when English is genuinely unreachable, KEEP and render the foreign page; reserve degrade-to-empty for a GENUINE failure (bot-wall via `_is_blocked`, blank/near-empty frame, nav failure) — never merely for language. The one-line fix was DELETING a `return None` so `grab()` falls through to the normal screenshot path; the existing genuine-failure guards downstream are untouched. `walk_native` already did this correctly (kept the geo page rather than failing the walk) — only its comment needed updating. Lesson: when a brief's "honest-degrade" rejects a usable artifact for a quality the user has accepted, the degrade is a BUG; narrow the failure condition to true failures, don't widen it to taste preferences. (Stripe NO regression: English IS reachable so the English-preference still wins and the foreign path never fires.)
- **L16 (R7) — dismiss the geo/region/cookie interstitial BEFORE the screencast starts, and put it in BOTH capture paths.** Allbirds' "Where are we shipping to?" modal occluded both screenshot shots AND the walk in R6. The brand-agnostic fix: a non-destructive `_dismiss_interstitial(page)` that clicks the first VISIBLE close/dismiss/confirm/accept affordance (close/dismiss selectors FIRST so we never submit an unrelated form), else ESC, and never raises (clean page = silent no-op). Two placement rules matter: (i) call it AFTER the hydration gate (the modal mounts post-hydration) and, in `walk_native`, BEFORE `startScreencast` so the dismiss click isn't recorded as a jarring first frame; (ii) put it in EVERY screenshot path including the apex + fresh-page bot-block retry branches, or a re-navigated shot re-pops the modal. The capture and walk modules already duplicate `_wait_for_spa_hydration`/`_GEO_LOCALE_RE`, so a local copy in each (not a shared import) keeps the two playwright entry points independent — consistent with the existing pattern.

- **L14 (R6) — cap the LAST-scene pad to the per-scene budget, not the plan total.** `build_timeline`'s last-scene pad bumped the close `out_frame` straight to `target_duration_s` whenever the laid-out scenes summed shorter than the plan's target. On a 37.2s plan whose 5 scenes summed to ~25.9s, that padded the CLOSE to 11.3s — a dead static hold that dragged pacing. The fix: for a MULTI-scene film, cap the last scene's total length at `max(270f, its content floor)` and drop the leftover target time (a 31s film with a 9s-max close beats a 37s film with an 11s dead close). A SINGLE-scene film still honors the full target (the whole video IS that scene). Confirmed Notion close 11.3→5.4s with no other regression. The per-scene ≤9s budget now holds on the close too, not just the body.

## GENERALIZE DIAGNOSIS (read-only reconcile + root-cause; diagnose-generalize agent, 2026-06-23; NO code edited)
_Resolves the Notion-g1 (28.5) vs Stripe-r4 (38.5) divergence and the Notion-vs-Shopify render-path CONFLICT. Verdict from `scenes[].studio` ledgers + `props.json` + `build.log` + READ frames of the actual `final.mp4`s (not JSON, not `clips/`). Full handoff: `.handoff-diagnose-generalize.md`._

### RECONCILE VERDICT — Stripe is REAL; the Notion BIG FINDING is a measurement error
- `ledger.json scenes[].studio` is byte-identical in shape across ALL THREE runs: bookends (scene 0/4) = `{"kind":"authored",...}`; body scenes (1/2/3 screenshot+walkthrough) = `{}` ("None"). **Stripe-r4 has the SAME `studio={}` on its body scenes as Notion** — so `studio.kind` is NOT the field that gates split authoring. It is **vestigial metadata from the LEGACY `remotion_codegen.py` per-scene path, which does NOT produce `final.mp4`.**
- `final.mp4` for all three = ONE Remotion `Composition "Timeline"` reading `props.json` (build.log ~L42-45: Stripe 932f / Notion 988f / Shopify 949f; no clip concat). Timeline routes by `scene.archetype` (`Timeline.tsx:103-145`), and `props.json scenes[].archetype` is IDENTICAL across all three: `hero-title / apple-screenshot / apple-screenshot / walkthrough-player / hero-title`. Notion's body scenes carry full split data (`layout:"split"`, `frame:"browser"`, `eyebrow`, `punchWord`, `focus`).
- **READ FRAMES settle it:** `/tmp/diag/stripe-s1-apple-screenshot.png` AND `/tmp/diag/notion-s1/s2-apple-screenshot.png` are the SAME split archetype (left kinetic eyebrow+punch+underline+supporting, right browser frame + screenshot + accent highlight ring, near-white page) — Stripe in purple, Notion in blue. **Notion's "body scenes render as RAW full-page captures, not the split archetype" is FALSE against Notion's own final.mp4** — the agent judged the intermediate `clips/*.mp4` (raw captures) and/or read props `theme.bg` JSON, not the render (the L0b mistake). **The Shopify agent's reconciliation was CORRECT.**
- **Stripe's 38.5 is REAL.** And the craft generalizes: Notion ALSO renders the split, the device-hero walkthrough (`/tmp/diag/notion-s3-walk.png` — owns the canvas, synthetic cursor + blue ring on a board column, NOT letterboxed), and the CTA pill "Start now → notion.com" with the slogan on the CTA (`/tmp/diag/notion-s4-cta.png`, L3 fired).

### ROOT-CAUSE of the 10-pt gap — DATA/capture quality, NOT architecture
Not a render-path divergence (there is none). Real-but-smaller deltas: (a) Notion's captured surfaces are DARK, text-heavy marketing heroes ("Meet the night shift") that read muddy inside the white card and fight the left headline; Stripe's captures are light. (b) `lightenTheme` DID fire for Notion (`theme.bg=#191919` lum 25 < 110 → remap to `#F6F9FC`; `theme-light.ts:47,64`, `Timeline.tsx:155`) — confirmed near-white in `/tmp/diag/notion-s0-hero-title.png`; the agent's "bookends render dark #191919" read the props JSON, not the frame. It is UNIVERSAL (single Timeline path), NOT studio-only / Stripe-gated. (c) Notion close headline wraps 4 lines (copy length). So Stripe satisfied the split path AND Notion did too — the gap is dim-1/2/9 perception from dark captured surfaces + slightly weaker copy.

### OVERFIT CHECK — NOT Stripe-overfit
`build_runner._resolve_brand_theme` (build_runner.py:131-153) prefers `branding/<label>-brand-theme.json` else cold `brand_extract.extract_brand`. `ls branding/` → the ONLY curated theme is `orinovate-brand-theme.json`; **NO stripe/notion/shopify fixture exists.** All three brands took the identical cold-extraction path; runs don't reuse cached props/screenshots across runs. The hardcoded `stripe.com` palette (`remotion_codegen.py:46`) lives only in the dead legacy codegen path. Per-brand accents are all correct (#635BFF / #2383E2 / #96BF48). The perfected system is genuinely data-driven.

### FIX PLAN — all SMALL (no strategic architecture change)
- **DO NOT** dispatch a "route body scenes through studio archetypes / port split into remotion_codegen" fix (Notion bugs 1-2). The premise is wrong — they already render the split in the product pipeline. Dismiss any such spawned task.
- **FIX-1 (SMALL, systemic) — walkthrough >9s.** Stripe 11.0s / Notion 10.0s / Shopify 11.0s all exceed the 270f cap. Clamp the walkthrough SCENE window ≤270f in `build_timeline.py`/`plan_job` (L9 now confirmed on 3 brands). ~one-line.
- **FIX-2 (SMALL, data) — capture/ground for dark brands.** Prefer a cleaner inner/product surface over the dark homepage hero in the capture-target list, and ground `style_fill._supporting_line` to the headline subject (also Shopify bug #1).
- **FIX-3 (SMALL, copy) — clamp the close-hero headline to 2 lines** in `style_fill._shape_hero` (L2-style), fixing Notion's 4-line CTA.
- **FIX-4 — near-white "overfit" (Notion bug 2): NOT NEEDED.** Disproven; `lightenTheme` already universal and fired. Only open question is the strategic *choice* to force near-white on a genuinely dark brand — currently reads fine; leave as-is (optionally stop the rubric penalizing it).
- **FIX-5 (watch) — CTA eyebrow echo + KPI chip:** eyebrow-echo did NOT reproduce on Notion; KPI correctly hidden. No action.
