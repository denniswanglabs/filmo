# Walk Native — Phase 2 handoff (smart goal-directed nav)

## Goal
Upgrade `walk_native.py` from Phase-1 scripted scroll-through to a
perceive→propose→execute step loop that NAVIGATES (clicks the right nav
link/button) to the emphasized feature, like the original walk-ultra agent.
Preserve all Phase-1 contracts. $0 on super-free (free 120B). Edit only
`walk_native.py`.

## Study done — what to PORT (from walk-ultra/explainer-agent/agent.sandbox-v26.js)
- PERCEIVE — `gatherClickables` (L822-864): page.evaluate over
  `a, button, [role=button], [role=link], [role=menuitem], [role=tab], [role=option], select`;
  filter by rect (w/h>=4, in/near viewport), visibility; dedup by tag|text|aria|x,y;
  emit {tag,text,aria,href,rect}, cap 80. `gatherPageText` (L866) = main.innerText.
  scroll metrics (scrollY/fullPageHeight/viewportHeight).
- PROPOSE — `pickNextAction` proposer prompt (L545-668): system prompt drives a
  browser to demonstrate a task; reply JSON only; actions click-by-id / scroll /
  done; KEYWORD-ANCHOR preference (prefer clickables whose text/aria/alt contains
  the goal's literal terms); WHEN-LOST → scroll first; DONE only when target
  content visible on current page. I trim canvas/drag/freedraw/keyboard/search
  branches (brand-explainer nav, not drawing). callNemotron (L173) = OpenAI-style
  chat-completions, temperature 0.2, parse JSON loose.
- EXECUTE — click branch (L2511-2586): `scrollAndMark` (L993-1020) scrolls the
  i-th clickable into view (scrollIntoView block:center) + marks it; remeasure
  rect (L1023); `page.mouse.click(centerX, centerY)`; then poll up to ~8s for
  url change OR pageText length delta >80 → urlChanged/textChanged; on urlChanged
  wait domcontentloaded + settle. Scroll branch: stepped scrollBy.
- Brain: hermes brain.py — BRAINS dict, super-free default
  (`nvidia/nemotron-3-super-120b-a12b:free`), ultra-paid
  (`nvidia/nemotron-3-ultra-550b-a55b`); OPENROUTER_URL; OPENROUTER_API_KEY.

## Contracts to PRESERVE (Phase 1)
- CDP screencast → <run_dir>/walk/frame.jpg (live) + frames/fNNNNN.jpg.
- state.json each step {url,step,action,ts} atomic. action ∈
  clicking|scrolling|navigating|done|failed.
- ffmpeg stitch; SIGALRM overall alarm; non-fatal (prints WALK_NATIVE: ok/failed,
  exit 0/1, never hangs/raises); browser closed in finally.

## Plan
1. Add brain wiring (import brain.py; WS_WALK_BRAIN env → normalize_brain).
2. `_gather_clickables(page)`, `_gather_page_text(page)`, `_scroll_metrics(page)`.
3. `_propose_next_action(...)` → POST OpenRouter chat-completions, parse loose.
4. `_execute_*` (click ref via scrollIntoView+mouse.click; scroll).
5. `_smart_nav(...)` step loop, cap MAX_STEPS=9 + wall-clock; updates state.json.
6. In run(): try _smart_nav; on ANY exception fall back to Phase-1 scroll-through
   (extracted to `_phase1_scrollthrough`). Screencast running throughout.
7. Verify on stripe.com → pricing, super-free, $0.

## STATUS: COMPLETE + VERIFIED ($0 super-free).

### What landed (walk_native.py only)
- Brain wiring: `import brain`; `_walk_brain()` reads WS_WALK_BRAIN (default
  super-free, allows ultra-paid/super-paid). `_call_nemotron` = OpenRouter
  chat-completions (matches validate_planner shape: Authorization Bearer,
  HTTP-Referer/X-Title, temp 0.2, max_tokens 2000), returns "" on any failure.
- PERCEIVE: `_gather_clickables` (ported gatherClickables selector + filters +
  dedup + 80 cap), `_gather_page_text`, `_scroll_metrics`. `_scroll_ref_into_view`
  re-runs the SAME selector so the index ref maps to the same element (port of
  scrollAndMark + remeasure).
- PROPOSE: `_PROPOSER_SYSTEM` = ported walk-ultra proposer prompt trimmed to
  click/scroll/done (dropped canvas drag/freedraw/clickAt/keyboard/search);
  KEYWORD-ANCHOR + WHEN-LOST-scroll + strict-DONE kept. `_parse_decision_loose`
  tolerates fences/prose/regex-mining; None -> scroll fallback.
- EXECUTE: `_smart_nav` loop (cap MAX_STEPS=9 + wall-clock). Click =
  scrollIntoView + remeasure + `page.mouse.click(center)` + 8s url/text-change
  poll + hydration settle. Scroll = stepped scrollBy. Writes state.json each step
  (clicking|scrolling|navigating|done|failed).
- FALLBACK: run() tries `_smart_nav`; on ANY exception (incl. no
  OPENROUTER_API_KEY) -> `_phase1_scrollthrough` (extracted verbatim from the old
  inline plan). Screencast/stitch/SIGALRM/finally-teardown all preserved.
- Alarm budget raised to min(190, duration+110) so OUR SIGALRM fires before the
  parent's 200s subprocess timeout.

### Verification (super-free, $0)
- stripe.com / "the pricing page": step 0 CLICKED id=5 "Pricing" -> step 1 DONE
  (URL became /pricing, title "Pricing & Fees"). 2 Nemotron calls. ended on
  https://stripe.com/pricing. 113 frames, valid 1280x800 h264 mp4. Visual:
  first frame = homepage, last frame = "Pricing built for businesses of all sizes".
  2nd run reached /pricing#standard-pricing. CLICKING confirmed, not scroll-only.
- bad URL: fails gracefully in 0.47s, exit 1, state action="failed", no hang.
- no key: degrades to Phase-1 scroll-through, 523 frames, exit 0 (ended /pricing
  via the Phase-1 sub-page hint).
- COST: $0 — super-free is `nvidia/nemotron-3-super-120b-a12b:free` (price 0/0).
  ~2 calls per successful nav. ultra-paid (550B) available via WS_WALK_BRAIN for
  the real deliverable.
