# P1 — apple-screenshot archetype + screenshot capture: HANDOFF (code done, pre-verify)

_2026-06-22. Code complete; verification render next._

## What landed (all files compile-clean by construction; render verify pending)

### Capture (Python)
- **`capture_screenshots.py`** (NEW, repo root) — Playwright headless capture.
  - `capture_url(url, out_dir, max_shots=2)` is the in-process API. If Playwright
    isn't importable in the current interpreter (host python3.14 has none), it
    RE-EXECS into `.venv-capture/bin/python` (a dedicated venv I created with
    Playwright 1.60 + chromium) and reads the manifest back. Verified working from
    BOTH the capture venv directly AND host python3.14 via re-exec.
  - Grabs homepage (above-the-fold, 1600x1000 @2x retina = 3200x2000 PNG) +
    auto-discovers up to `max_shots-1` same-origin nav routes (deterministic: first
    N content-y links, skips login/legal/blog/etc).
  - Writes `shot-NN.png` + `manifest.json` to out_dir.
  - CLI: `python capture_screenshots.py --url <U> --out-dir <D> --max 2`.
- **`.venv-capture/`** (NEW, gitignored) — py3.11 venv with playwright+chromium.
  Added to `.gitignore`.

### adapters.py
- **`generate_screenshots(url, out_dir, mode, max_shots=2)`** (~after generate_walkthrough).
  ALWAYS real-captures (mode-independent). On capture error → ONE synth card PNG
  (`_synth_card_png`, ffmpeg solid color) so the scene is never blank; returns a
  not-ok manifest with the error.

### build_runner.py
- **`_capture_screenshots_for_run(url, run_dir)`** (before `_run_vo_engine`) —
  idempotent (skips if manifest.json exists), writes to `runs/<id>/screenshots/`.
- Called ONCE in `_run_vo_engine` right after brand resolve, best-effort
  (wrapped in try/except — a capture failure never fails the paid render).

### studio/src/timeline/types.ts
- Added `"apple-screenshot"` to the `Archetype` union.
- Added to `SceneData`: `imageSrc?: string; frame?: "browser"|"none"; caption?: string;`.

### studio/src/timeline/Timeline.tsx
- Imported `AppleScreenshot`.
- Renamed `resolveAudio` -> `resolveAsset` (same staticFile-or-passthrough logic),
  MOVED it above `SceneBody` so the dispatch can use it. Both <Audio> calls now use
  `resolveAsset`.
- Added `apple-screenshot` dispatch branch passing `resolveSrc={resolveAsset}`.

### studio/src/timeline/archetypes/AppleScreenshot.tsx (NEW)
- `<Img src={resolveSrc(data.imageSrc)}/>` inside a brand-tinted mac browser card
  (traffic-light dots tinted from theme.navy/accent/navyBright, address pill shows
  the caption host+path). Apple shrink/zoom arrival: scale 1.06->1.00 cubic
  ease-out (NO overshoot, per feedback_apple_screenshot_animation) + top-down clip
  mask reveal + breath drift + exit fade. `frame:"none"` drops the chrome.
  Never-blank floor: caption/wordmark placeholder if no image.

### style_fill.py
- Generalized `_stage_one_audio` -> **`_stage_one_asset`** (audio OR image; also
  searches `out_dir/screenshots/`). Back-compat alias `_stage_one_audio` kept.
- `_stage_audio` now ALSO stages `scene.data.imageSrc` -> `shot-<run>-<id>.<ext>`
  in public/, drop-if-missing (like audio).

## Verify done so far
- Capture: stripe.com -> shot-01.png (1.18MB, 3200x2000) + shot-02.png (/pricing,
  862KB). orinovate.com via host re-exec -> home + /materials, both 3200x2000.

## VERIFY — DONE (PASS)
- Staged orinovate shot via the REAL style_fill stager: imageSrc 'shot-01.png' ->
  'shot-p1verify-shot.png', PNG copied into studio/public/ (confirms _stage_audio
  image branch + screenshots/ search path).
- Rendered Timeline stills via `remotion still` (1920x1080):
  - studio/out/p1verify/settled.png (frame 70): real orinovate.com homepage inside
    a clean white brand-tinted browser card — navy/blue traffic-light dots, address
    pill reading "www.orinovate.com", soft drop shadow on a brand-mesh bg, muted
    mono caption "https://www.orinovate.com" centered below. Site (CNC arm hero,
    "數位製造專家", nav, CTAs) fully legible. On-brand (matches fixture Orinovate
    navy #1a3a5c / accent #2563eb).
  - studio/out/p1verify/arrival.png (frame 16): mid shrink/zoom — card slightly
    larger, top-down clip mask still partly closed (chrome bar/address pill clipped
    at top), caption not yet settled. Confirms the Apple cubic-ease arrival.
- 10/10 existing tests.test_style_fill pass (staging rename non-regressive).
- Cleaned the verify PNG out of studio/public/ afterward.
- DO NOT COMMIT (per brief; repo is git but commits are the orchestrator's call).
