# Handoff — dashboard clarity pass (COMPLETE)

Edited ONLY: dashboard/{index.html, app.js, styles.css, serve.py}.
Never read/modified runs/ content. Active build runs/build-orinovate-edf804 left untouched
(confirmed by ls -ld mtime unchanged; no reads of its contents).

================================================================
PART A — RUN DELETE
================================================================
serve.py (NEEDS A RESTART to activate — Dennis triggers, NOT me):
- safe_run_dir(run_id): hard sanitizer. Allows ONLY [A-Za-z0-9._-] (fullmatch);
  rejects empty / '.' / '..' / any id containing '..'; then realpath() and REQUIRES
  os.path.dirname(realpath(candidate)) == realpath(RUNS_DIR) so the resolved target
  can't escape runs/. Refuses the runs dir itself.
- rebuild_runs_index(): regenerates runs/index.json from surviving ledgers (shape per
  LEDGER.md), newest-first.
- POST /api/delete {id}: sanitize -> 400 on any bad id BEFORE any fs touch ->
  shutil.rmtree(confined target) -> rebuild index -> returns {deleted, runs}.
  404 if folder missing, 500 on rmtree error.
- imported shutil; added RUNS_DIR, _RUN_ID_RE.

Frontend (LIVE now — static files served fresh):
- Each run is a .run-wrap holding the selectable .run-card + an overlaid .rc-del trash
  button (ghost, appears on hover/focus, warms red). NOT a button-in-button.
- Click trash -> askDelete() swaps in an inline .rc-confirm bar
  "Delete this build? [Cancel] [Delete]" — TWO-STEP, no one-click delete.
- Confirm -> confirmDelete() POSTs /api/delete, uses the returned run list directly
  (no refetch race), clears the stage to idle if the deleted run was selected.

================================================================
PART B — DECLUTTER (one clear primary focus per state; dense -> on-demand)
================================================================
Unifying device: disclosure() = native <details> (keyboard-accessible, no-JS toggle,
deterministic in headless). One coherent collapse system across live + delivered.
Each disclosure has an index badge + a one-line hint so a COLLAPSED section still says
what's inside. wireProofDisclosures() lazily starts the Studio code-typing only when
its section is first opened (delivered runs land on the video, not a typing animation).

PER STATE — what's PRIMARY vs what COLLAPSED:

IDLE / landing:
  PRIMARY: new-build input (rail) + clean scannable BUILDS list (delete control on each).
  Empty stage = a single hero (product one-liner + "paste a URL / open a build"),
  replacing the bare "Select a job from the ledger." Rail title "JOB LEDGER"->"BUILDS",
  "takes"->"builds". Raw index-load error softened to "Couldn't reach the console…".

BUILDING (producing/etc):
  PRIMARY: live header + phase track; storyboard scene-card progress with an
  "N of M scenes" readout in the section label; compact P&L ticker (livePnl).
  COLLAPSED: "The script — what the agent wrote…" and "Agent activity — live browser &
  action feed" (the verbose log) — both <details> closed by default.

AWAITING_PAYMENT:
  PRIMARY: the pay gate (the decision) + a compact economics row livePnl(l,true):
  "You pay / Agent budget / Auto-declines over" — value + guardrail visible BEFORE paying
  (was hidden until after payment).
  COLLAPSED: "The script…" and "The storyboard — queued, awaiting payment".

DELIVERED:
  PRIMARY: the video hero (deliveredHero — already built; Download MP4 + Copy link).
  COLLAPSED proof-of-craft <details> (all closed): 01 Profit & loss, 02 Budget gate
  timeline, 03 Studio code, 04 Source media, 05 Money layer & run log, 06 Run metadata
  (the old top slate is folded in here so it stops competing with the hero).
  Each shows a one-line hint while collapsed (price/margin, scene count, etc).

FAILED:
  liveFailed unchanged — single clear FAILED pill + message (one focus). Still renders.

Also (cheap, while decluttering): :focus-visible amber rings on all buttons/inputs;
rail shows a status tag (running/failed/awaiting_payment) instead of a bogus "—%" margin
for non-delivered runs; responsive media query collapses the new grids too.

================================================================
VERIFICATION ($0) — ALL PASS
================================================================
- node --check dashboard/app.js  -> OK
- python3 ast.parse serve.py      -> OK
- Sanitizer unit test (isolated, throwaway sandbox, NO real run touched):
  22/22 — rejects ../, .., '.', ../../etc/passwd, /etc, /etc/passwd,
  build/../../../etc, a/b, a\b, space, empty, whitespace, null byte, foo/.., ~,
  foo$bar, None, int; accepts valid ids (confined inside sandbox).
- Delete + index-regen flow on sandbox: deletes exactly one folder, regenerates index.
- Headless per-state render checks (DOM shim, against demo-1/3/4 + synthesized
  running/awaiting_payment/failed): 36/36 substantive — each state renders, primary
  focus present, dense panels collapsed-by-default (0 open <details>).
  (One assertion string-matched lowercase against an uppercased label; the N-of-M
  readout IS present: "STORYBOARD · 2 OF 5 SCENES" — confirmed separately.)
- curl http://localhost:3030 confirms served static files carry the edits live.

SERVE.PY RESTART NOTE:
  The running serve.py is the OLD process — /api/delete returns 404 until restart
  (verified). Static HTML/CSS/JS are already live (no-store). RESTART serve.py to
  activate the delete endpoint. (Not done by me, per brief.)

WHAT TO LOOK AT IN SAFARI (http://localhost:3030):
  1. Idle: clean hero + BUILDS list; hover a run -> trash appears; click -> confirm bar.
     (Actual delete needs the serve.py restart.)
  2. A delivered run (e.g. build-stripe-9e7829 / demo-1-approve): video hero leads;
     everything else is collapsed proof sections — open them one at a time.
  3. demo-4-vo-over: silent video ("audio off") still delivered correctly.
  4. demo-3-decline: the cut scene + decline styling preserved inside the gate section.
  5. Start a real/mock build to watch BUILDING (storyboard primary, feed/script folded)
     and AWAITING_PAYMENT (pay gate + economics row, script/storyboard folded).

LIKELY ITERATION POINTS:
  - Disclosure default-open policy: maybe auto-open P&L on the money-shot (demo-3)?
  - The delivered Run-metadata fold replaces the slate entirely on delivered runs — if
    Dennis wants the take/mode strip visible up top, re-add a slim slate above the hero.
  - "Agent activity" collapsed during build hides the live feed — if the live feed is
    the demo centerpiece, consider open-by-default while phase==producing.
  - Delete confirm is inline-per-card; a global "are you sure" modal is an alternative.
  - Copy for the pay-gate dev note (stripe login) left as-is (decluttering only).
