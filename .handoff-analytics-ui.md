# Handoff — Analytics UI: strip economics from customer view + operator Analytics tab

Date 2026-06-21. Scope: dashboard/{index.html, app.js, styles.css} ONLY.
Server :3030 orchestrator-managed — NOT restarted. Build flow, composer, quality
toggle, pay-gate, sidebar, lookbook all PRESERVED. NO EMOJI. Native SVG. STATUS: COMPLETE.

## Backups
dashboard/{index.html,app.js,styles.css}.pre-analytics-ui.bak written before any edit.

## TASK 1 — STRIP ECONOMICS FROM THE CUSTOMER VIEW — DONE (app.js)
The customer now sees ONLY: the PRICE they pay (pay-gate quote/total), build progress
(phase track + storyboard), the script, the delivered video + Download. All COGS /
margin / P&L / budget / decline / overage internals are GONE from the customer paths.
Removed (callers deleted; the functions remain DEFINED but unreferenced = harmless dead code):
- `renderLive()` awaiting_payment: removed `livePnl(l, true)` (the "You pay / Agent
  budget / Auto-declines over" budget row). `payGate(l)` (price-only quote) KEPT.
- `renderLive()` production: removed `livePnl(l)` (Price / Spent / Budget left / Margin
  ticker). Phase track + storyboard progress KEPT.
- `renderDetail()` proof array: removed the "Profit & loss" disclosure (`pl(l)` — the
  P&L receipt, NET MARGIN card, COGS/GROSS PROFIT, "budget gate auto-declined / saved
  $X overage"), the "Budget gate · timeline" disclosure (`gate(l, scenes)` — would-cost
  / declines / spent), and the "Money layer & run log" disclosure (`bottom(l)` — Stripe
  spending limit + declined-authorization note). KEPT: Studio, Source media, Run metadata.
- `slate()` (Run-metadata header): removed the MARGIN cell and the "· target margin"
  suffix on the URL line. Now TAKE/MODE/STATUS/CLIENT/OVERLAYS + goal + url only.
Dead-but-defined (no callers anywhere): `pl`, `gate`, `gateRow`, `voRow`, `declineAmt`,
`bottom`, `declineAuthNote`, `kv`, `livePnl`. Left in place to avoid breakage risk.

## TASK 2 — OPERATOR ANALYTICS TAB — DONE (index.html + app.js + styles.css)
- ENTRY POINTS: a quiet `#rail-analytics` button in the sidebar footer (above the
  conn line, coin icon, label "Analytics") AND a `?view=analytics` deep-link
  (handled in init() after the checkout-return check; loads the rail then opens it).
- DATA: `GET /api/analytics` -> `{ builds:[...], totals:{...} }` (contract in
  .handoff-analytics-backend.md). Lazy fetch in `openAnalytics()`.
- RENDER (`analyticsView` -> `analyticsCards` + `analyticsTable`):
  * 7 TOTALS metric cards: Gross profit (HERO, coral, spans 2 cols), Revenue, Cost of
    goods, Avg margin, Overage auto-saved, Builds, Declines. Numbers in mono.
  * PER-BUILD table: Build (brand+mode+run-id), Quality, Price, COGS, Margin, Gate
    (declines + "saved $X"), Status (delivered/complete/running/failed pills).
    Brand label derived from `b.brand` (company_url) via brandFromHost; unpriced builds
    show "—" for price/margin. `declines[]` keyed on `scene`/`saved_cents` per contract.
- GRACEFUL STATES: loading spinner; `analyticsUnavailable()` (404/error) with a clear
  "activates when the server restarts" message + Retry button; empty (no builds) state.
- NAV: `selectRun()` + `newBuild()` call `exitAnalytics()` so opening a build / New
  build clears the analytics active state. `state.view` tracks "analytics".

## TASK 3 — REMOVE "PRODUCT WALKTHROUGH" EXAMPLE CHIP — DONE (index.html)
Deleted the `.ex-chip` "Product walkthrough" button from `#ex-chips`. Remaining chips:
Brand explainer, Social ad, Launch film (verified 3 chips, walkthrough gone).

## VERIFICATION (server NOT restarted; live preview serverId a463af0b on :3030)
- `node --check dashboard/app.js` -> OK. CSS braces 670/670 balanced.
- `/api/analytics` returns 404 now (route inactive pre-restart) — confirmed.
- Analytics entry exists; clicking it renders the graceful 404 state ("Analytics aren’t
  available yet" + HTTP 404 + Retry), NOT a broken page.
- Stubbed the analytics fetch in-page with the contract's sample -> 7 totals cards
  render with correct values ($447.19 gross / $455.71 rev / $8.52 cogs / 64.1% margin /
  $1.48 overage / 22 builds / 8 declines) + 3 table rows (Stripe/Linear/Orinovate) with
  Gate "1 cut · saved $0.48" and "—" price/margin for the running build. (fetch restored.)
- Customer DELIVERED view (build-linear-54d446): video hero + Download present; proof
  titles = Studio / Source media / Run metadata only; ZERO banned terms
  (P&L/COGS/margin/budget/overage/Money layer) and ZERO banned classes
  (.receipt/.margin-card/.live-pnl/.pl-wrap/.auth-note). Slate cells =
  TAKE/MODE/STATUS/CLIENT/OVERLAYS (MARGIN gone); url has no "target margin".
- Customer NON-delivered view (build-luceostudio-0c2f0f): proof = Final cut / Source
  media only; zero banned terms; slate clean.
- Walkthrough chip gone (3 chips remain).
- Nav: build-open clears analytics is-on; New build returns to composer + clears it;
  ?view=analytics deep-link opens analytics with the rail populated (25 builds).
- Zero console errors across all interactions. Screenshot captured (Studio P&L view).

## Files edited (+ backups)
dashboard/index.html, dashboard/app.js, dashboard/styles.css
dashboard/{index.html,app.js,styles.css}.pre-analytics-ui.bak
NOT touched: serve.py / any backend/python (/api/analytics already exists).

COMPLETE.
