# Handoff: thin-voiceover fix (VO fullness)

## Phase 1 — DONE: edits applied
Bug: planner brain undershot VO (~8 words for 30s) because VOICEOVER RULES gave
only a MAXIMUM (3 words/sec) with no minimum, so the model wrote fragments.

Live prompt confirmed in `validate_planner.py` SYSTEM_PROMPT; `plan_job.py` imports
`validate_planner as vp` and builds the system prompt from `vp.SYSTEM_PROMPT`
(plan_job.py line 282-283). `planner-prompt.md` is the doc mirror.

Files changed (ONLY the two I'm allowed to touch):
- `validate_planner.py` — VOICEOVER RULES: replaced the single MAX-only WORD BUDGET
  bullet with a FLOOR+ceiling bullet (2.0-2.6 words/sec of duration_s, hit the floor,
  no fragments) + added a BE SPECIFIC bullet (concrete nouns, discourage buzzwords).
- `planner-prompt.md` — same two bullets, kept in sync.

Nothing else changed: schema, beats structure, structure rules, voice="Adam",
standard/premium quality blocks, temp 0.2, JSON-only contract all intact.

## Phase 2 — verification (free brain, $0)
Call: plan_job.plan_job('https://www.orinovate.com', cinematic-launch goal, 30,
quality='standard', brain='super-free'). Assert VO total ~55-75 words, full
sentences, JSON schema still passes.
Status: PASS (live free-brain run, $0).
- Before: ~8 words total for 30s. After: 67 words across 5 beats.
- Per-beat (all full sentences, no fragments):
  opening-title 8w, feature-1 15w, feature-2 15w, feature-3 17w, closing-title 12w.
- Durations sum to 30s. schema_check(quality="standard") -> SCHEMA_PROBLEMS: NONE.
- Beats are specific to Orinovate (AI design generation, virtual prototyping/stress
  tests, team collaboration, CTA to Orinovate.com) — not generic filler.
Temp verifier script was removed after the run.
