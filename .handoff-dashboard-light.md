# Handoff — Dashboard LIGHT (Hera-like) palette re-base

**Scope:** color/palette shift ONLY on `dashboard/{index.html,app.js,styles.css}`.
Dark graphite + amber → near-white (Hera `#F9F9F9`) base + refined coral accent.
Preserve ALL structure, states, declutter (`<details>` folds, run-delete + 2-step
confirm), motion polish (word-build hero, blur-rise/grow-in vocab, phase-sweep,
shimmer, N-of-M), radius tiers, reduced-motion guard. NO emoji. Declines stay RED.

Do NOT touch serve.py / adapters.py / finish_cut.py / remotion_codegen.py / runs/.

## Phase 0 — recon (DONE)
- Server live on :3030 (curl 200 on /dashboard/styles.css). node v22.23.0 present.
- `index.html`: structural, no inline colors (hero data-attrs are content only).
- `app.js`: 63KB, ZERO hardcoded palette colors — the 3 `color:` hits are all
  `var(--neg)`/`var(--neg-2)` inline styles. Fully token-driven. Likely NO app.js
  color edits needed; verify only.
- `styles.css`: token-driven via :root. Hardcoded values to convert:
  - `#1a1206` ×8 = dark text on amber buttons → route to `--on-accent` (near-white
    on coral for contrast).
  - `#000` ×7 = video/preview stages → keep dark intentionally (cinema frame, the
    Hera "one dark card" inversion) via `--video-bg`.
  - `#fff` ×4 = text on red pills/badges → stays white on red. OK.
  - code syntax `#7FB7D6`,`#8FA6FF`,`--pos` in `.tok-*` → darken for light code surface.

## States to verify render + legible in LIGHT:
idle (`.stage-empty` / `.hero-title`), building (`renderLive` production path:
`.live-head`/`.ph-track`/storyboard/`.sb-card.s-queued` shimmer), awaiting_payment
(`payGate` / `.pg-price` / `.pg-pay`), delivered (`deliveredHero` / `.dh-video`),
failed (`liveFailed` / `.live-pill.bad`).

## DONE — final light palette tokens (all in styles.css :root)
- Base: --bg #F4F5F7 (canvas) · --bg-1 #FFFFFF (cards) · --bg-2 #F7F8FA (wells/receipt)
  · --bg-3 #EEF0F4 (tracks/gutter) · --line #E4E7EC / --line-2 #D5DAE2 (hairlines).
- Ink: --ink #14171C (AAA, 16.5:1) · --ink-2 #3D4654 · --ink-3 #6B7686 · --ink-4 #98A1B0.
- ACCENT (single, coral): --amber #D6351C (renamed-in-place; warm hue ~8°).
  white-on-coral 4.79 (AA CTAs) · coral-as-text 4.4-4.8 on white (AA). --amber-2 #F26B52.
  --on-accent #FFFFFF (text on the coral fill, was the old dark #1a1206 ×8).
- Declines RED & reserved: --neg #C01A2B (deep COOL crimson hue ~354°, distinct
  from warm coral; white-on 6.13) · --neg-2 #B42318 (decline text, 6.6:1 AAA).
- --pos #0E9F6E (green, legible on white). --video-bg #0C0E12 (intentional dark
  cinema frame for video stages, was stray #000 ×7).
- rgba glows remapped: coral 214,53,28 · green 14,159,110 · decline 192,26,43.
- Depth: dark-mode coral glows → subtle dark drop-shadows + hairline coral borders
  on .mark/.hero-mark/.delivered-hero/.pg-main/.pace-opt.is-on/.ph-chip.now.
- .grain re-based light: soft coral bloom top-right (.07) + faint paper grain +
  slow drift (parked under reduced-motion). Code syntax tokens darkened for white
  surface (.tok-num/.tok-tag/.tok-str/.tok-com). Receipt → --bg-2 so perforation reads.

## How each STATE reads in light (screenshots /tmp/hermes-light-*.png):
- idle: near-white, coral H mark + Build CTA pop, hero builds word-by-word w/ coral
  accent word, green margins, coral bloom visible, decline run shows red "✕ 1 cut".
- building(producing): phase track w/ PRODUCING lit coral + coral fill sweep, "1 of 4
  scenes" + coral progress bar, WORKING card coral-bordered, queued cards shimmer.
- awaiting_payment: coral $49.00 price hero, coral "Pay…" CTA (white text), TEST-MODE
  coral badge, PAYMENT phase chip lit, side summary; script/storyboard folds closed.
- delivered: coral DELIVERED badge, word-built headline, dark cinema video frame
  (intentional), coral Download MP4 CTA, 6 proof folds CLOSED; open fold → off-white
  perforated receipt + green NET MARGIN read fine.
- failed: solid cool-crimson FAILED pill (distinct from coral) + deep-red message.

## Preserved + adapted (verified):
- Declutter: <details> folds (closed by default), run-delete .rc-del + two-step
  confirm .rc-confirm/.rcc-yes/.rcc-no (18 CSS rules + wired in app.js) — intact.
- Motion polish: all 6 keyframes (rise/blur-rise/grow-in/ph-sweep/ph-now-pulse/
  sb-shimmer) + word-by-word .hw build + N-of-M + radius tiers — intact, adapted to
  light (shimmer/fill/glow visible on white). prefers-reduced-motion guard intact
  (+ .grain drift parked).
- app.js: ZERO color edits needed (was already 100% token-driven). index.html: no
  edits (structural). Only styles.css recolored.

## $0 VERIFICATION (all pass):
- node --check dashboard/app.js → OK.
- Headless Chrome DevTools harness drove ALL 5 states via the app's own render fns:
  25/25 checks pass — each state renders, folds present+closed, delete/confirm markup
  present, motion markup present, and computed-color CONTRAST: ink/canvas 16.5 (AAA),
  every coral CTA white-text ≥4.5 (AA), coral-as-text ≥4.1 (AA), FAILED pill red 6.13
  + confirmed hue-distinct from coral.
- curl :3030 confirms served styles.css carries the final tokens (edits live).
- Temp harness removed; dashboard/ holds only the 3 source files (+ serve.py untouched).

## FLAG — Dennis's Safari step ($0):
Final visual sign-off is yours in Safari at http://localhost:3030 — specifically
(1) light-mode CONTRAST in your eye (the 4.79 coral CTAs / 4.4 coral text are AA but
on the tighter side — confirm they feel crisp, not muddy), and (2) how the MOTION
reads on white: the coral phase-fill sweep, the queued-card shimmer, the word-by-word
hero build, and the ambient coral bloom drift — all tuned for white but only your eye
confirms they pop without looking washed out.

