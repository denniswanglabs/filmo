# Handoff — card-copy fix in remotion_codegen.py

## Scope (locked)
Edit ONLY `remotion_codegen.py`. The ONE problem: `_copy_from_brief()` prints scene
DIRECTION and internal/meta tooling language as on-screen copy.

Two sub-bugs:
1. motion_graphic branch returns `brief[:48]` as the card TITLE -> renders the raw
   scene direction (e.g. "Simple animated divider with the text 'Built for").
2. Hardcoded meta kickers "Producer cut" / "Call to action" render (uppercased via CSS)
   as PRODUCER CUT / CALL TO ACTION on client-facing cards.

## Phase log
- [phase 0] Read brief + VIDEO-GENERATION-REVIEW.md + VERIFICATION + remotion_codegen.py.
  Confirmed bug sites: lines 316 (cta kicker), 319 (open kicker), 321-322 (motion_graphic title).
  `_DIVIDER` template uses BADGE (number) + TITLE (label). Plan: derive a clean short
  title-cased LABEL from the brief, stripping leading directive phrases; fall back to the
  badge alone (empty TITLE) if nothing presentable. Replace meta kickers with brand-neutral copy.
- [phase 1] DONE. Edited `_copy_from_brief` + added `_section_label` helper. ONLY file touched: remotion_codegen.py.
- [phase 2] VERIFIED ($0). Generated TSX for (a) motion_graphic divider, (b) open title, (c) close title via
  `generate()`. On-card copy clean (no direction dump, no Producer cut / Call to action). `tsc --noEmit`
  with studio compiler opts + real react/remotion types -> EXIT=0 for all three. No protected runs touched;
  no active.tsx overwritten (used a throwaway src/_tsxcheck/ dir, then removed it).

## What changed (old -> new)
motion_graphic TITLE:
  OLD: `return brief[:48] or "Section", "", "", "2"`   (dumped raw direction)
  NEW: `return _section_label(brief), "", "", "2"`       (clean section label or "" -> badge only)
  + new `_section_label(brief)` helper: prefers a quoted on-screen phrase in the direction,
    else strips leading directive scaffold ("the text", "reading", filler words like
    simple/animated/divider/with...), title-cases, caps at 42 chars; returns "" if nothing clean.
open-title kicker:  OLD "Producer cut"  -> NEW "Introducing"
close-title kicker: OLD "Call to action" -> NEW "Get started"
generic-title kicker: OLD "Scene" -> NEW "Section" (minor; "Scene" read as tooling label)

## On-card text for the 3 sample scenes
(a) motion_graphic divider (Linear, brief "Simple animated divider with the text 'Built for developers'"):
    TITLE "Built For Developers", SUB "", KICKER "", BADGE "2"  (no direction dump)
(b) open title (Notion, brief "Notion logo and tagline"):
    TITLE "Notion", SUB "One workspace. Every team.", KICKER "Introducing", BADGE "01"
(c) closing title (Linear, brief "Call to action, get started"):
    TITLE "Start building", SUB "linear.app", KICKER "Get started", BADGE "→"

## Compile check
tsc --noEmit (studio opts: react-jsx/strict/bundler/ES2020) on all three generated .tsx -> EXIT=0.

