# R3-C Feel Checkpoint — D9 feel + D7 on white-bg brands

**Status:** COMPLETE — both problems fixed, verified via rendered stills + tests green.
**Date:** 2026-06-22

---

## Problems addressed

### Problem 1: White-bg brands show no brand accent (D7/D9 gap)
Shopify green, TripAdvisor green, Plaid blue → title/CTA beats rendered navy text only;
accent color was invisible on white backgrounds when `punchWord` was absent.

### Problem 2: Redundant title hierarchy (Plaid "PLAID"/"Plaid"/"Plaid" triple repeat)
`_shape_hero` opening path: kicker = WORDMARK, title = tagline (≈wordmark), subtitle = tagline again.
When tagline ≈ brand name, all three lines echoed the same word.

---

## Files changed

### `studio/src/timeline/archetypes/HeroTitle.tsx`
- Added **accent underline rule** (4px rounded bar in `theme.accent`) between the title and subtitle.
- Slides in with the title animation, width breathes gently on the breath cycle.
- Glow intensity pulses with `punchGlow` so it amplifies when a punchWord fires.
- Visible on any background — the accent color now always appears on white-bg brands.

### `studio/src/timeline/archetypes/AppleHero.tsx`
- Same accent underline rule added between title and subtitle.
- Slides in with `title.transform`, width interpolates with `punchP`.
- `marginTop: -8` so it tucks under the title without excess gap.

### `style_fill.py`
- Added `_distinct_tagline(brand, avoid)` helper:
  - Priority 1: brand's clean tagline, if distinct from wordmark and avoid.
  - Priority 2: synthesize from first 2 short feature labels joined with " · ".
  - Returns "" if nothing distinct found.
- Updated `_shape_hero` OPENING path:
  - When `_brand_fallback_title` resolves to just the wordmark (no useful tagline),
    promotes wordmark to eyebrow and calls `_distinct_tagline` for a distinct display title.
  - Subtitle also goes through `_distinct_tagline(avoid=title)` to ensure 3 distinct lines.
  - Final guard: subtitle never equals wordmark.
- Updated `_shape_hero` CTA/closing path:
  - Added guard: subtitle ≠ title (falls back to cta_url when they'd match).

---

## Verification

### Rendered stills (hand-authored props.json, $0, no build_runner)

**Shopify (white-bg, green accent `#96BF48`):**
- `/tmp/r3c-verify-shopify/still-open.png` — frame 60: "Commerce made better" with "better"
  in green + GREEN ACCENT UNDERLINE visibly below title on pure white bg. ✓
- `/tmp/r3c-verify-shopify/still-cta.png` — frame 200: "Start selling today" with green
  underline rule. ✓

**Plaid (white-bg, blue accent `#2563eb`, no-repeat tagline):**
- `/tmp/r3c-verify-plaid/still-open.png` — frame 60: wordmark "Plaid" / kicker "PLAID" /
  display "The financial data network" (explicit in fixture) — 3 DISTINCT lines. Blue
  accent underline visible below title on white. ✓
- `/tmp/r3c-verify-plaid/still-cta.png` — frame 200: "Connect your financial life" + blue
  underline + "plaid.com" subtitle. ✓

### Python unit test (logic verification)
- Plaid tagline=wordmark → kicker "PLAID" / title "Bank Connections · Identity Verification"
  / subtitle "" — no triple-repeat. ✓
- Plaid no tagline → same feature synthesis. ✓
- Shopify real tagline → passes through normally. ✓

### TypeScript
- `tsc --noEmit` — clean, no errors. ✓

### Test suite
- `./run_all_tests.sh --no-eval` — ALL TESTS PASSED, $0 spent. ✓

---

## What the orchestrator should do next

Gate: wait for R3-A and R3-B to land, then run the REGENERATE on the fixed subset
(stripe/tripadvisor/linear/shopify/plaid) with `WS_WALKTHROUGH_CACHE=/tmp/walk-smoke-short.mp4`
and re-score vs rubric. Attribute D7/D9 delta to this fix. Keep if mean improves, revert if flat.

Files owned by R3-C (DO NOT touch in other fixes):
- `studio/src/timeline/archetypes/HeroTitle.tsx`
- `studio/src/timeline/archetypes/AppleHero.tsx`
- `style_fill.py` (the `_distinct_tagline` helper + `_shape_hero` dedup logic)
