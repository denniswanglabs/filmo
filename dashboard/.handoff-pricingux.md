# Pricing UX → cost-plus per-video (composer + quote). DONE.

Date: 2026-06-21. Server (:3030, orchestrator-managed serverId bc31a226) left
untouched — driven read-only via Claude Preview eval, never started/stopped.

## Pricing contract (pricing.json v2, model "cost_plus")
- markup 6.0, price_floor_cents 500, round_to_cents 50.
- base = max(floor, round_to_nearest(round_to_cents, plan_cogs * markup)).
- addons.premium_vo: label "Premium voiceover", price_cents 300 ($3), cogs 20.
- NO tiers. ONE add-on: premium_vo = ElevenLabs STOCK voice, NO consent.
- selection shape: `{ options: { premium_vo: bool } }`.

## Files edited (backups: *.pre-pricingux.bak)
- index.html, app.js, styles.css.

## What changed
### COMPOSER (index.html)
- REMOVED the `<details #cmp-pricing>` expander (tier selector + booster grid +
  consent guardrail + `#pricing-menu`). Replaced with one quiet line
  `.cmp-pricenote`: "The agent scours your site, picks the best video, and
  prices it per-video. You'll see the quote before you pay." Composer is now
  just URL + goal + pacing + mock + Build.

### app.js
- state.selection default → `{ options: { premium_vo: false } }`.
- DELETED: PMENU, loadPricing's renderPricingMenu call, boosterEligible,
  boosterQty, computeQuote, consentRequiredButMissing, isPlaceholderOwner,
  renderPricingMenu, consentBlock, wirePricingMenu, stepQty, renderQuote,
  updateConsentWarn, syncBuildButton (~260 lines).
- ADDED cost-plus helpers: premiumVoCents/Label, costPlusBase(planCogs)
  (mirrors price_for_plan), ledgerBaseCents(l) (reads earn.base_price_cents →
  earn.price_cents [backs out add-on] → cost-plus from plan_cogs → legacy
  suggested_price).
- loadPricing: just fetches pricing.json (no menu render); null on failure
  (quote falls back to priced ledger).
- buildSelectionPayload → returns `{ options: { premium_vo: !!... } }` only.
  startBuild sends `{url,goal,mode,pace,selection}` on POST /api/build, selection
  read live from state.selection (consent gate removed).
- BOOT: loadIndex auto-select branch REMOVED. Fresh load (no active build, no
  selection) stays on the COMPOSER. Active-build resume (/api/active→beginPoll)
  and ?paid/?cancelled checkout return still take priority; explicit sidebar
  click still opens a past build.

### QUOTE / PROPOSAL (app.js payGate, phase=awaiting_payment)
- payGate rebuilt as the quote: proposal line "Here's the video the agent will
  make for <brand> — <goal>." + big TOTAL price (#pg-price) + cost-plus subtitle.
- premiumVoToggle(): one upgrade row "<label> more natural ... +$3" (#pg-up-chk).
- quoteItems(): itemization "Video production $base" + (if on) "Premium
  voiceover $3" + "Total $total" (#pg-q-total).
- Pay button "Pay $<total> to produce" (#pg-pay-amt).
- initPayGate: toggle handler sets state.selection.options.premium_vo + calls
  refreshQuote(l) — updates big price + pay amount + itemization + total IN
  PLACE (no full re-render; survives poll re-renders since it reads state).

### styles.css
- REMOVED .cmp-pricing*, .build-go.pm-gated, the whole PREMIUM PRICING MENU
  block (.pmenu/.pm-*/.pm-q-*). ADDED .cmp-pricenote, .pg-proposal/.pg-prop-goal,
  .pg-up* (upgrade toggle), .pg-quote/.pg-q-* (itemization). Coral on selected
  upgrade + total only.

## Verification (all PASS)
- `node --check app.js` clean. No dead refs to removed fns/classes in app.js or
  styles.css. Served files (curl + preview) reflect edits.
- curl: / → 302 /dashboard/index.html → 200; /pricing.json 200.
- Preview eval (live shipped code, sandbox re-eval of app.js):
  - Cost-plus math: 120c COGS → $7.00; +VO → $10.00. Floor (10c) → $5.00.
  - Priced ledger 95c: off $0.95; ON $3.95 (exactly +$3). Toggle reverts to $0.95.
  - Pay-gate render: proposal line correct; NO tier/booster/consent
    (hasTier/hasBooster/hasConsent all false); itemization
    [Video production $0.95, Premium voiceover $3.00, Total $3.95]; #pg-price,
    #pg-pay-amt, #pg-q-total all track the toggle live.
  - Build payload when VO on: `{"options":{"premium_vo":true}}`.
  - Fresh load + "New build": composer visible, composerHasPricingUI=false.
  - loadIndex auto-select branch absent from live app.js; boot comment present.
- Screenshot: clean composer (URL/goal/pacing/mock/Build + cost-plus note),
  sidebar + styles gallery preserved.

## Note
- No run currently in awaiting_payment, so the pay-gate quote was verified by
  re-evaluating the SHIPPED app.js against a synthetic awaiting_payment ledger
  (and by the cost-plus math harness) — not by an existing live run. Orchestrator
  can confirm visually once a build reaches the pay-gate.
- An active build (demo-parallel-stripe, stitching) was in flight; boot correctly
  resumes it. That is the preserved live-build path, NOT the removed auto-select.
