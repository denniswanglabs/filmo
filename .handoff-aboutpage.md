# .handoff-aboutpage.md — About page for Walk Studio dashboard

## Task
Remove the curated/inspiration lookbook from the FRONT PAGE (composer empty-state)
and create an ABOUT view holding (a) an ARCHITECTURE pipeline explanation w/ native
SVG diagram, and (b) a "WHY OUR VIDEOS ARE BETTER" curation thesis where the curated
lookbook films now live ("the bar every video is held to — hand-picked by Dennis").

## Design language learned (match exactly)
- Tokens in styles.css `:root`: --amber (#D6351C coral accent), --ink/-2/-3/-4,
  --bg/-1/-2/-3, --line/-2, --pos, --neg, --on-accent. Radius: --r-ctl 6 / --r-card 10
  / --r-pill 999. Fonts: --mono (body!), --serif (headlines). Eases: --ease-rise,
  --ease-spring.
- Routing pattern (mirror Analytics): sidebar button `#rail-analytics` -> `openAnalytics()`
  sets `state.view`, hides #stage-empty, renders into #detail; `exitAnalytics()` resets;
  called from newBuild()/selectRun()/openAnalytics(). Deep-link `?view=analytics` in init().
- Lookbook is in index.html `<section class="styles styles-lite">` (lines ~188-250):
  5 `<article class="style-card">` each = poster `<img class="sc-poster">` +
  YouTube watch link. Posters: /dashboard/assets/styles/{orinovate,hotcake,kuli,trayd,jgb}.jpg.
  NO `<video>` files — just JPG posters linking to YouTube. Reuse SAME 5 assets.
- Analytics view markup helpers: analyticsHead() (.an-eyebrow + .an-title serif + .an-sub),
  .an-cards/.an-card. CSS for .styles/.style-card/.sc-* fully reusable on About page.

## Curated assets (relocate, don't fabricate)
5 JPG posters under dashboard/assets/styles/: orinovate.jpg, hotcake.jpg, kuli.jpg,
trayd.jpg, jgb.jpg. Each links to a YouTube watch URL (see index.html). Moving the
whole `<section class="styles">` markup into the About view.

## Real pipeline (from HANDOFF) to explain truthfully
URL + goal -> brand_extract (palette/fonts/wordmark/real copy) -> planner brain
(Nemotron Super-120B) decomposes into a storyboard -> align_vo (VO + word-level timing)
-> build_timeline (picture built FROM the voice; scenes anchored to words) ->
style_fill (fills each scene w/ designed on-brand motion-graphics in a curated style)
-> Remotion renders -> cost-plus priced + paid on Stripe -> video ships.

## Plan / phases
1. [DONE] Read context, learn design language, write this checkpoint.
2. index.html: add `#rail-about` sidebar button (mirror #rail-analytics); remove the
   `<section class="styles styles-lite">` lookbook from #stage-empty; add `#about` view
   container OR render into #detail like analytics (chose: render into #detail, same as analytics).
3. app.js: openAbout()/exitAbout(), call exitAbout() in newBuild/selectRun/openAnalytics,
   call exitAnalytics-equiv from openAbout; wire #rail-about in init(); ?view=about deep-link;
   move the curated cards into an aboutView() string; build the architecture SVG + copy.
4. styles.css: .about-* styles (reuse .styles/.style-card tokens), pipeline diagram CSS.
5. VERIFY: node --check app.js, emoji scan, grep front page has no lookbook, About routes.

## STATUS: phases 1-4 complete; verifying (phase 5).

### What changed (file -> change)
- dashboard/index.html:
  - Removed the entire `<section class="styles styles-lite">` lookbook from #stage-empty
    (front page now ends after the hidden #hero-start hook; left a comment pointing to About).
  - Added 6 new icon symbols to the sprite: i-info, i-star, i-layers, i-brand, i-mic, i-wave.
  - Added `<button id="rail-about" class="rail-analytics rail-about">` in .rail-foot,
    ABOVE #rail-analytics (About = customer-facing; Analytics = operator). Uses #i-info.
- dashboard/app.js:
  - newBuild() / selectRun() / openAnalytics() now also call exitAbout().
  - New block before `/* boot */`: openAbout(), exitAbout(), ABOUT_LOOKBOOK (5 films),
    aboutLookbookCards(), aboutPipelineSVG() (native inline SVG, 7-stage pipeline),
    aboutStep(), aboutView() (Section A architecture diagram + numbered steps;
    Section B curation thesis + relocated lookbook). Mirrors openAnalytics/analyticsView.
  - init(): wired #rail-about -> openAbout; added ?view=about deep-link (same shape as
    ?view=analytics).
- dashboard/styles.css:
  - Appended ABOUT block after the analytics CSS: .about, .about-sec, .about-kicker,
    .about-h, .about-lead, .ab-em; .ab-diagram + .ab-svg (themed via --ws-* vars) +
    ab-pop entrance; .ab-steps/.ab-step; .about-thesis/.about-thesis-card/.att-*;
    .about-lookbook (reuses shared .styles-grid/.style-card/.sc-*). Responsive @880px.

### Assets relocated
The SAME 5 JPG posters (no new assets, no external media): dashboard/assets/styles/
{orinovate,hotcake,kuli,trayd,jgb}.jpg, each linking to its existing YouTube watch URL.
Moved from the front-page lookbook into aboutView()'s "The bar every video is held to"
grid (via ABOUT_LOOKBOOK).

### How to open the About view
- Click "About" in the sidebar footer (above Analytics), OR
- Deep-link: http://localhost:3030/dashboard/index.html?view=about

## VERIFY RESULTS (phase 5 — all green)
- `node --check dashboard/app.js` -> OK (run twice).
- Emoji scan `grep -nP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' dashboard/*.{html,js,css}` -> empty.
- Front-page lookbook markup in index.html (styles-grid/style-card/sc-poster/styles-lite) -> 0.
- About wiring in app.js -> 15 hits; #rail-about button in index.html -> present.
- All About SVG/step icons resolve to defined sprite symbols; all 5 original YouTube
  URLs preserved; 5 posters intact.
- Composer / hero / ex-chips / cmp-quality / Analytics tab all still present (untouched).

## STATUS: COMPLETE.
