# Handoff — loudness fix (finish_cut.py)

Status: DONE, verified. Date 2026-06-19.

## Problem
Delivered finals ship ~6 LU under the -14 LUFS streaming standard.
Measured baseline (verifier's NEW finding): demo-1 final = -19.8 LUFS / -6.2 dBTP;
build-stripe = -20.6 LUFS. Sounds conspicuously quiet on a judged stage.

## Fix (finish_cut.py ONLY — no other files touched)
Added EBU R128 single-pass loudnorm on the FINAL mixed audio track, placed AFTER
the music-bed duck/mix so it normalizes the delivered mix (both VO and no-VO paths).

Filter-chain change (around lines 82-91):
- Renamed the two existing final audio labels from `[aout]` to `[amix]`:
  - VO path:    `[musduck][vomix]amix=inputs=2:duration=first:normalize=0[amix]`
  - no-VO path: `[musraw]volume=0.85[amix]`
- Appended one stage producing the final `[aout]`:
  - `[amix]loudnorm=I=-14:TP=-1.0:LRA=11[aout]`
The existing `-map "[aout]"` now maps the normalized track. No other edits.

## Verification ($0, no mutation of any run)
Copied runs/demo-1-approve/final.mp4 -> /tmp/loudtest-src.mp4, applied the exact
filter to the COPY, re-measured the RESULT file (not just the predicted output).
ffmpeg bounded with `perl -e 'alarm 180; exec @ARGV'`.

- BEFORE: Input Integrated -19.8 LUFS, True Peak -6.2 dBTP, LRA 1.8
- AFTER (measured on normalized output): Integrated -14.0 LUFS, True Peak -1.0 dBTP
- Clipping check (astats): sample Peak level -1.02 dBFS, Flat factor 0.000 -> NO clipping
- `python3 -c "import ast; ast.parse(open('finish_cut.py').read())"` -> PARSE OK

/tmp test files cleaned up.

## Notes
- Single-pass loudnorm chosen (clean + correct); landed dead on -14.0 / -1.0.
- LLN works for both VO and no-VO branches since both feed `[amix]`.
- Did NOT touch apply_real_media.py, remotion_codegen.py, adapters.py, dashboard, or any runs/.
