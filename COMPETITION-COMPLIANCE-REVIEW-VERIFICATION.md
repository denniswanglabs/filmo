# Compliance Review — Adversarial Verification

_Read-only verification of `COMPETITION-COMPLIANCE-REVIEW.md`, 2026-06-19. NO edits to code, NO builds,
NO money, NO paid API calls, NO serve.py restart. Method: re-ran the load-bearing greps and read the
cited code/config/artifacts directly. Verdict on each claim below with file:line evidence._

## Headline: the audit is accurate and fair. All four pillar verdicts CONFIRMED. Two minor cite/wording corrections, one real nuance the audit under-states, and three MISSING compliance angles. No false alarms; no missed mandate that the audit didn't already flag for Dennis.

---

## 1. THE CRITICAL CLAIM — "Hermes doesn't drive the run" → **CONFIRMED, with an important nuance the audit under-states**

**Grep (whole project, all langs):** searched `hermes (chat|run|exec|skill)`, `"hermes"` as argv,
`subprocess.*hermes`, `/hermes` across `*.py/*.sh/*.js/*.ts/*.yaml/*.json`. Every hit is one of:
a docstring/brand string (`producer.py:2`, `remotion_codegen.py:82` "generated by the Hermes producer agent"),
the `~/.hermes/.env` config path (`stripe_money.py:29`, `stripe_earn.py:58`), a Stripe param brand
string (`stripe_money.py:142` `individual[first_name]: "Hermes"`), or an eval that calls **Nemotron**
directly (`hermes_*_eval.py`). **No project code invokes the `hermes` binary.** Confirmed "empty."

**Dashboard → engine chain (CONFIRMED, exact quotes):**
- `dashboard/serve.py:125-129`:
  ```python
  inner = ("cd %s && python3 build_runner.py --url %s --goal %s --run-id %s --mode %s --duration %d" % ...)
  subprocess.Popen(["zsh", "-lc", inner], stdout=logf, stderr=subprocess.STDOUT, cwd=PROJECT_ROOT, start_new_session=True)
  ```
- `build_runner.py:114`: `data, _ = orchestrator.orchestrate(plan, run_id, mode=mode, ...)` — a **plain
  Python function call**, no Hermes.
- `orchestrator.py:4-6` docstring: _"This is the deterministic Python harness for the producer-brain
  SKILL flow … the test suite asserts against it, and a live Hermes/Nemotron run should reproduce its
  gate verdicts."_ — quoted accurately by the audit.
- Real path also Hermes-free: grep of `orchestrator.py` + `adapters.py` for a `hermes` binary call →
  NONE. The real walkthrough shells `bash ./tutorial-maker.sh` (`adapters.py:200-202`), not `hermes`.

So "the DASHBOARD pipeline bypasses Hermes" is **fully confirmed**.

**NUANCE the audit under-states (correction in our favor): a real, runnable Hermes-driven path DOES exist as a skill.**
- The `hermes` binary is present and real: `~/.local/bin/hermes` → execs
  `~/.hermes/hermes-agent/venv/bin/hermes`.
- `~/.hermes/skills/producer-brain/SKILL.md:94-99` contains a concrete runnable Hermes invocation for
  the PLAN step: `hermes chat -Q -q "$(cat .../planner-prompt.md)\nCOMPANY_URL=… GOAL=…" > /tmp/producer-plan.json`,
  then shells to `producer.py estimate`/`gate` and the segment skills step by step (lines 110-185).
  Hermes `config.yaml:404-406` is correctly wired to it (`default: nvidia/nemotron-3-super-120b-a12b`,
  `base_url: https://integrate.api.nvidia.com/v1`, `provider: nvidia`).
- So the precise truth is: **a Hermes-agent path EXISTS and is wired, but (a) it is NOT what the dashboard
  runs, and (b) nothing proves it has been executed end-to-end autonomously.** The SKILL.md's own
  evidence is a `## Worked Example: DRY-RUN` (line 253) that "See the session transcript" (line 255) —
  a manual dry-run, not an autonomous Hermes run. The audit said exactly this (Pillar 1 (b), "nothing
  proves Hermes actually runs this skill end to end"). **It is correct, but its TL;DR phrasing ("The
  Hermes runtime never drives an end-to-end run") slightly undersells that the on-ramp is built** — the
  remediation is "execute and record the existing skill via `hermes chat`," NOT "build a Hermes path
  from scratch." The audit's fix (d) gets this right; the table verdict reads slightly more dire than
  the body. **Verdict AT-RISK stands; severity HIGH stands.**

---

## 2. Nemotron → **CONFIRMED COMPLIANT**

- **Right model/endpoint/free tier:** `validate_planner.py:15-16`, `hermes_judgment_eval.py:28-29`,
  `hermes_skill_eval.py:23-24` all hardcode `API_URL = https://integrate.api.nvidia.com/v1/chat/completions`
  + `MODEL = nvidia/nemotron-3-super-120b-a12b`. Hermes `config.yaml:404-406` matches. The audit cited
  "config.yaml" without a line number — substantively correct; **minor cite imprecision** (the value is
  at line 404, and the per-role override blocks near the top are empty/`auto`, which could confuse a
  reader who greps the top of the file first).
- **Planning is load-bearing:** `plan_job.py:38-56` `_plan_with_nemotron` calls `vp.call_model`,
  extracts JSON with a one-repair retry, and returns `None` (→ deterministic template) ONLY when the key
  is absent (`:39`) or the call throws (`:55-56`). Real path = the model. Confirmed.
- **Evals are real and load-bearing:** saved results `runs/judgment-eval.json` = **7/7** and
  `runs/skill-eval.json` = **19/19**, both `model: nvidia/nemotron-3-super-120b-a12b`. The judgment eval
  computes the GOLDEN verdict with `producer.py` (deterministic) and checks the **live model** matches
  (`hermes_judgment_eval.py:11`), incl. the `moneyshot-decline` scenario (`:64`).
- **The "gates run deterministically in producer.py, not via live Nemotron" caveat is CONFIRMED:**
  `orchestrator.py:64-65` + `:263` call `producer.py gate` (budget math), not a live Nemotron call. The
  reliability handling (`max_tokens: 8000` + JSON extract/repair, `validate_planner.py:99-102`,
  `plan_job.py:48-54`) is real. **Verdict COMPLIANT stands.**

---

## 3. NemoClaw → **CONFIRMED PARTIAL**

- **Binary present:** `~/.local/bin/nemoclaw` (97-byte wrapper → `/opt/homebrew/bin/nemoclaw`).
- **`tutorial-maker.sh` genuinely drives it:** at `~/Desktop/Projects/Hackathons/walk-ultra/tutorial-maker.sh:36`
  (`NEMOCLAW="$HOME/.local/bin/nemoclaw"`), `:64` ("Step 2/4: NemoClaw agent run"), `:68` (the exec), and
  `:114` (`"$NEMOCLAW" walk-ultra exec --no-tty -- …`). The audit's cited lines (~36, 55-68, 114) match.
- **Gated to `--mode real`:** `adapters.py:189-205` `generate_walkthrough` → `mode == "mock"` renders a
  `synth_clip` color card (`:190-192`); only the real branch shells to `tutorial-maker.sh` (`:200-202`).
  Confirmed.
- **ALL committed ledgers are `mode: mock`:** verified every `runs/*/ledger.json` (7 files) →
  `mode=mock`. No sandbox run in any artifact. Confirmed. (Memory line 45 independently confirms demo-1/3
  real footage was swapped in by `apply_real_media.py` as a SEPARATE manual step, not the orchestrated
  ledger.) **Verdict PARTIAL stands.**
- **Minor wording note:** the audit calls the mock walkthrough a "teal color card." It's
  `TYPE_COLOR["walkthrough"]` — plausibly teal but I did not read the literal hex; immaterial to the
  verdict.
- **Scope note (not an error):** `tutorial-maker.sh` lives in the **sibling `walk-ultra/` project**, not
  inside `hermes-video-agent/`. The audit acknowledges this. A judge browsing only the submitted repo
  would not see the NemoClaw integration unless walk-ultra is included — worth Dennis noting for the
  submission package.

---

## 4. Stripe → **CONFIRMED PARTIAL**

- **Earn = real TEST Checkout, refuses live, asserts livemode:** `stripe_earn.py:92-113`
  (`build_session_params`, `mode: payment`, inline `price_data`, `client_reference_id = job_id`),
  `:116-152` (`create_checkout_session`), `_assert_test_key` refuses missing AND `sk_live_/rk_live_`
  keys (`:49-69`), asserts `livemode==false` → `LiveModeError` (`:138-142`, `:164-166`). Confirmed.
- **Spend simulated by default:** `stripe_money.py:80-83` `live` defaults False and only activates with a
  present key; `:171-180` simulated `authorize` returns `iauth_sim_%d` objects. `orchestrator.py:172`
  `StripeMoney(live=stripe_live)`; `:88` `stripe_live=False`. Confirmed.
- **3 Stripe Hermes skills installed but unused at runtime:** `stripe-link-cli`, `stripe-projects`,
  `mpp-agent` all present in `~/.hermes/skills/`. The ONLY runtime reference is `hermes_skill_eval.py:32,47-52`
  — the routing eval, which routes to a NAME and does not execute. Confirmed.
- **THE KEY CLAIM — no committed ledger has a real `cs_test_` earn session:** `grep -rl 'cs_test_'
  runs/*/ledger.json` → **NO ledger contains `cs_test_`**. Every ledger's earn block =
  `provider: dev, status: dev_mode, session_id: None`. NO ledger has an `awaiting_payment` phase (they
  predate the pay-gate). Every ledger DOES contain `iauth_sim` (the simulated decline). **So the audit
  is exactly right: the only Stripe evidence a judge sees in artifacts is the simulated decline.**
  Memory line 39 confirms a real `cs_test_` session was created — but only via `tests/test_stripe_earn.py`
  and a `PRODUCER_SIMULATE_PAID` run, never captured in a committed ledger. Confirmed.
- **Overclaim trap is real and well-flagged:** simulated `authorize` hardcodes
  `decline_reason: "spending_controls"` (`stripe_money.py:178`) even in sim mode — so narrating the sim
  decline as "Stripe's native cap declined it" would be a genuine overclaim. The audit's honesty caveat
  (and SKILL.md "Do NOT overclaim the money layer") correctly catches this. **Verdict PARTIAL stands.**

---

## Claims that are WRONG or overstated

1. **(MINOR, wording) Pillar 1 TL;DR.** "The Hermes runtime never drives an end-to-end run" is true as
   stated, but reads as if no Hermes path exists. Correction: a runnable Hermes path is **built and
   wired** (`producer-brain/SKILL.md:94-185` + `config.yaml:404-406` + the `hermes` binary); it is simply
   not the dashboard's path and has not been recorded running autonomously. This makes the fix cheaper
   than the table implies (execute + film the existing skill, not build it). The audit's body and fix
   already say this — only the one-liner is slightly alarmist. **Not a false alarm; the HIGH severity is
   warranted** because, recorded artifacts and dashboard behavior considered, Hermes is not visibly the
   driver today.
2. **(MINOR, cite precision) Nemotron config.yaml.** Audit cites "config.yaml" generically. The
   load-bearing values are at `config.yaml:404-406`; the top-of-file per-role blocks are empty/`auto`.
   Substance correct, location worth pinning so the next reader doesn't conclude the model is unset.
3. No claim was found to be materially wrong, overstated against the evidence, or mis-cited to a wrong
   file. The four verdicts are fair and grounded.

---

## MISSING compliance angles (the audit did not cover these)

1. **Open-source / code-submission + LICENSE.** There is **no `LICENSE` file** in the project root. If
   the hackathon requires an open-source license or a public code repo (common for these), that is an
   unaddressed gap. The MUST-VERIFY list mentions "open-source requirement" but the audit body never
   checks the repo's licensing state. **Add: confirm whether code must be submitted/open-sourced, and if
   so add a LICENSE.**
2. **Submission-package scope (multi-repo).** The NemoClaw integration (`tutorial-maker.sh`) and the
   real walk footage live in the **sibling `walk-ultra/` repo**, and the Hermes skills live in
   `~/.hermes/skills/` (outside any repo). If the submission is "a repo link," a judge cloning
   `hermes-video-agent/` alone sees neither the NemoClaw driver nor the producer-brain SKILL.md. **Add:
   decide what the submitted artifact is (mono-repo? zip? the dashboard?) so the pillar evidence is
   actually present in what's submitted.**
3. **Reuse-of-prior-work specifics (Walk Agent).** The audit lists "prior-work allowed?" in MUST-VERIFY
   but doesn't surface that **walk-ultra / Walk Agent is a prior hackathon WINNER** (per memory: won the
   NVIDIA Agent Hackathon). Reusing a prior winning entry's IP can be explicitly disallowed by some
   hackathon rules. This is higher-stakes than a generic "reuse?" line — **call it out specifically.**
   (The new "producer brain" IP is genuinely new; the walkthrough engine is the reused part.)
4. **Demo-video must-show content.** The audit's submission checklist confirms a 1–3 min video is
   PENDING but does not enumerate any sponsor "the demo MUST show X" requirement (some hackathons require
   the agent visibly using each sponsor tool on-camera). Folded into MUST-VERIFY #2 but worth making
   explicit given pillars 1/3/4 are all "capability exists but not in the recorded artifact."

---

## Corrected prioritized risk list

1. **[HIGH] Hermes doesn't drive the recorded run / dashboard run.** CONFIRMED. The path exists as a
   skill but is not executed; the dashboard runs `python3 build_runner.py`. Fix = record a real
   `hermes chat` run of `producer-brain`, OR be scrupulously honest the console visualizes a Python
   pipeline. **Disqualifying IF the rules mandate "must be a Hermes-runtime agent" (see MUST-VERIFY #1).**
2. **[MED] Stripe agentic-commerce SPEND not demonstrated; no real `cs_test_` in any artifact.** CONFIRMED.
   Only the simulated `iauth_sim` decline is in the ledgers. Fix = run one build through the pay-gate so
   a ledger shows a real `cs_test_` session (cheap), and/or wire one real `stripe-link-cli` buy (best).
   Do NOT narrate the sim decline as a real Stripe decline.
3. **[MED] NemoClaw absent from all committed artifacts** (all 7 ledgers `mock`). CONFIRMED. Fix = one
   real (free) walk-ultra/NemoClaw clip in the cut, narrated as sandboxed.
4. **[MED — RAISED from the audit's implicit silence] Submission-package + licensing gap.** No LICENSE;
   pillar evidence spread across 3 locations (project repo, walk-ultra, ~/.hermes/skills). Decide the
   submitted artifact and licensing before the deadline. *(New — not in the original risk list.)*
5. **[LOW] Nemotron judgment proven in evals but pipeline gates are deterministic.** CONFIRMED. Fix =
   one visible live per-scene gate call in the demo.
6. **[LOW] Submission mechanics unverified** (form, channel, timezone, axes, eligibility, prize terms).
   CONFIRMED unverified — correctly deferred to Dennis.

---

## MUST-VERIFY — completeness check

The audit's 7-item MUST-VERIFY list is well-framed and correctly scopes what only Dennis can confirm
against the live @NousResearch post / Discord pinned / official form. It already covers the Hermes/
NemoClaw/Stripe mandate question (#1 — the disqualification axis), submission mechanics (#2), deadline/TZ
(#3), judging axes (#4), eligibility incl. prior-work/open-source (#5), prize terms (#6), and real-vs-test
Stripe expectations (#7). **Recommended additions:**
- **#5 sharpen:** explicitly ask whether reusing a **prior hackathon-WINNING** entry (Walk Agent) is
  permitted — not just generic "prior work allowed?"
- **#1/#2 sharpen:** ask whether the demo video must **visibly show the agent using each sponsor pillar**
  (Hermes/Nemotron/NemoClaw/Stripe) — because today three of the four are "capability exists, not in the
  recorded artifact."
- **New #8:** is a **public code repo / open-source LICENSE** required? (No LICENSE exists today.) And:
  what exactly is "the submission" — a repo, a zip, a hosted demo — so the multi-location pillar evidence
  is actually included.

_Bottom line: the audit is correct, fair, and grounded; verdicts and severities hold. Tighten the
Pillar-1 one-liner (a Hermes path is built, just not run/recorded), pin the Nemotron config cite to
line 404, and add the licensing / submission-package / prior-winner angles to MUST-VERIFY._
