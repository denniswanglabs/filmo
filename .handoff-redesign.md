# Handoff — Dashboard RE-SHELL to Claude/ChatGPT-style (Walk Studio)

Date 2026-06-21. Scope: dashboard/{index.html,app.js,styles.css} ONLY.
RE-SHELL, not a logic rewrite. Server on :3030 is orchestrator-managed — never
restart. serve.py lives at project root (not dashboard/), serves files from disk,
302 / -> /dashboard/index.html, so edits go live on hard reload.

## Step 1 — DONE: backups
index.html.pre-redesign.bak, app.js.pre-redesign.bak, styles.css.pre-redesign.bak written.

## Element IDs / selectors app.js depends on (MUST preserve)
IDs: conn-dot, conn-text, refresh, rail, build-url, build-goal, build-pace,
build-pace-lbl, pricing-menu, build-mock, build-go, rail-stats, rail-list, stage,
stage-empty, stage-empty-msg, hero-title, hero-start, hero-sample, stage-empty-foot,
detail. Classes: .run-wrap/.run-card/.rc-del (rail list, built by renderRail),
.pace-opt, .stage-empty(.is-live), .how-step, .hero-cta, .hero-foot, .build-in.nudge.
- showEmpty(): toggles #detail hidden + #stage-empty visible; reads #stage-empty-msg.
- selectRun() error path: empty.querySelector("p").textContent — needs a <p> in #stage-empty.
- $("stage").scrollTo — #stage is the scroll container of the center column.
- init(): wires refresh, build-go (click), build-url (Enter), hero-start, hero-sample,
  build-pace .pace-opt; calls buildHeroTitle + replayLandingMotion + loadPricing.

## Target layout (Claude-like)
- LEFT SIDEBAR (#rail): Walk Studio logo (inline SVG, moved from header) at top;
  "New build" button; BUILDS list (#rail-list + #rail-stats, unchanged hooks);
  conn indicator + SYNC moved into sidebar footer (keep #conn-dot/#conn-text/#refresh).
  Collapses < 940px (existing @media hides .rail).
- CENTER (#stage): EMPTY state (#stage-empty) = greeting + COMPOSER (the NEW BUILD
  bar: #build-url, #build-goal, pace pill, #build-mock, #build-go) + collapsible
  Pricing (<details>) wrapping #pricing-menu + understated styles gallery.
  ACTIVE state = #detail (live build view) — untouched.
- Top header bar removed (logo now in sidebar).

## Step 2 — DONE: index.html restructured
- Removed header.bar. New top-level .shell = grid (#rail | #stage).
- Sidebar #rail: .rail-brand (logo SVG moved here) + #rail-collapse; #rail-new
  ("New build"); #rail-head (#rail-stats); #rail-list; .rail-foot (#conn-dot/
  #conn-text/#refresh in .rail-conn + tech stack). #rail-reopen floats in #stage.
- Center #stage: #stage-empty composer (#build-url, #build-goal, #build-pace +
  #build-pace-lbl, #build-mock, #build-go in .composer) + collapsible #cmp-pricing
  <details> wrapping #pricing-menu + understated .styles gallery. #detail unchanged.
- #hero-start now hidden (kept as hook); #hero-sample now an inline link in footnote.
- Removed the .how pipeline strip + .hero-cta markup (Claude-style simplicity).

## Step 3 — DONE: styles.css
- Replaced .bar/.layout/.rail with .shell + sidebar + .rail-collapsed/.rail-open
  + .rail-new/.rail-brand/.rail-collapse/.rail-reopen/.rail-conn/.rail-stack.
- New .composer block (card, .cmp-fields/.cmp-in, .cmp-bar, .cmp-pricing disclosure).
- Hid redundant .pm-head inside .cmp-pricing-body. Reworked .styles (eyebrow + h).
- Responsive @media 940px: sidebar collapses (rail-open overlay), grids stack.
- Brace balance verified (626/626). Old .build-bar/.how CSS left as harmless dead.

## Step 4 — DONE: app.js (DOM-move deltas only; ALL build/plan/pay logic preserved)
- Added newBuild() (stop poll, clear selection, deselect cards, showEmpty, focus URL).
- init(): wired #rail-new->newBuild, #rail-collapse/#rail-reopen->setRail (wide:
  toggle .rail-collapsed; narrow: toggle .rail-open). #hero-start/#hero-sample/
  #refresh/#build-go/#build-url/#build-pace wiring unchanged.
- selectRun(): on narrow viewport closes the overlay sidebar (.rail-open off).
- node --check app.js -> OK.

## Step 5 — DONE: verified (server NOT restarted)
- node --check app.js -> OK. CSS braces 626/626 balanced.
- curl :3030 serves the new shell; all ids present (shell/rail/rail-new/composer/
  cmp-pricing/build-* /pricing-menu/stage-empty/detail/conn-*); css+js 200.
- Preview (orchestrator :3030, serverId bc31a226...):
  * Empty composer renders (greeting "What should we make?", URL+goal fields,
    pacing pill, mock toggle, Build, collapsible Pricing).
  * Sidebar lists 21 past builds; logo + New build present; conn "21 jobs".
  * LIVE QUOTE climbs: Starter $19.00/89.5% -> Studio $249.00/75.9% -> +rush
    $299 -> +multi_format $329 -> +1 format qty $359. (money-shot works.)
  * "New build" returns to composer, deselects rail, focuses URL.
  * Clicking a past build (build-linear-54d446) opens its delivered-hero view in
    the center; empty hides, rail card marks active.
  * Zero console errors across all interactions.
- Backups: index.html/app.js/styles.css .pre-redesign.bak all present.

COMPLETE.
