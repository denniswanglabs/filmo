# Handoff — Apply chosen Walk Studio logo (horizontal two-tone)

Date: 2026-06-21
Status: DONE — all edits applied, page serves horizontal logo, suite green ($0).

## Chosen mark
Horizontal "two-tone + refined glyph": film-frame glyph (rounded square, coral
ticks) + coral-stroked cursor, "Walk" ink #16181D + "Studio" coral #D6351C,
viewBox 0 0 432 120.

## Changes made

### 1. Canonical file overwrites (branding/)
- `cp logo-walkstudio-h-light.svg -> logo-walkstudio-light.svg` (overwrote old stacked)
- `cp logo-walkstudio-h-dark.svg  -> logo-walkstudio-dark.svg`  (overwrote old stacked)
- `-h-` source files kept intact.

### 2. dashboard/index.html — header inline SVG swap
- Replaced the old stacked inline SVG (viewBox 0 0 540 120, "STUDIO" caption
  BELOW "Walk", 6-tick film frame at translate(16 18)) inside `.bar-brand`
  with the HORIZONTAL recommended logo: inlined the content of
  `branding/logo-walkstudio-h-light.svg` (viewBox 0 0 432 120, glyph at
  translate(18 18), "Walk" #16181D + "Studio" #D6351C side by side).
- Kept `class="bar-logo"` on the svg and the `/ PRODUCER CONSOLE` sub-label.

### 3. dashboard/index.html — favicon
- Updated `<link rel="icon">` data:image/svg+xml URI to the REFINED glyph only
  (viewBox 0 0 84 84): rx=22 frame #FFF1EE, border #F0BFB3, ticks #EAB6A9,
  coral cursor #D6351C with 1.4 stroke. Matches the new horizontal mark's glyph.

### 4. dashboard/styles.css — .bar-logo
- Kept `height: 36px; width: auto; display: block;`; added `flex: 0 0 auto;`
  so the wider lockup doesn't shrink. Updated comment to note new 432×120
  (3.6:1) viewBox. Renders at 36px H / ~130px W.

## SACRED — untouched
app.js logic, serve.py, Stripe path, "WHAT WALK STUDIO MAKES" Styles section.
No server start/stop (preview-managed on :3030). No emoji.

## Verification
- `curl http://localhost:3030/dashboard/index.html` (root 302 -> /dashboard/index.html):
  - viewBox "0 0 432" present (1), "Studio" x3, rel="icon" x1, favicon rx='22'
    + %23EAB6A9 present; OLD viewBox 540 = 0, OLD favicon %23F4C9BF = 0.
- Header tag balance: svg 2/2, g 2/2, header 1/1, text 2/2 — all OK.
- Preview reload + eval confirmed: viewBox 0 0 432, texts ["Walk" #16181D,
  "Studio" #D6351C], rendered H=36, favicon=refined-glyph. Screenshot shows
  glyph + Walk Studio + "/ PRODUCER CONSOLE" + SYNC all aligned.
- `./run_all_tests.sh --no-eval` -> "ALL TESTS PASSED — $0 spent."

Note: preview initially served a stale cached page (old 540 viewBox); a
`location.reload(true)` (no server restart) picked up the disk edits.
