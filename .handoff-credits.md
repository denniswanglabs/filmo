# Handoff — About page sponsor/stack credits (Hermes hackathon)

Status: COMPLETE. Static verification passed.

## Files edited (only these three, no backend touched)
- dashboard/app.js
- dashboard/styles.css
- (dashboard/index.html: NOT edited — no markup change needed; reused existing #i-spark icon)

## What was added

### 1. "Built on" credit row
Placement: at the TAIL of the Architecture section (Section A), after the numbered
`.ab-steps` walkthrough and before Section B. New `aboutBuiltOn()` helper +
`ABOUT_BUILT_ON` data array (app.js ~line 1802), called inside `aboutView()`'s `arch`
section (`'<ol class="ab-steps">' + steps + "</ol>" + aboutBuiltOn() + "</section>"`).

Three pillars, clean styled TEXT wordmarks (no fabricated logos), each over one line:
- NVIDIA  (coral "Nemotron" sub) — "Plans every storyboard. The agent's brain."
- Stripe                          — "Prices, charges, and budget-gates every build."
- Hermes                          — "The agent runtime that runs the whole studio end to end."

CSS: new `.about-built / .about-built-k / .about-built-grid / .bo-card / .bo-mark /
.bo-sub / .bo-line` block in styles.css after `.ab-step-d` (~line 1424), using
existing tokens (--bg-1, --line, --r-card, --serif, --mono, --amber, --ink-*).
Header chip "Built on" uses the existing #i-spark icon. Responsive: collapses to
1 column at max-width 880px (added to the existing media query).

### 2. Architecture narrative weave
Added to the `arch` section's `.about-lead` paragraph (app.js, after "...lands on
the words."): one sentence naming Hermes as the runtime driving every link, NVIDIA
Nemotron as the planning brain, and Stripe as what prices/charges/budget-gates every
build (declining over-margin scenes, no human in the loop). All three wrapped in
`.ab-em` emphasis spans. Existing architecture copy, diagram, steps, thesis, lookbook
all preserved.

## Verification
- node --check dashboard/app.js -> OK
- "Hermes" present in app.js (2x), "Stripe" (17x total, incl. new narrative), "Nemotron" (6x)
- emoji scan: empty (perl Unicode-range scan over the 3 files exit 0, no matches;
  note BSD grep lacks -P so used perl equivalent — orchestrator's GNU grep -P will
  also be clean)
