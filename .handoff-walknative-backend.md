# Handoff: native walkthrough backend (Phase 1)

## Status: COMPLETE. walk_native.py built + verified; adapters REAL branch + orchestrator wired; full suite green.

### Verification results (all PASS)
- T1 stripe.com 10s: exit 0; frame.jpg exists; 468 frames over 15.3s wall-clock (live updates proven, all distinct mtimes); state.json -> done (followed /pricing sub-page from emphasis); mp4 H.264 10.07s.
- T2 bad url: exit 1, returned in 0s (no hang), state action=failed, no mp4.
- T3 mock + WS_WALKTHROUGH_CACHE branches: unchanged, both produce valid mp4.
- T4 ./run_all_tests.sh --no-eval: ALL TESTS PASSED — $0. parallel/failure-isolation classes green.
- Integration via adapters.generate_walkthrough REAL: success -> {real,native:True}+frame.jpg+done; bad url -> returns None (graceful skip), no mp4, state failed, .walkagent.log written.

## Files
- NEW `walk_native.py` — standalone Playwright + CDP screencast + ffmpeg stitch. DONE.
- EDIT `adapters.py` generate_walkthrough REAL branch (~783) — PENDING.
- EDIT `orchestrator.py` walkthrough scene non-fatal (~427-438, _walkthrough_gen ~370) — PENDING.

## Key facts learned
- Capture venv: `.venv-capture/bin/python` -> python3.11, Playwright + chromium present, CDP `new_cdp_session` works.
- capture_screenshots launch: `chromium.launch(args=["--no-sandbox"])`, ctx locale="en-US", UA `_DESKTOP_UA`, `_EXTRA_HEADERS`, hydration gate = wait_for_selector("h1,h2,main,article,nav a") + networkidle + settle.
- adapters ffmpeg style: libx264 crf 20 veryfast, format=yuv420p,setsar=1, +faststart. `_run(cmd, timeout, check)`, `synth_clip`, AdapterError.
- generate_walkthrough(scene, out_path, mode, job_url) — PRESERVE the WS_WALKTHROUGH_CACHE branch (755-772) and mock branch (773-775) byte-for-byte; only replace REAL branch (776-798).
- orchestrator _walkthrough_gen closure (370-383) has run_dir, job, studio_active, mode, palette in scope.
- Walkthrough scene already isolated by `_isolate_failed_scene` via the try/except at 421-457; a returned None/skip must NOT append a clip and must mark scene dropped.
- `clips.append((idx, out))` only on success; stitch assembles survivors.

## walk_native contract (writes under <run_dir>/walk/)
- frame.jpg            (atomic, live preview — latest screencast frame)
- frames/fNNNNN.jpg    (numbered, for the video)
- state.json          {"url","step","action","ts"}  actions: navigating|scrolling|clicking|done|failed
- stdout final line: `WALK_NATIVE: ok <out>` or `WALK_NATIVE: failed <reason>`; exit 0 ok / 1 skip.

## Plan for wiring (non-fatal skip)
- adapters real branch: invoke [CAPTURE_PY, walk_native.py, url, goal, emphasis, out, run_dir, "12"], timeout=200, check=False.
  - run_dir derived from out_path parent-of-clips OR threaded param. emphasis = scene brief.
  - valid mp4 -> {"output_path": out, "real": True, "native": True}; else return None (graceful skip sentinel). Do NOT raise.
- orchestrator: walkthrough slot — if _walkthrough_gen returns None -> mark scene dropped/skipped, DO NOT append clip, continue. Already inside try/except so safe.
