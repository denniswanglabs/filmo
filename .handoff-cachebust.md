# Handoff: dashboard/app.js stale-state cache-bust fix

Scope: edited ONLY `dashboard/app.js`. No serve.py / studio / pipeline changes.
No server restart needed — app.js is served statically; user hard-refreshes to pick it up.
Backup: `dashboard/app.js.pre-cachebust.bak`.

## Edits

1. **New state field `ledgerToken: null`** (after `editorRuns`, ~line 30) — holds the
   per-render cache-bust token (ledger `Last-Modified`).

2. **New helper `mp4Bust()`** (after `icon`, ~line 39) — returns `"?t=<encoded token>"`
   or `""` when no token yet. Uses `state.ledgerToken`, never `Date.now()`.

3. **Fix 1 — invalidate editorRuns on rail refresh.** Start of `loadIndex()` (~line 200):
   added `state.editorRuns = null;`. Next `ensureEditorRuns()` refetches `/api/editor/runs`.
   In-render cache (`if (state.editorRuns) return`) is kept, so no per-render refetch.

4. **Fix 2 — capture token from ledger fetch.**
   - `selectRun()` ledger fetch (~line 575): `state.ledgerToken = res.headers.get("Last-Modified") || runId;`
   - live build poll `tick()` ledger fetch (~line 1195): same capture (keeps live scene
     clips fresh during production). The `?_=Date.now()` there is only the LEDGER-JSON
     buster, untouched.

5. **Fix 2 — append `mp4Bust()` to all 6 runs/<id>/*.mp4 video srcs:**
   - `deliveredHero()` `<source>` (line 711) — playback src only; Download + Copy-link keep clean URL
   - `sourceMedia()` scene clips (line 871)
   - live preview-stage `video.src` (line 1046)
   - inline final-cut `viewer()` `<source>` (line 1095)
   - live walkthrough-done `<video>` (line 1720)
   - storyboard `sb-clip` `<video>` (line 1850)
   Out of scope (correctly untouched): SHOWCASE reels use `/dashboard/assets/video/` (not runs/);
   live screencast `frame.jpg` already has its own `bust`.

## Verification (all PASS)
- `node --check dashboard/app.js` — OK
- `build-stripe-d0cf77` present in `/api/editor/runs` → editable after refetch
- ledger `Last-Modified` token identical across two loads of unchanged file (STABLE)
- token changes after `touch` (re-render) of ledger (CHANGES) — 1s granularity
