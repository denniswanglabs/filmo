# R7-G3 — GENRE-aware grounding + kill stage-direction artifacts (DONE)

_Agent: R7-G3. Files edited: `plan_job.py`, `validate_planner.py` ONLY. $0. No commit. :3030 untouched._

## What the 12-brand test exposed (the bug)
- **GENRE GAP:** The Verge (media/publication) got VO "Theverge gives your team the latest quick posts" — the planner treated a news publication as a B2B SaaS tool. World-knowledge/grounding had NO genre awareness beyond SaaS/dev-tools.
- **PROMPT ARTIFACT:** notion got "Show call-to-action: 'Start" as a rendered string — a stage direction emitted as spoken content / title.

## FIX 1 — GENRE-AWARE grounding (`plan_job.py` + `validate_planner.py`)
- NEW `detect_genre(company_url, company_facts)` in plan_job.py: classifies brand business model into one of `dev-tool | fintech | marketplace | ecommerce | media | social | services`. Priority: host hint (`_GENRE_HOST_HINTS`) → keyword inference (`_GENRE_KEYWORDS`, specific→broad) → `GENRE_DEFAULT="dev-tool"`.
- NEW `_GENRE_FRAMING` map: per-genre audience + value_verb + generic 5-tuple (open/home/inner/walk/cta) so an UNKNOWN media/retail/marketplace brand gets genre-appropriate copy, never SaaS "gives your team".
- NEW `genre_hint_text(genre)`: a GENRE HINT paragraph appended to the LLM system prompt (between style and quality blocks). Empty for dev-tool → **SaaS prompt byte-unchanged for SaaS brands**. media/marketplace/ecommerce get explicit "NEVER 'gives your team'" guards.
- `_grounded_template_beats(...)` now takes `company_facts`, detects genre, and:
  - scraped-features path: opening line via NEW `_genre_open_frame()` (media→"brings you", marketplace→"connects you with", ecommerce→"brings you", fintech→"powers … for your business", …) instead of the old hardcoded `"%s gives your team %s."` for EVERY brand.
  - last-resort path: uses `_GENRE_FRAMING[genre]` lines (genre-appropriate), not the old SaaS "helps your team get real work done".
- Seeded `_BRAND_WORLD_KNOWLEDGE` for the 7 new brands, GENRE-grounded: theverge (readers/coverage), airbnb (hosts/guests/stays), allbirds (wool/eco footwear), huckberry (outdoor gear), vercel (frontend deploy/preview), notion (docs/wiki/projects), webflow (visual web design/no-code).
- `validate_planner.company_facts_block(facts, company_url, genre=None)` now takes genre; the thin-scrape world-knowledge ENRICHMENT examples are genre-specific (`_GENRE_ENRICH_EXAMPLES`) — media/marketplace/retail examples instead of only Stripe/Linear/Plaid SaaS — plus a "do NOT default to 'gives your team'" nudge. `genre=None` → historical SaaS list (unchanged for SaaS).

## FIX 2 — kill stage-direction artifacts (`plan_job.py` + `validate_planner.py`)
- ORIGIN: VO beats flow straight from the plan into align_vo/TTS and titles — there was NO beat-level sanitizer. A small planner leaks the scene BRIEF or a prompt fragment into beat text (notion "Show call-to-action: 'Start").
- NEW `_is_stage_direction_text()` + `_degut_stage_direction_beats()` in plan_job.py, run on the FINAL plan (LLM AND template paths) BEFORE `_degut_bare_wordmark_beats`. Rewrites any instruction/stage-direction beat ("Show call-to-action:", "Display the…", "Animated…", leaked briefs like "<Brand> wordmark and tagline" / "Real captured homepage … in a branded browser card", "CTA:"/"Title:" labels) into grounded spoken copy for THIS brand (CTA-shaped → grounded CTA; title → open line; else home line). Conservative: keeps legit copy that merely starts with "Show your work…".
- `validate_planner.SYSTEM_PROMPT` VOICEOVER RULES: added a "NO STAGE DIRECTIONS / INSTRUCTIONS AS COPY — automatic FAIL" block listing the banned shapes (prevention at the LLM level).

## VERIFY (isolated, $0, no brand_extract)
- Genre detection: theverge→media, airbnb→marketplace, allbirds→ecommerce, stripe→fintech (host-hint), + keyword-path cases (unknown news→media, unknown shop→ecommerce, unknown tool→dev-tool) — ALL PASS.
- Deterministic template (theverge media, allbirds retail, notion, + UNKNOWN media/retail brands not in the map): all VO beats genre-appropriate (theverge = journalism/coverage, allbirds = products/materials), NO "gives your team", NO stage-direction artifact. plan_job end-to-end STANDARD: schema-valid, durations sum to 30.
- Stage-direction killer: notion "Show call-to-action: 'Start" → "Build your workspace on Notion."; "Animated feature beat…" → real spoken copy. Legit "Show your work to the world." kept.
- `./run_all_tests.sh --no-eval` → ALL TESTS PASSED, $0 (EXIT 0). Build green.

## Caveats / notes for the orchestrator regenerate
- Stripe now also receives a fintech GENRE HINT on the LLM path (was the SaaS default). Additive/correct (Stripe IS fintech); scene-type rules unchanged. Watch Stripe's score doesn't regress.
- The retail "Plus <feat> — built right into the product." inner line is mildly product-y but not a banned phrase; left as-is (existing pattern, out of scope).
- LLM-path genre grounding (genre_hint in the system prompt) is validated by the orchestrator's full regenerate (needs the brain key), not in this isolated $0 verify.
