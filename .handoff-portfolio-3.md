# Handoff — Portfolio breadth + v2-formula validation, real Seedance (iteration 7)

## Budget (HARD gates honored)
- START balance: **2219.3** credits (`higgsfield account status`; matches brief's ~2219.3).
- END balance: **2165.3** credits.
- **This iteration used: 54cr** (2 × 27cr Seedance, 6s 16:9 720p). Under the ≤70cr allotment; far above the 2023.3 floor.
- Cost-previewed FREE before EVERY submit (`generate cost seedance_2_0 --duration 6 --aspect_ratio 16:9 --prompt "$(cat file)"` = 27cr each — note the model is a POSITIONAL `<job_set_type>`, NOT `--model`; and the CLI needs `--prompt`, so write prompt to a file then pass it).
- Balance checked after each gen: 2219.3 → 2192.3 (Shopify v2) → 2165.3 (NVIDIA). Each charged exactly 27cr as previewed. NO retry-on-spend.
- A 3rd PAID gen would have been 81cr cumulative this iter (> 70 allotment) → HARD STOP. Did not happen.
- **Cumulative overnight: 258/400** (204 prior + 54 this iteration).

## What ran (direct higgsfield, NO build pipeline; read-only on code)
Imported `adapters.cinematic_prompt`, `adapters.detect_kind` (now v2), `remotion_codegen.palette_for`.
Both scenes carry `model="seedance_2_0"` → `detect_kind` correctly forced the anti-text `establish`
track (detectkind video-override still working). Brand hexes merged into the returned palette dict
(`dict(palette_for(url), accent=..., accent2=...)`); NO code edits.

v2 formula confirmed live in the prompt (verified by printing before each gen):
- single-hero cue present (`a single hero object as the sole focal point … no array of cards`);
- only text noun is the TRAILING pure-negative clause (`no text, no words, …`);
- SATURATED hex injected: Shopify `#5E8E3E`→`#468E15`, `#95BF47`→`#86BF1D`; NVIDIA `#76B900` (already
  saturated, unchanged), `#A4D233`→`#9ED220`; prefixed "vivid saturated brand-accent palette".
- Neutral phrasing in both briefs (single device / hero object / GPU compute module) — neither gen
  tripped nsfw.
- (Cosmetic only: brief ends in "." then the ". " join yields a harmless double-period after the
  subject sentence. Not worth a code edit; read-only this iter; matches how iter-5 ran.)

### Clip 1 — SHOPIFY v2 RE-TEST (e-commerce / merchant platform)
- url `https://shopify.com` → `_default`/generic palette, merged real Shopify greens `#5E8E3E`/`#95BF47`
  (→ saturated `#468E15`/`#86BF1D`).
- Brief (NEUTRAL, single-hero, NO UI-text nouns): "Establishing opener plate for Shopify … A premium
  dark studio: a single sleek e-commerce storefront device, one hero product silhouette drifting in
  soft focus over a glossy reflective floor, gentle volumetric haze, shallow depth of field."
- kind = `establish`. Job `9b8504c5-4e22-4fe0-9bf2-a77dc2d7858f`. Completed, charged 27cr.
- Saved `evidence/portfolio/shopify-v2.mp4` (1280×720, 6.08s, 24fps) + frames f0.5/f3.0/f5.5 +
  `shopify-v2-sheet.png` + `shopify-v2-bottom-zoom.png` + `shopify-v2-submit.json`.
- Kept the iter-5 `shopify.mp4` intact for comparison (named the new one `-v2` per brief).

### Clip 2 — NVIDIA (hardware / accelerated-computing / AI — NEW genre) — DELIVERED
- url `https://nvidia.com` → `_default`/generic palette, merged real NVIDIA green `#76B900` / tint
  `#A4D233` (→ `#76B900` unchanged / `#9ED220`).
- Brief (NEUTRAL, single-hero, NO UI-text nouns): "Establishing opener plate for NVIDIA … A premium
  dark studio: a single sleek GPU compute module as the hero object, machined metal and dark heatsink
  fins catching a cool key light, drifting in slow push-in over a glossy reflective floor with soft
  volumetric haze and shallow depth of field."
- kind = `establish`. Job `4721b6ca-5eb2-4a3b-90d8-027a021875ff`. Completed, charged 27cr.
- Saved `evidence/portfolio/nvidia.mp4` (1280×720, 6.06s, 24fps) + frames f0.5/f3.0/f5.5 +
  `nvidia-sheet.png` + `nvidia-bottom-zoom.png` + `nvidia-submit.json`.

## ===== v2-vs-v1 SHOPIFY VERDICT (the headline result) =====

**GARBLE: FIXED.** ✅
- iter-5 v1 (`shopify.mp4`): multi-element array of frosted-glass dashboard cards + a clear garbled
  caption strip along the bottom edge ("The eranstimes re mowh mse conum thes orfestting…" — see
  `shopify-caption-zoom.png`).
- iter-7 v2 (`shopify-v2.mp4`): SINGLE hero device (one phone/storefront silhouette) on a glossy
  reflective floor with volumetric haze swirls. **ZERO caption strip** — the bottom-edge zoom
  (`shopify-v2-bottom-zoom.png`) shows only clean haze and the device's lower edge. No fake pseudo-text
  anywhere. The single-hero cue did exactly its job: no multi-element UI surface = no text to garble.
  This is the primary v2 hypothesis and it held cleanly.

**BRAND GREEN: NOT fixed.** ❌ (different failure mode than v1, but green still absent)
- The saturated `#468E15` did NOT steer here. The device rim reads warm amber/copper on the left edge
  (f3.0/f5.5) and iridescent purple-blue on the right edge (f0.5) — NOT Shopify green.
- v1 failed by reading the dashboard accents ORANGE; v2 failed by reading the rim amber/purple. The
  saturation boost alone was insufficient to overcome the model's default cool-studio + warm-rim look
  for this hue. (Contrast NVIDIA below, where a saturated single green DID steer.)
- Net: v2 fixed the more serious defect (garble) but the brand-color steer for Shopify green remains
  weak. A future Shopify re-roll likely needs an explicit color-DOMINANCE phrase in the brief
  ("rim light dominated by vivid green glow") rather than just a palette-line hex — a
  `cinematic_prompt`/brief change, NOT attempted (read-only this iter).

## Frame observations (quality / brand / garble)

**SHOPIFY v2 — clean single-hero render; garble GONE; brand-green still missing. Portfolio-worthy as a
generic premium device plate, NOT as a brand-green Shopify plate.**
- Beautiful dark studio, single device push-in (distant tiny silhouette f0.5 → centered hero f5.5),
  glossy mirror floor, elegant volumetric haze ribbons. Filmic, Apple-keynote energy. Cleaner and less
  "busy" than the v1 multi-card version.
- Motion: clean push-in, no warp/melt, holds 6s. Strong top + side negative space for a title overlay;
  no bottom caption to crop now.
- GARBLE: none. Anti-text guard + single-hero held perfectly.
- BRAND: rim is amber/purple, not green. Reads as a generic premium phone, not specifically Shopify.

**NVIDIA — agency-grade, PORTFOLIO-WORTHY, brand-green steers reliably.**
- Premium dark studio, single hero GPU graphics card (dual-fan shroud) pushing in distant→centered on a
  glossy reflective floor with volumetric haze. Machined-metal read, filmic grade.
- BRAND: by f5.5 the card carries a vivid, unmistakable **NVIDIA-green (`#76B900`) edge glow** outlining
  the whole card — the signature brand green steered perfectly. Confirms the iter-5 rule: a SATURATED
  SINGLE green (NVIDIA `#76B900`, like Robinhood `#00C805`) steers reliably; a muted/olive green
  (Shopify) does not, even after the saturation boost.
- GARBLE: none. Bottom-edge zoom (`nvidia-bottom-zoom.png`) shows only the green-glow reflection + haze.
  Single hardware object = no UI text surface = clean by construction.
- Title room: strong negative space top + sides around the centered card. Real overlay room.
- NEW genre added to the reel: hardware / accelerated-computing / AI compute (distinct from payments,
  dev-tool, travel, fintech, e-commerce).

## Portfolio-worthy verdict
- `evidence/portfolio/nvidia.mp4` — **YES, clean portfolio piece.** Strong NVIDIA-green read, clean
  single-hero push-in, zero garble. New hardware/AI genre. Pairs naturally with the Walk-Ultra / NVIDIA
  hackathon angle.
- `evidence/portfolio/shopify-v2.mp4` — **PARTIAL.** Portfolio-worthy as a *generic* premium single-device
  plate (clean, no garble), but NOT brand-faithful for Shopify (no green). If the reel needs a true
  Shopify-green plate, re-roll with an explicit green-dominance phrase. The v2 garble fix itself is a
  clear win to log.
- (iter-5 `shopify.mp4` v1 left in place for the comparison; superseded by v2 for the clean composition,
  though both miss brand green.)

## v2 validation summary (the experiment's answer)
- **Single-hero cue: VALIDATED.** Eliminated the Shopify caption-strip garble entirely; NVIDIA clean too.
  Keep single-hero as the default for video establish plates.
- **No-positive-UI-text-noun + trailing pure-negative: VALIDATED.** No garbled wordmarks/toasts/captions
  in either clip.
- **Saturated hex: PARTIALLY validated.** Works when the brand hue is already a strong single color
  (NVIDIA green steered). Still insufficient for a muted/olive brand green (Shopify) — saturation alone
  doesn't beat the model's default rim color. Next lever for stubborn hues: explicit color-DOMINANCE
  phrasing in the brief (a future code/brief change, not this read-only iter).

The reel now spans 6 brands across 6 genres:
- iter-3: Stripe (payments infra) — `evidence/seedance-ab/formula.mp4`
- iter-4: Linear (B2B dev tool) — `evidence/portfolio/linear.mp4`; Airbnb (travel) — `airbnb.mp4`
- iter-5: Robinhood (consumer fintech) — `robinhood.mp4`; Shopify v1 (e-commerce) — `shopify.mp4`
- iter-7: Shopify v2 (e-commerce, clean single-hero re-test) — `shopify-v2.mp4`;
  NVIDIA (hardware/AI compute) — `nvidia.mp4`

## Scope honored
- Direct higgsfield only; NO build pipeline; stayed off orchestrator.py / stripe_webhook.py / dashboard.
- Read-only on code (imported the v2 builder; merged brand hexes into the returned palette dict, no edits).
- All clips + frames + submit receipts under `evidence/portfolio/`. OVERNIGHT-LOG.md untouched (findings
  returned for the orchestrator to log).
- Foreground only; `wait` bounded by `perl -e 'alarm(540)'` with Bash timeout matched (560000ms) under the
  600s auto-background cap — neither gen auto-backgrounded; no orphaned jobs.

## Status — DONE
Shopify v2 re-test (garble FIXED, green NOT) + fresh NVIDIA clip (clean, brand-green steered, NEW
hardware/AI genre) delivered and frame-verified. v2 single-hero + no-text-noun refinements validated on
real video; saturated-hex validated only for already-strong hues. Budget gates all honored (54cr;
balance 2165.3 >> floor 2023.3; cumulative 258/400). START 2219.3 → END 2165.3.
