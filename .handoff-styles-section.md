# Handoff — Styles showcase section (Walk Studio dashboard)

Date: 2026-06-21. Status: COMPLETE + verified.

## What was added
A static "Styles" showcase gallery on the dashboard landing (the `#stage-empty`
inner), placed AFTER the hero CTA footnote. Pure HTML/CSS — no app.js, no
build-console JS touched. Reading order on the landing is now:
what-it-is (hero) -> how-it-works (pipeline strip) -> what-to-click (CTAs) ->
**what-it-makes (Styles)**.

- Section label: `04  WHAT WALK STUDIO MAKES` (reuses `.sec-label`, recentered).
- Heading (serif): "Three looks, one agent. Pick a `style`, it re-skins to your
  brand." ("style" set in the coral accent via `.hw-accent-static`).
- Sub: "Style is the genre; the palette is always pulled from your site..."
- 3-up grid (`.styles-grid`, repeat(3,1fr)); collapses to 2-up <=940px, 1-up <=560px.

## The 5 cards (grouped by family, tagged)
| Card | Family tag | YouTube id |
|---|---|---|
| Orinovate | Kinetic-light | FNXrDx8v42I |
| Hotcake | Kinetic-light | BL8IVJ-f14Y |
| Kuli | Kinetic-light | nxFYamrhC5o |
| Trayd | Apple-style | w-YUqHcC9vU |
| JGB Property | Cinematic | OKW6xlPMrpo |

Each card: poster thumbnail (frame doubles as Watch link), family-tag pill,
serif film name, mono "WATCH" link (i-link icon) opening the YouTube watch URL
in a new tab. Hover: card lifts + coral border, poster scales 1.035, coral play
disc fades in (progressive disclosure). Family tag colors use NO second accent:
kinetic=coral, apple=ink, cinematic=dark-inverted scrim.

## Thumbnails — dashboard/assets/styles/*.jpg (all 800px wide, all <=200KB)
Extracted via ffmpeg from a representative mid-video content beat, lanczos
downscale to 800px, JPEG q:v 3. Sources used:
- orinovate.jpg  44.8 KB  <- Orinovate/orinovate-video-zh/out/orinovate-kinetic-light-1080p.mp4 @34s
  (NOTE: brief's primary path ~/Desktop/orinovate-kinetic-light-4K.mp4 did NOT
   exist; used the official kinetic-light 1080p render instead — same piece.)
- hotcake.jpg    22.1 KB  <- Demos/hotcake-promo/out/hotcake-promo-4k.mp4 @26s
- kuli.jpg       55.3 KB  <- Demos/kuli-promo/out/kuli-promo.mp4 @14s
- trayd.jpg      19.6 KB  <- Demos/trayd-promo/out/trayd-promo.mp4 @13s
- jgb.jpg        19.1 KB  <- Demos/jgb-promo/out/jgb-promo-v6.mp4 @33s
All other source paths matched the catalog. No fallbacks needed beyond Orinovate.

## Files edited
- dashboard/index.html  (+ `<section class="styles">` with 5 `.style-card`s)
- dashboard/styles.css   (+ STYLES SHOWCASE block before the responsive @media;
                          + .styles-grid responsive rules)
- dashboard/assets/styles/{orinovate,hotcake,kuli,trayd,jgb}.jpg (NEW)

## Verification (no server restart; preview-managed :3030)
- `curl -sL http://localhost:3030/` -> section present (/ 302s to
  /dashboard/index.html; 18 "styles/style-card" matches when following redirect).
- Each thumbnail URL `/dashboard/assets/styles/<slug>.jpg` -> HTTP 200.
- All 5 thumbnails <=200KB (max kuli 55.3 KB).
- Preview DOM inspect: 5 cards, correct family tags + names + watch hrefs, all 5
  posters loaded at naturalWidth 800. Screenshot confirms aesthetic match.
- `./run_all_tests.sh --no-eval` -> ALL TESTS PASSED — $0 spent.

## Aesthetic conformance
Light near-white surfaces, single coral `--amber (#D6351C)` accent, mono+serif
type, hairline (0.5-1px) borders, the existing radius tier scale (`--r-ctl`,
`--r-card`, `--r-pill`) and motion eases (`--ease-rise`, `--ease-spring`).
Icons from the inline sprite only (i-play, i-link). No emoji. Did not touch
app.js logic, serve.py, the Stripe path, or the build-console section.
