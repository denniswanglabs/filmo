# Handoff — UX delivered-state: video-led payoff (UX-IMPROVEMENTS §5.1/5.2/5.3)

## Scope (ONE problem only)
Make the DELIVERED state the payoff: video hero at top + Download + Share(copy link),
P&L/Studio/source-media collapse BELOW as proof. Touch ONLY dashboard/{app.js,index.html,styles.css}.

## Findings (Phase 0 — read/verify, done)
- Detection: use `l.status === "delivered"` (reliable). `phase` is absent on demo-1-approve
  but present on build-stripe-9e7829 — do NOT rely on phase.
- Render entry: `renderDetail(root,l,runId)` app.js:90. Builds `secs[]` in order
  [P&L, gate, studio, source media, final cut(if stitch), money/log], prepends `slate(l)`.
  Final cut = `viewer(l,runId)` app.js:320, currently section 5/6 — buried.
- final.mp4 served at /runs/<id>/final.mp4 → confirmed `curl -sI` = 200 (demo-1-approve, 11.98MB).
- Hero video src = `/runs/<runId>/<stitch.output_path>` (output_path == "final.mp4").
- Tokens: --amber primary; --neg RESERVED for declines (don't use); --pos sparingly; mono+serif.
- Reuse: .pg-pay (amber filled prominent btn), .pg-badge, .btn, .sec-label, .slate, .viewer.
- Animation rule: time-based rAF only (setInterval throttles). Copy-confirm uses a rAF timer.
- node v22.23.0 at ~/.local/bin/node. serve.py running on :3030 (Python SimpleHTTP). Do NOT restart.

## Plan
1. index.html: add #i-link (share/copy) + #i-play SVG symbols to sprite.
2. app.js: add `deliveredHero(l,runId)` (video hero + "Your <brand> promo is ready" + Download <a download>
   + Share button copy-to-clipboard). In renderDetail, if status==delivered: render
   slate + deliveredHero, then the supporting sections (P&L, gate, studio, source) UNDER a
   "How it was built — proof" disclosure; drop the now-redundant "Final cut" viewer section
   (hero replaces it). Non-delivered runs keep existing order untouched.
   Wire copy-link handler (rAF-based revert) in renderDetail after innerHTML.
3. styles.css: .delivered-hero, .dh-actions, .dh-dl/.dh-share buttons, .proof-fold styling.

## Verify
- node --check dashboard/app.js
- curl -sI :3030/runs/demo-1-approve/final.mp4 == 200
- curl served app.js|index.html|styles.css and grep new identifiers
- (final visual = Dennis in Safari :3030)

## Status: COMPLETE

### Edits made (only dashboard/{index.html,app.js,styles.css})
- index.html: added `#i-link` (share/copy) + `#i-play` SVG symbols to the inline sprite.
- app.js:
  - state += `shareTimer`.
  - `renderDetail` now branches: `delivered = l.status === "delivered" && l.stitch`.
    Delivered → `slate + deliveredHero + .proof-fold{ P&L, gate, studio, source, money/log }`.
    The old inline "Final cut" viewer is dropped on delivered (hero replaces it).
    Non-delivered → original order preserved (viewer spliced into historical slot).
  - new `deliveredHero(l,runId)` — DELIVERED badge + "Your <brand> promo is ready" headline +
    big autoplay-muted-loop-controls `<video class=dh-video>` + `<a download>` Download MP4 +
    Copy-link `#dh-share` button. Amber accent only; no emoji; no red.
  - new `initDeliveredHero` / `fallbackCopy` / `shareConfirmed` — clipboard copy of
    location.origin + /runs/<id>/final.mp4, "Link copied" confirm reverting after 1.6s via rAF.
- styles.css: `.delivered-hero`, `.dh-head/.dh-badge/.dh-title`, `.dh-frame/.dh-video`,
  `.dh-bar/.dh-meta/.dh-actions`, `.dh-dl`(amber filled)/`.dh-share`(+`.copied` uses --pos),
  `.proof-fold`/`.proof-lead`. All reuse existing tokens.

### Verification (read-only, $0) — all PASS
- `node --check dashboard/app.js` → OK.
- `curl -sI :3030/runs/demo-1-approve/final.mp4` → 200.
- served greps: app.js(7 hits), index.html(i-link,i-play), styles.css(12 hits) — live from disk.
- headless structural check vs REAL ledgers demo-1-approve + build-stripe-9e7829:
  hero present & above proof-fold AND above P&L; download(href+download) + share present & above
  proof; P&L kept; old inline viewer gone; brand headline present. 11/11 PASS each fixture.
- non-delivered (status=running) regression check: no hero/proof-fold, original viewer+Final cut
  section retained. PASS.
- Final VISUAL confirmation = Dennis in Safari at :3030 (agent is read-tier, cannot drive Safari).

