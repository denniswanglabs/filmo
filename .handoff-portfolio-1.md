# Handoff — Portfolio breadth, real Seedance (iteration 4)

## Budget (HARD gates honored)
- START balance: **2334.3** credits (recorded 2026-06-20, matches iter-3 END).
- END balance: **2280.3** credits.
- **This iteration used: 54cr** (2 × 27cr Seedance, 6s 16:9 720p). Under the ≤70cr allotment; far above the 2023.3 floor.
- **Cumulative overnight: 143/400** (89 prior + 54 this iteration).
- Cost-previewed FREE before EVERY submit (`generate cost` = 27cr each). NO retry-on-spend.
- A 3rd PAID gen would have been 81cr cumulative this iter (> 70 allotment) → would have been a HARD STOP. Did not happen; see nsfw note below.

## What ran (direct higgsfield, NO build pipeline; read-only on code)
Imported `adapters.cinematic_prompt`, `adapters.detect_kind`, `remotion_codegen.palette_for`.
All scenes carry `model="seedance_2_0"` → `detect_kind` correctly forced the anti-text
`establish` track for every clip (the detectkind-fix override is working end-to-end).

### Clip 1 — LINEAR (B2B / dev tool, kitted palette) — DELIVERED
- url `https://linear.app` → `palette_for` accent `#5E6AD2` / accent2 `#8A93E0` (real Linear indigo, kitted).
- Brief: "Establishing opener plate for Linear, the issue-tracking and product-planning tool…sleek dark workspace with project boards and roadmap timelines drifting in soft focus."
- kind = `establish`. Job `c787a75d-25dc-4e84-a7b2-faa3c2542b66`. Completed, charged 27cr.
- Saved `evidence/portfolio/linear.mp4` (1280×720, 6.04s, 24fps, 145f). Frames f0.5/f3.0/f5.5 + `linear-sheet.png`.

### Clip 2 (first attempt) — SPOTIFY — BLOCKED (nsfw), $0, NOT delivered
- url `https://spotify.com` → `_default` palette, then merged real Spotify hexes `#1DB954`/`#1ED760`.
- Job `99d244d8-1cc1-4aa1-aa0c-3a484921cd88` came back **status `nsfw`**, empty `result_url`.
  Content-filter false positive (prompt was a clean product plate — likely the "mobile listening
  experience" / album-art phrasing tripped it). **NO charge** — balance unchanged (2307.3 before and
  after). This is a moderation block, not a spend; swapping to a different brand is a fresh budgeted
  gen, NOT a paid retry.

### Clip 2 (delivered) — AIRBNB (consumer app) — DELIVERED
- url `https://airbnb.com` → `_default` palette, then merged real Airbnb hexes `#FF385C`/`#FF5A5F` (Rausch/coral).
- Brief: "Establishing opener plate for Airbnb, the home-and-stays booking app…premium warm mobile travel experience with listing photo cards, a map pin and a booking card drifting in soft focus."
- kind = `establish`. Job `c44c0004-5b3e-49ca-809b-bf1e0c6a79c0`. Completed, charged 27cr.
- Saved `evidence/portfolio/airbnb.mp4` (1280×720, 6.04s, 24fps, 145f). Frames f0.5/f3.0/f5.5 + `airbnb-sheet.png`.

Diversity satisfied: B2B dev-tool (Linear) + consumer travel app (Airbnb), distinct genres, distinct palettes (cool indigo vs warm coral), neither is Stripe.

## Frame observations (quality / brand / garble)

**LINEAR — agency-grade, PORTFOLIO-WORTHY.**
- Dark premium studio, volumetric haze, glossy floor reflection. Blue-accent UI cards drift up
  through the lower third with bokeh + shallow DoF — exactly the establish formula. Apple-keynote energy.
- Brand fidelity: indigo/blue accent reads as the Linear family in the card chips and mini-charts.
- GARBLE: anti-text guard HOLDING. Card labels are soft/blurred smudges (faint mark-shapes), NOT
  legible garbled wordmarks. No fake headlines, no garbled toasts (the baseline failure mode from
  iter-3 is absent by construction).
- Title room: huge clean negative space across the top ~60% — real overlay room.
- Motion: tasteful drift/parallax of the card grid, no warp/melt; holds the full 6s.

**AIRBNB — agency-grade, PORTFOLIO-WORTHY (slightly stronger composition than Linear).**
- Warm-rim-lit floating glass cards with travel/landscape photo thumbnails converging toward center
  over a dark hazy studio floor with mirror reflection. Coral map-pin centerpiece + a booking-style
  card top-left. Genuinely premium, the most "finished" of the two.
- Brand fidelity: the coral pin + warm rim light read clearly as Airbnb's Rausch coral. Travel-photo
  content matches the brand's domain (stays/destinations).
- GARBLE: anti-text guard HOLDING. The top-left card's text is soft/illegible; no garbled wordmarks.
- Title room: strong negative space top + both sides; the converging-cards motion clears center by mid-clip.
- Motion: clean card-assembly drift (cards slide in from edges, converge), graceful, no warping.

Both clips confirm: the iter-3 formula generalizes cleanly across brands and genres on real Seedance
video. The detect_kind anti-text override is the load-bearing piece — it kept BOTH video gens on the
legible-free establish track and there is zero text garble in either.

## What to improve next (one note)
**Add a lightweight moderation pre-flight / phrasing guard for the establish brief.** The Spotify
gen was a false-positive nsfw block (cost $0 but wasted ~3 min and a brand slot). The trigger was
almost certainly benign brand/UX phrasing ("mobile listening experience"). Cheapest fix: in the brief
template (or a thin wrapper at the call site), prefer neutral nouns — "product surface / app
interface / device mockup" over "experience / body / listening" — and avoid words a video moderator
over-indexes on. Even better, since `generate cost` is FREE, a future iteration could add a $0
moderation dry-run if the CLI exposes one. NOTE: this is a CODE/template suggestion only — read-only
this iteration, did NOT edit. (Carrying forward iter-3's still-open note too: the detectkind video
override is already landed and verified working here.)

## Portfolio-worthy verdict
- `evidence/portfolio/airbnb.mp4` — **YES, lead portfolio piece.** Most polished, clearest brand cue, best motion.
- `evidence/portfolio/linear.mp4` — **YES, portfolio piece.** Clean B2B/dev-tool establish plate, great title room.
- Both pair well with the iter-3 `evidence/seedance-ab/formula.mp4` (Stripe) as a 3-brand establish-plate reel.

## Scope honored
- Direct higgsfield only; NO build pipeline; stayed off orchestrator.py / stripe_webhook.py / dashboard.
- Read-only on code (imported the builder; merged brand hexes into the returned palette dict, no edits).
- All clips + frames under `evidence/portfolio/`. OVERNIGHT-LOG.md untouched (findings returned).
- Submit JSON receipts saved: linear-submit.json, spotify-submit.json, airbnb-submit.json.

## Status — DONE
2 diverse brand-faithful establishing clips delivered + frame-verified, formula proven to generalize,
budget gates all honored (54cr, balance 2280.3 >> floor 2023.3), balances recorded.
