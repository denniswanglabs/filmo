# Handoff — re-baseline tests/test_pricing.py (cost-plus -> fixed-tier)

## Status: DONE. 25 tests, 0 failures (was ~11 failures).

Scope obeyed: edited ONLY `tests/test_pricing.py`. pricing.py / pricing.json / producer.py untouched.

Run: `PYTHONPATH="$PWD" python3 -m unittest tests.test_pricing -v` -> OK, Ran 25 tests.

## Constants added
STANDARD_PRICE = 999 ($9.99), PREMIUM_PRICE = 1999 ($19.99) — from pricing.json tier_price_cents.

## Re-baselined assertions (old -> new)
- model string: `out["model"] == "cost_plus"` -> `"fixed_tier"` (line_items_shape, price_selection shim).
- standard price: `price_for_plan(.., standard).total_price_cents == 500 (floor)` -> `999`.
- premium price: `price_for_plan(.., premium).total_price_cents == 600` -> `1999`.
- line item price_cents 600 -> PREMIUM_PRICE (1999); margin (600-100)/600 -> (1999-100)/1999.
- producer suggested_price_cents: standard 500 -> 999; premium 600 -> 1999; default 500 -> 999.
- menu["model"] cost_plus -> fixed_tier (on_path_default test).
- legacy premium_vo / price_selection shim total_price_cents 600 -> 1999.
- header docstring rewritten cost-plus -> fixed-tier.

## Renamed/repurposed tests (coverage preserved, not removed)
- TestCostPlusCore -> TestFixedTierCore.
- `test_price_is_cogs_times_markup_floored_rounded` -> `test_base_price_for_cogs_is_cogs_times_markup_floored_rounded` (same 3 base_price_for_cogs asserts, unchanged math — that fn is retained).
- `test_typical_dollar_cogs_video_prices_5_to_9` (asserted old $5-9 band) -> replaced by `test_price_is_fixed_tier_independent_of_cogs` (loops cogs 0..5000, asserts flat 999/1999). The old $5-9 band assertion no longer describes the model and was dropped; coverage replaced by the COGS-independence loop.
- `test_price_floor_applies_to_cheap_plans` -> `test_cheap_plan_still_pays_full_tier_price` (tiny COGS still = 999).
- `test_premium_cogs_floor_lifts_price_above_standard` -> `test_premium_priced_above_standard`.
- `test_zero_cogs_floors` -> `test_zero_cogs_still_pays_full_tier_price`.
- `test_on_path_standard_floors_and_drops_higgsfield_vo_cogs` -> `test_on_path_standard_drops_higgsfield_vo_cogs_flat_price`.

## Tests ADDED (new behavior)
- `test_price_is_fixed_tier_independent_of_cogs` — price flat 999/1999 across many COGS values.
- `test_premium_price_independent_of_cogs_margin_below_one` — premium cogs=300 -> price 1999, margin = (1999-300)/1999, 0 < margin < 1.0 (the COGS-independence requirement).
- `test_tier_price_cents_helper` — new pricing.tier_price_cents("standard"/"premium") == 999/1999.
- `test_base_price_for_cogs_floor` — retained cost-plus floor (base_price_for_cogs(0) == 500), keeps floor coverage that the old zero-cogs test used to give.

## Tests KEPT intact (still valid)
- base_price_for_cogs rounding (test_rounds_to_nearest_50), bad-cogs raises, quality flags,
  default selection, no-consent, price_for_selection, all legacy back-compat mapping,
  producer OFF-path byte-identity (test_off_path_unchanged / _equals_plan_without_selection).

## Assertions intentionally NOT changed (would have been wrong to change)
- `on["pricing_mode"] == "cost_plus"` (producer.py:338 still hard-codes the string "cost_plus";
  producer.py is OUT OF SCOPE and was not touched). The PRICE producer carries (suggested_price_cents)
  IS the fixed tier price and is asserted as 999/1999. Added an inline comment noting the producer tag
  is stale. FLAG FOR ORCHESTRATOR: if you want producer's pricing_mode tag itself to read "fixed_tier",
  that's a one-line producer.py change (line 338) + flip this one assertion — separate from test scope.

## Assertion removed + why
- The "$5-9 band" assertions (`assertTrue(500 <= total <= 900)`) were removed: they tested cost-plus
  banding that no longer exists under fixed tiers. Replaced with exact-value assertions (999/1999) — a
  STRENGTHENING, not a loosening.
