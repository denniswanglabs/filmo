# P2 walkthrough-player — handoff

## DONE (edits complete)
- `studio/src/timeline/types.ts` — added `"walkthrough-player"` to Archetype union (kept apple-screenshot); added SceneData fields `videoSrc/videoFit/overlayTitle/muteClip`.
- `studio/src/timeline/archetypes/WalkthroughPlayer.tsx` — NEW. OffthreadVideo in brand-tinted frame + kinetic title bar (overlayTitle + accent underline on cues `card-in`/`title-in`/`accent`). muteClip default true. never-blank placeholder.
- `studio/src/timeline/Timeline.tsx` — import + dispatch branch for walkthrough-player (reuses resolveAsset). KEPT apple-screenshot branch.
- `build_timeline.py` — added `media_duration_s` (or ffprobe of media_path/video_path) as a scene-length FLOOR (`media_frames`) in all 3 length branches.
- `style_fill.py` — ARCH_WALKTHROUGH const; `_shape_walkthrough` (videoSrc/overlayTitle/muteClip=True/videoFit); role_map walkthrough+demo -> ARCH_WALKTHROUGH; archetype_for downgrades to ARCH_EXPLAINER when no clip (never-blank); registered shaper; `_stage_audio` now stages videoSrc into studio/public/.

## VERIFIED (all done, $0, no NIM)
- Re-encode: /tmp/walk-smoke.mp4 (62s 1920x1080 h264+aac) -> studio/public/walk-smoke-test.mp4 (62s, faststart, -an, 2.8MB).
- props: runs/p2-walkthrough-verify/props.json (Stripe theme; scene0 walkthrough-player + scene1 apple-screenshot).
- Stills rendered + READ:
  - walk-f60.png: branded frame + "Stripe — Dashboard walkthrough" title bar (violet dot+underline) + "stripe.com/dashboard" caption; clip is BLACK at f60 (clip's own ~2s black intro) but cursor visible -> OffthreadVideo IS playing.
  - walk-f220.png: real Stripe Pricing page playing inside the frame, contain-fit letterboxed, overlay legible & on-brand. (Clip is the FINISHED explainer -> baked-in green step bar visible; LOOP-STATE says prefer raw base.mp4 for clean branding, P3 concern.)
  - shot-f420.png: P1 apple-screenshot STILL WORKS — real Orinovate homepage in browser card, coexists.
- tsc --noEmit: clean (both archetypes in union + dispatch).
- ./run_all_tests.sh --no-eval: ALL TESTS PASSED — $0 spent (3 OK blocks, 15/15 stripe checks, 0 fail/error). Added tests: test_style_fill.test_walkthrough_routes_to_player_only_with_clip; test_build_timeline.test_media_duration_floors_scene_length + does_not_shrink_longer_voice. No rebaselining needed (no existing assertion broke).

VERDICT: walkthrough-player plays the clip inside a clean branded frame with a legible on-brand overlay title bar. PASS.
